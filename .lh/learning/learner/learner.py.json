{
    "sourceFile": "learning/learner/learner.py",
    "activeCommit": 0,
    "commits": [
        {
            "activePatchIndex": 75,
            "patches": [
                {
                    "date": 1727819756744,
                    "content": "Index: \n===================================================================\n--- \n+++ \n"
                },
                {
                    "date": 1727820445303,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,244 @@\n+import logging\n+import time\n+from collections import defaultdict\n+from pathlib import Path\n+from termcolor import colored\n+from typing import List, MutableSet, Dict\n+import clingo\n+import pymimir as mm\n+from dlplan.policy import PolicyMinimizer, NamedBoolean, NamedNumerical\n+from collections import defaultdict\n+from .src.exit_codes import ExitCode\n+from .src.iteration import EncodingType, ASPFactory, ClingoExitCode, IterationData, LearningStatistics, Sketch, D2sepDlplanPolicyFactory, ExplicitDlplanPolicyFactory, compute_feature_pool, compute_per_state_feature_valuations, compute_state_pair_equivalences, compute_tuple_graph_equivalences, minimize_tuple_graph_equivalences\n+from .src.util import Timer, create_experiment_workspace, change_working_directory, write_file, change_dir, memory_usage, add_console_handler, print_separation_line\n+from .src.preprocessing import InstanceData, PreprocessingData, StateFinder, compute_instance_datas, compute_tuple_graphs\n+\n+def learn_sketch_for_problem_class(\n+    domain_filepath: Path,\n+    problems_directory: Path,\n+    workspace: Path,\n+    width: int,\n+    disable_closed_Q: bool = True,\n+    max_num_states_per_instance: int = 10000,\n+    max_time_per_instance: int = 10,\n+    encoding_type: EncodingType = EncodingType.EXPLICIT,\n+    max_num_rules: int = 1,\n+    enable_goal_separating_features: bool = True,\n+    disable_feature_generation: bool = True,\n+    enable_incomplete_feature_pruning: bool = False,\n+    concept_complexity_limit: int = 9,\n+    role_complexity_limit: int = 9,\n+    boolean_complexity_limit: int = 9,\n+    count_numerical_complexity_limit: int = 9,\n+    distance_numerical_complexity_limit: int = 9,\n+    feature_limit: int = 1000000,\n+    additional_booleans: List[str] = None,\n+    additional_numericals: List[str] = None,\n+    enable_dump_files: bool = False,\n+):\n+    # Initialize variables and setup\n+    if additional_booleans is None:\n+        additional_booleans = []\n+    if additional_numericals is None:\n+        additional_numericals = []\n+    instance_filepaths = list(problems_directory.iterdir())\n+   \n+\n+    add_console_handler(logging.getLogger(), logging.INFO)\n+    create_experiment_workspace(workspace)\n+    change_working_directory(workspace)\n+\n+    total_timer = Timer()\n+    preprocessing_timer = Timer()\n+    asp_timer = Timer(stopped=True)\n+    verification_timer = Timer(stopped=True)\n+    iteration_data = IterationData()\n+\n+    # Generate data\n+    with change_dir(\"input\"):\n+        logging.info(colored(\"Constructing InstanceDatas...\", \"blue\", \"on_grey\"))\n+        domain_data, instance_datas, num_ss_states, num_gfa_states = compute_instance_datas(domain_filepath, instance_filepaths, disable_closed_Q, max_num_states_per_instance, max_time_per_instance, enable_dump_files)\n+        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+        if instance_datas is None:\n+            raise Exception(\"Failed to create InstanceDatas.\")\n+\n+        state_finder = StateFinder(domain_data, instance_datas)\n+\n+        logging.info(colored(\"Initializing TupleGraphs...\", \"blue\", \"on_grey\"))\n+        gfa_state_id_to_tuple_graph: Dict[int, mm.TupleGraph] = compute_tuple_graphs(domain_data, instance_datas, state_finder, width, enable_dump_files)\n+        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+    preprocessing_data = PreprocessingData(domain_data, instance_datas, state_finder, gfa_state_id_to_tuple_graph)\n+    preprocessing_timer.stop()\n+\n+    if not preprocessing_data.instance_datas:\n+        raise RuntimeError(\"Data is empty\")\n+\n+    sketches = set()\n+    total_features = set()\n+    sketch_features = set()\n+    # Learn sketch\n+    if encoding_type == EncodingType.EXPLICIT:\n+        create_experiment_workspace(workspace)\n+        preprocessing_timer.resume()\n+\n+        for instance_data in preprocessing_data.instance_datas:\n+            print(f\"Instance {instance_data.idx}:\")\n+            for state in instance_data.mimir_ss.get_states():\n+                    state_data = state.get_state()\n+                    fluents = state_data.get_fluent_atoms()\n+                    #print(f\"State {state.get_index()} fluents: {fluents}\")\n+            # TODO: when is the best time to generate features?\n+            # Based on current instance currently looks most reasonable.\n+            # It could be restricted to the states in the tuple graph as well,\n+            # which reduces the number of rules, but not fully sure yet about the consequences in the bigger picture.\n+            iteration_data = IterationData()\n+            iteration_data.instance_datas = [instance_data]\n+            iteration_data.gfa_states = instance_data.gfa.get_states()\n+\n+            # Report some progress\n+            write_file(f\"{instance_data.idx}.dot\", instance_data.dlplan_ss.to_dot(1))\n+            print(\"     \", end=\"\")\n+            print(\"id:\", instance_data.idx,\n+                    \"problem_filepath:\", instance_data.mimir_ss.get_problem().get_filepath(),\n+                    \"num_states:\", instance_data.mimir_ss.get_num_states(),\n+                    \"num_state_equivalences:\", instance_data.gfa.get_num_states())\n+\n+            logging.info(colored(\"Initializing DomainFeatureData...\", \"blue\", \"on_grey\"))\n+            iteration_data.feature_pool = compute_feature_pool(\n+                preprocessing_data,\n+                iteration_data,\n+                gfa_state_id_to_tuple_graph,\n+                state_finder,\n+                disable_feature_generation,\n+                enable_incomplete_feature_pruning,\n+                concept_complexity_limit,\n+                role_complexity_limit,\n+                boolean_complexity_limit,\n+                count_numerical_complexity_limit,\n+                distance_numerical_complexity_limit,\n+                feature_limit,\n+                additional_booleans,\n+                additional_numericals\n+            )\n+            # Add features to features that were generated\n+            total_features.update(feature.dlplan_feature for feature in iteration_data.feature_pool)\n+\n+            logging.info(colored(\"Constructing PerStateFeatureValuations...\", \"blue\", \"on_grey\"))\n+            iteration_data.gfa_state_global_idx_to_feature_evaluations = compute_per_state_feature_valuations(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Constructing StatePairEquivalenceDatas...\", \"blue\", \"on_grey\"))\n+            iteration_data.state_pair_equivalences, iteration_data.gfa_state_global_idx_to_state_pair_equivalence = compute_state_pair_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Constructing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n+            iteration_data.gfa_state_global_idx_to_tuple_graph_equivalence = compute_tuple_graph_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Minimizing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n+            minimize_tuple_graph_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+            preprocessing_timer.stop()\n+            asp_timer.resume()\n+\n+            # We count the number of subgoal tuples for which no rule could be found.\n+            # This usually happens if the pool of features is not sufficiently rich.\n+            count_unsat_tuples = 0\n+\n+            for gfa_state in instance_data.gfa.get_states():\n+                gfa_state_global_idx = gfa_state.get_global_index()\n+                gfa_state_idx = gfa_state.get_index()\n+\n+                if instance_data.gfa.is_deadend_state(gfa_state_idx):\n+                    continue\n+\n+                #print(f\"Keys in gfa_state_global_idx_to_tuple_graph: {list(preprocessing_data.gfa_state_global_idx_to_tuple_graph.keys())}\")\n+                #print(f\"gfa_state_global_idx: {gfa_state_global_idx}\")\n+\n+                tuple_graph = preprocessing_data.gfa_state_global_idx_to_tuple_graph[gfa_state_global_idx]\n+\n+                for distance, group in enumerate(tuple_graph.get_vertices_grouped_by_distance()):\n+                    if distance == 0:\n+                        # We skip subgoal tuples at distance zero because they do not encode progress towards a goal.\n+                        continue\n+                    for vertex in group:\n+                        # Here we find all simplest single sketch rules for a pair (state, subgoal tuple).\n+                        t_idx = vertex.get_index()\n+\n+                        start_time = time.time()\n+                        asp_factory = ASPFactory(encoding_type, enable_goal_separating_features, max_num_rules)\n+                        facts = asp_factory.make_facts(preprocessing_data, iteration_data, gfa_state, t_idx)\n+\n+                        print(f\"Factory time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        start_time = time.time()\n+                        asp_factory.ground(facts)\n+                        print(f\"Grounding time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        start_time = time.time()\n+                        symbolss, returncode = asp_factory.solve_all_opt()\n+                        print(f\"Solving time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        if returncode in [ClingoExitCode.UNSATISFIABLE, ClingoExitCode.EXHAUSTED]:\n+                            print(colored(\"ASP is unsatisfiable or exhausted!\", \"red\", \"on_grey\"))\n+                            print(colored(f\"No sketch of width {width} exists that solves all instances!\", \"red\", \"on_grey\"))\n+                            # There might be tuples where we get unsat.\n+                            # If such cases occur, which is very likely in complex domains, then we must look closer into this.\n+                            count_unsat_tuples += 1\n+                            continue\n+                        elif returncode == ClingoExitCode.UNKNOWN:\n+                            print(colored(\"ASP solving throws unknown error!\", \"red\", \"on_grey\"))\n+                            exit(ExitCode.UNKNOWN)\n+                        elif returncode == ClingoExitCode.INTERRUPTED:\n+                            print(colored(\"ASP solving interrupted!\", \"red\", \"on_grey\"))\n+                            exit(ExitCode.INTERRUPTED)\n+                        else:\n+                            logging.info(f\"Solution found with return code: {returncode}\")\n+\n+                        for symbols in symbolss:\n+                            dlplan_policy = ExplicitDlplanPolicyFactory().make_dlplan_policy_from_answer_set(symbols, preprocessing_data, iteration_data)\n+\n+                            sketch = Sketch(dlplan_policy, width)\n+                            sketches.add(sketch)\n+                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_booleans())\n+                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_numericals())\n+                            print(dlplan_policy)\n+\n+    else:\n+        raise Exception(\"No implementation for the given encoding type.\")\n+\n+    preprocessing_timer.stop()\n+    asp_timer.stop()\n+    verification_timer.stop()\n+    total_timer.stop()\n+\n+    # Compute feature histograms by complexity\n+    total_features_by_complexity = defaultdict(int)\n+    for feature in total_features:\n+        total_features_by_complexity[feature.compute_complexity()] += 1\n+    total_features_by_complexity = {key: total_features_by_complexity[key] for key in sorted(total_features_by_complexity)}\n+\n+    sketch_features_by_complexity = defaultdict(int)\n+    for feature in sketch_features:\n+        sketch_features_by_complexity[feature.compute_complexity()] += 1\n+    sketch_features_by_complexity = {key: sketch_features_by_complexity[key] for key in sorted(sketch_features_by_complexity)}\n+\n+    # Output the result\n+    with change_dir(\"output\"):\n+        print_separation_line()\n+        logging.info(colored(\"Summary:\", \"green\", \"on_grey\"))\n+\n+        print(f\"Preprocessing time: {int(preprocessing_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"ASP time: {int(asp_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Verification time: {int(verification_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Total time: {int(total_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Total memory: {int(memory_usage() / 1024)} GiB.\")\n+        print(f\"Total number of states: {num_ss_states}\")\n+        print(f\"Total number of abstract states: {num_gfa_states}\")\n+        print(f\"Number of unsat tuples: {count_unsat_tuples}\")\n+        print(f\"Number of sketch rules: {len(sketches)}\")\n+        print(f\"Number of total features: {len(total_features)}\")\n+        print(f\"Number of sketch features: {len(sketch_features)}\")\n+        print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n+        print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n"
                },
                {
                    "date": 1727820487539,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -164,9 +164,9 @@\n                         continue\n                     for vertex in group:\n                         # Here we find all simplest single sketch rules for a pair (state, subgoal tuple).\n                         t_idx = vertex.get_index()\n-\n+                        print(f\"State Transition: s_i = {gfa_state_global_idx}, s_j = {t_idx}\")\n                         start_time = time.time()\n                         asp_factory = ASPFactory(encoding_type, enable_goal_separating_features, max_num_rules)\n                         facts = asp_factory.make_facts(preprocessing_data, iteration_data, gfa_state, t_idx)\n \n"
                },
                {
                    "date": 1727820630196,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -144,8 +144,9 @@\n \n             # We count the number of subgoal tuples for which no rule could be found.\n             # This usually happens if the pool of features is not sufficiently rich.\n             count_unsat_tuples = 0\n+            total_transitions = 0 \n \n             for gfa_state in instance_data.gfa.get_states():\n                 gfa_state_global_idx = gfa_state.get_global_index()\n                 gfa_state_idx = gfa_state.get_index()\n@@ -165,8 +166,9 @@\n                     for vertex in group:\n                         # Here we find all simplest single sketch rules for a pair (state, subgoal tuple).\n                         t_idx = vertex.get_index()\n                         print(f\"State Transition: s_i = {gfa_state_global_idx}, s_j = {t_idx}\")\n+                        total_transitions += 1\n                         start_time = time.time()\n                         asp_factory = ASPFactory(encoding_type, enable_goal_separating_features, max_num_rules)\n                         facts = asp_factory.make_facts(preprocessing_data, iteration_data, gfa_state, t_idx)\n \n@@ -233,8 +235,9 @@\n         print(f\"ASP time: {int(asp_timer.get_elapsed_sec()) + 1} seconds.\")\n         print(f\"Verification time: {int(verification_timer.get_elapsed_sec()) + 1} seconds.\")\n         print(f\"Total time: {int(total_timer.get_elapsed_sec()) + 1} seconds.\")\n         print(f\"Total memory: {int(memory_usage() / 1024)} GiB.\")\n+        print(f\"Total number of transitions: {total_transitions}\")\n         print(f\"Total number of states: {num_ss_states}\")\n         print(f\"Total number of abstract states: {num_gfa_states}\")\n         print(f\"Number of unsat tuples: {count_unsat_tuples}\")\n         print(f\"Number of sketch rules: {len(sketches)}\")\n"
                },
                {
                    "date": 1727820710377,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -244,248 +244,4 @@\n         print(f\"Number of total features: {len(total_features)}\")\n         print(f\"Number of sketch features: {len(sketch_features)}\")\n         print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n         print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n-import logging\n-import time\n-from collections import defaultdict\n-from pathlib import Path\n-from termcolor import colored\n-from typing import List, MutableSet, Dict\n-import clingo\n-import pymimir as mm\n-from dlplan.policy import PolicyMinimizer, NamedBoolean, NamedNumerical\n-from collections import defaultdict\n-from .src.exit_codes import ExitCode\n-from .src.iteration import EncodingType, ASPFactory, ClingoExitCode, IterationData, LearningStatistics, Sketch, D2sepDlplanPolicyFactory, ExplicitDlplanPolicyFactory, compute_feature_pool, compute_per_state_feature_valuations, compute_state_pair_equivalences, compute_tuple_graph_equivalences, minimize_tuple_graph_equivalences\n-from .src.util import Timer, create_experiment_workspace, change_working_directory, write_file, change_dir, memory_usage, add_console_handler, print_separation_line\n-from .src.preprocessing import InstanceData, PreprocessingData, StateFinder, compute_instance_datas, compute_tuple_graphs\n-\n-def learn_sketch_for_problem_class(\n-    domain_filepath: Path,\n-    problems_directory: Path,\n-    workspace: Path,\n-    width: int,\n-    disable_closed_Q: bool = True,\n-    max_num_states_per_instance: int = 10000,\n-    max_time_per_instance: int = 10,\n-    encoding_type: EncodingType = EncodingType.EXPLICIT,\n-    max_num_rules: int = 1,\n-    enable_goal_separating_features: bool = True,\n-    disable_feature_generation: bool = True,\n-    enable_incomplete_feature_pruning: bool = False,\n-    concept_complexity_limit: int = 9,\n-    role_complexity_limit: int = 9,\n-    boolean_complexity_limit: int = 9,\n-    count_numerical_complexity_limit: int = 9,\n-    distance_numerical_complexity_limit: int = 9,\n-    feature_limit: int = 1000000,\n-    additional_booleans: List[str] = None,\n-    additional_numericals: List[str] = None,\n-    enable_dump_files: bool = False,\n-):\n-    # Initialize variables and setup\n-    if additional_booleans is None:\n-        additional_booleans = []\n-    if additional_numericals is None:\n-        additional_numericals = []\n-    instance_filepaths = list(problems_directory.iterdir())\n-   \n-\n-    add_console_handler(logging.getLogger(), logging.INFO)\n-    create_experiment_workspace(workspace)\n-    change_working_directory(workspace)\n-\n-    total_timer = Timer()\n-    preprocessing_timer = Timer()\n-    asp_timer = Timer(stopped=True)\n-    verification_timer = Timer(stopped=True)\n-    iteration_data = IterationData()\n-\n-    # Generate data\n-    with change_dir(\"input\"):\n-        logging.info(colored(\"Constructing InstanceDatas...\", \"blue\", \"on_grey\"))\n-        domain_data, instance_datas, num_ss_states, num_gfa_states = compute_instance_datas(domain_filepath, instance_filepaths, disable_closed_Q, max_num_states_per_instance, max_time_per_instance, enable_dump_files)\n-        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-        if instance_datas is None:\n-            raise Exception(\"Failed to create InstanceDatas.\")\n-\n-        state_finder = StateFinder(domain_data, instance_datas)\n-\n-        logging.info(colored(\"Initializing TupleGraphs...\", \"blue\", \"on_grey\"))\n-        gfa_state_id_to_tuple_graph: Dict[int, mm.TupleGraph] = compute_tuple_graphs(domain_data, instance_datas, state_finder, width, enable_dump_files)\n-        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-    preprocessing_data = PreprocessingData(domain_data, instance_datas, state_finder, gfa_state_id_to_tuple_graph)\n-    preprocessing_timer.stop()\n-\n-    if not preprocessing_data.instance_datas:\n-        raise RuntimeError(\"Data is empty\")\n-\n-    sketches = set()\n-    total_features = set()\n-    sketch_features = set()\n-    # Learn sketch\n-    if encoding_type == EncodingType.EXPLICIT:\n-        create_experiment_workspace(workspace)\n-        preprocessing_timer.resume()\n-\n-        for instance_data in preprocessing_data.instance_datas:\n-            print(f\"Instance {instance_data.idx}:\")\n-            for state in instance_data.mimir_ss.get_states():\n-                    state_data = state.get_state()\n-                    fluents = state_data.get_fluent_atoms()\n-                    print(f\"State {state.get_index()} fluents: {fluents}\")\n-            # TODO: when is the best time to generate features?\n-            # Based on current instance currently looks most reasonable.\n-            # It could be restricted to the states in the tuple graph as well,\n-            # which reduces the number of rules, but not fully sure yet about the consequences in the bigger picture.\n-            iteration_data = IterationData()\n-            iteration_data.instance_datas = [instance_data]\n-            iteration_data.gfa_states = instance_data.gfa.get_states()\n-\n-            # Report some progress\n-            write_file(f\"{instance_data.idx}.dot\", instance_data.dlplan_ss.to_dot(1))\n-            print(\"     \", end=\"\")\n-            print(\"id:\", instance_data.idx,\n-                    \"problem_filepath:\", instance_data.mimir_ss.get_problem().get_filepath(),\n-                    \"num_states:\", instance_data.mimir_ss.get_num_states(),\n-                    \"num_state_equivalences:\", instance_data.gfa.get_num_states())\n-\n-            logging.info(colored(\"Initializing DomainFeatureData...\", \"blue\", \"on_grey\"))\n-            iteration_data.feature_pool = compute_feature_pool(\n-                preprocessing_data,\n-                iteration_data,\n-                gfa_state_id_to_tuple_graph,\n-                state_finder,\n-                disable_feature_generation,\n-                enable_incomplete_feature_pruning,\n-                concept_complexity_limit,\n-                role_complexity_limit,\n-                boolean_complexity_limit,\n-                count_numerical_complexity_limit,\n-                distance_numerical_complexity_limit,\n-                feature_limit,\n-                additional_booleans,\n-                additional_numericals\n-            )\n-            # Add features to features that were generated\n-            total_features.update(feature.dlplan_feature for feature in iteration_data.feature_pool)\n-\n-            logging.info(colored(\"Constructing PerStateFeatureValuations...\", \"blue\", \"on_grey\"))\n-            iteration_data.gfa_state_global_idx_to_feature_evaluations = compute_per_state_feature_valuations(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Constructing StatePairEquivalenceDatas...\", \"blue\", \"on_grey\"))\n-            iteration_data.state_pair_equivalences, iteration_data.gfa_state_global_idx_to_state_pair_equivalence = compute_state_pair_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Constructing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n-            iteration_data.gfa_state_global_idx_to_tuple_graph_equivalence = compute_tuple_graph_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Minimizing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n-            minimize_tuple_graph_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-            preprocessing_timer.stop()\n-            asp_timer.resume()\n-\n-            # We count the number of subgoal tuples for which no rule could be found.\n-            # This usually happens if the pool of features is not sufficiently rich.\n-            count_unsat_tuples = 0\n-\n-            for gfa_state in instance_data.gfa.get_states():\n-                gfa_state_global_idx = gfa_state.get_global_index()\n-                gfa_state_idx = gfa_state.get_index()\n-\n-                if instance_data.gfa.is_deadend_state(gfa_state_idx):\n-                    continue\n-\n-                #print(f\"Keys in gfa_state_global_idx_to_tuple_graph: {list(preprocessing_data.gfa_state_global_idx_to_tuple_graph.keys())}\")\n-                #print(f\"gfa_state_global_idx: {gfa_state_global_idx}\")\n-\n-                tuple_graph = preprocessing_data.gfa_state_global_idx_to_tuple_graph[gfa_state_global_idx]\n-\n-                for distance, group in enumerate(tuple_graph.get_vertices_grouped_by_distance()):\n-                    if distance == 0:\n-                        # We skip subgoal tuples at distance zero because they do not encode progress towards a goal.\n-                        continue\n-                    for vertex in group:\n-                        # Here we find all simplest single sketch rules for a pair (state, subgoal tuple).\n-                        t_idx = vertex.get_index()\n-\n-                        start_time = time.time()\n-                        asp_factory = ASPFactory(encoding_type, enable_goal_separating_features, max_num_rules)\n-                        facts = asp_factory.make_facts(preprocessing_data, iteration_data, gfa_state, t_idx)\n-\n-                        print(f\"Factory time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        start_time = time.time()\n-                        asp_factory.ground(facts)\n-                        print(f\"Grounding time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        start_time = time.time()\n-                        symbolss, returncode = asp_factory.solve_all_opt()\n-                        print(f\"Solving time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        if returncode in [ClingoExitCode.UNSATISFIABLE, ClingoExitCode.EXHAUSTED]:\n-                            print(colored(\"ASP is unsatisfiable or exhausted!\", \"red\", \"on_grey\"))\n-                            print(colored(f\"No sketch of width {width} exists that solves all instances!\", \"red\", \"on_grey\"))\n-                            # There might be tuples where we get unsat.\n-                            # If such cases occur, which is very likely in complex domains, then we must look closer into this.\n-                            count_unsat_tuples += 1\n-                            continue\n-                        elif returncode == ClingoExitCode.UNKNOWN:\n-                            print(colored(\"ASP solving throws unknown error!\", \"red\", \"on_grey\"))\n-                            exit(ExitCode.UNKNOWN)\n-                        elif returncode == ClingoExitCode.INTERRUPTED:\n-                            print(colored(\"ASP solving interrupted!\", \"red\", \"on_grey\"))\n-                            exit(ExitCode.INTERRUPTED)\n-                        else:\n-                            logging.info(f\"Solution found with return code: {returncode}\")\n-\n-                        for symbols in symbolss:\n-                            dlplan_policy = ExplicitDlplanPolicyFactory().make_dlplan_policy_from_answer_set(symbols, preprocessing_data, iteration_data)\n-\n-                            sketch = Sketch(dlplan_policy, width)\n-                            sketches.add(sketch)\n-                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_booleans())\n-                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_numericals())\n-                            print(dlplan_policy)\n-\n-    else:\n-        raise Exception(\"No implementation for the given encoding type.\")\n-\n-    preprocessing_timer.stop()\n-    asp_timer.stop()\n-    verification_timer.stop()\n-    total_timer.stop()\n-\n-    # Compute feature histograms by complexity\n-    total_features_by_complexity = defaultdict(int)\n-    for feature in total_features:\n-        total_features_by_complexity[feature.compute_complexity()] += 1\n-    total_features_by_complexity = {key: total_features_by_complexity[key] for key in sorted(total_features_by_complexity)}\n-\n-    sketch_features_by_complexity = defaultdict(int)\n-    for feature in sketch_features:\n-        sketch_features_by_complexity[feature.compute_complexity()] += 1\n-    sketch_features_by_complexity = {key: sketch_features_by_complexity[key] for key in sorted(sketch_features_by_complexity)}\n-\n-    # Output the result\n-    with change_dir(\"output\"):\n-        print_separation_line()\n-        logging.info(colored(\"Summary:\", \"green\", \"on_grey\"))\n-\n-        print(f\"Preprocessing time: {int(preprocessing_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"ASP time: {int(asp_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Verification time: {int(verification_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Total time: {int(total_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Total memory: {int(memory_usage() / 1024)} GiB.\")\n-        print(f\"Total number of states: {num_ss_states}\")\n-        print(f\"Total number of abstract states: {num_gfa_states}\")\n-        print(f\"Number of unsat tuples: {count_unsat_tuples}\")\n-        print(f\"Number of sketch rules: {len(sketches)}\")\n-        print(f\"Number of total features: {len(total_features)}\")\n-        print(f\"Number of sketch features: {len(sketch_features)}\")\n-        print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n-        print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n"
                },
                {
                    "date": 1727820741004,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,247 @@\n+import logging\n+import time\n+from collections import defaultdict\n+from pathlib import Path\n+from termcolor import colored\n+from typing import List, MutableSet, Dict\n+import clingo\n+import pymimir as mm\n+from dlplan.policy import PolicyMinimizer, NamedBoolean, NamedNumerical\n+from collections import defaultdict\n+from .src.exit_codes import ExitCode\n+from .src.iteration import EncodingType, ASPFactory, ClingoExitCode, IterationData, LearningStatistics, Sketch, D2sepDlplanPolicyFactory, ExplicitDlplanPolicyFactory, compute_feature_pool, compute_per_state_feature_valuations, compute_state_pair_equivalences, compute_tuple_graph_equivalences, minimize_tuple_graph_equivalences\n+from .src.util import Timer, create_experiment_workspace, change_working_directory, write_file, change_dir, memory_usage, add_console_handler, print_separation_line\n+from .src.preprocessing import InstanceData, PreprocessingData, StateFinder, compute_instance_datas, compute_tuple_graphs\n+\n+def learn_sketch_for_problem_class(\n+    domain_filepath: Path,\n+    problems_directory: Path,\n+    workspace: Path,\n+    width: int,\n+    disable_closed_Q: bool = True,\n+    max_num_states_per_instance: int = 10000,\n+    max_time_per_instance: int = 10,\n+    encoding_type: EncodingType = EncodingType.EXPLICIT,\n+    max_num_rules: int = 1,\n+    enable_goal_separating_features: bool = True,\n+    disable_feature_generation: bool = True,\n+    enable_incomplete_feature_pruning: bool = False,\n+    concept_complexity_limit: int = 9,\n+    role_complexity_limit: int = 9,\n+    boolean_complexity_limit: int = 9,\n+    count_numerical_complexity_limit: int = 9,\n+    distance_numerical_complexity_limit: int = 9,\n+    feature_limit: int = 1000000,\n+    additional_booleans: List[str] = None,\n+    additional_numericals: List[str] = None,\n+    enable_dump_files: bool = False,\n+):\n+    # Initialize variables and setup\n+    if additional_booleans is None:\n+        additional_booleans = []\n+    if additional_numericals is None:\n+        additional_numericals = []\n+    instance_filepaths = list(problems_directory.iterdir())\n+   \n+\n+    add_console_handler(logging.getLogger(), logging.INFO)\n+    create_experiment_workspace(workspace)\n+    change_working_directory(workspace)\n+\n+    total_timer = Timer()\n+    preprocessing_timer = Timer()\n+    asp_timer = Timer(stopped=True)\n+    verification_timer = Timer(stopped=True)\n+    iteration_data = IterationData()\n+\n+    # Generate data\n+    with change_dir(\"input\"):\n+        logging.info(colored(\"Constructing InstanceDatas...\", \"blue\", \"on_grey\"))\n+        domain_data, instance_datas, num_ss_states, num_gfa_states = compute_instance_datas(domain_filepath, instance_filepaths, disable_closed_Q, max_num_states_per_instance, max_time_per_instance, enable_dump_files)\n+        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+        if instance_datas is None:\n+            raise Exception(\"Failed to create InstanceDatas.\")\n+\n+        state_finder = StateFinder(domain_data, instance_datas)\n+\n+        logging.info(colored(\"Initializing TupleGraphs...\", \"blue\", \"on_grey\"))\n+        gfa_state_id_to_tuple_graph: Dict[int, mm.TupleGraph] = compute_tuple_graphs(domain_data, instance_datas, state_finder, width, enable_dump_files)\n+        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+    preprocessing_data = PreprocessingData(domain_data, instance_datas, state_finder, gfa_state_id_to_tuple_graph)\n+    preprocessing_timer.stop()\n+\n+    if not preprocessing_data.instance_datas:\n+        raise RuntimeError(\"Data is empty\")\n+\n+    sketches = set()\n+    total_features = set()\n+    sketch_features = set()\n+    # Learn sketch\n+    if encoding_type == EncodingType.EXPLICIT:\n+        create_experiment_workspace(workspace)\n+        preprocessing_timer.resume()\n+\n+        for instance_data in preprocessing_data.instance_datas:\n+            print(f\"Instance {instance_data.idx}:\")\n+            for state in instance_data.mimir_ss.get_states():\n+                    state_data = state.get_state()\n+                    fluents = state_data.get_fluent_atoms()\n+                    #print(f\"State {state.get_index()} fluents: {fluents}\")\n+            # TODO: when is the best time to generate features?\n+            # Based on current instance currently looks most reasonable.\n+            # It could be restricted to the states in the tuple graph as well,\n+            # which reduces the number of rules, but not fully sure yet about the consequences in the bigger picture.\n+            iteration_data = IterationData()\n+            iteration_data.instance_datas = [instance_data]\n+            iteration_data.gfa_states = instance_data.gfa.get_states()\n+\n+            # Report some progress\n+            write_file(f\"{instance_data.idx}.dot\", instance_data.dlplan_ss.to_dot(1))\n+            print(\"     \", end=\"\")\n+            print(\"id:\", instance_data.idx,\n+                    \"problem_filepath:\", instance_data.mimir_ss.get_problem().get_filepath(),\n+                    \"num_states:\", instance_data.mimir_ss.get_num_states(),\n+                    \"num_state_equivalences:\", instance_data.gfa.get_num_states())\n+\n+            logging.info(colored(\"Initializing DomainFeatureData...\", \"blue\", \"on_grey\"))\n+            iteration_data.feature_pool = compute_feature_pool(\n+                preprocessing_data,\n+                iteration_data,\n+                gfa_state_id_to_tuple_graph,\n+                state_finder,\n+                disable_feature_generation,\n+                enable_incomplete_feature_pruning,\n+                concept_complexity_limit,\n+                role_complexity_limit,\n+                boolean_complexity_limit,\n+                count_numerical_complexity_limit,\n+                distance_numerical_complexity_limit,\n+                feature_limit,\n+                additional_booleans,\n+                additional_numericals\n+            )\n+            # Add features to features that were generated\n+            total_features.update(feature.dlplan_feature for feature in iteration_data.feature_pool)\n+\n+            logging.info(colored(\"Constructing PerStateFeatureValuations...\", \"blue\", \"on_grey\"))\n+            iteration_data.gfa_state_global_idx_to_feature_evaluations = compute_per_state_feature_valuations(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Constructing StatePairEquivalenceDatas...\", \"blue\", \"on_grey\"))\n+            iteration_data.state_pair_equivalences, iteration_data.gfa_state_global_idx_to_state_pair_equivalence = compute_state_pair_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Constructing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n+            iteration_data.gfa_state_global_idx_to_tuple_graph_equivalence = compute_tuple_graph_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Minimizing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n+            minimize_tuple_graph_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+            preprocessing_timer.stop()\n+            asp_timer.resume()\n+\n+            # We count the number of subgoal tuples for which no rule could be found.\n+            # This usually happens if the pool of features is not sufficiently rich.\n+            count_unsat_tuples = 0\n+            total_transitions = 0 \n+\n+            for gfa_state in instance_data.gfa.get_states():\n+                gfa_state_global_idx = gfa_state.get_global_index()\n+                gfa_state_idx = gfa_state.get_index()\n+\n+                if instance_data.gfa.is_deadend_state(gfa_state_idx):\n+                    continue\n+\n+                #print(f\"Keys in gfa_state_global_idx_to_tuple_graph: {list(preprocessing_data.gfa_state_global_idx_to_tuple_graph.keys())}\")\n+                #print(f\"gfa_state_global_idx: {gfa_state_global_idx}\")\n+\n+                tuple_graph = preprocessing_data.gfa_state_global_idx_to_tuple_graph[gfa_state_global_idx]\n+\n+                for distance, group in enumerate(tuple_graph.get_vertices_grouped_by_distance()):\n+                    if distance == 0:\n+                        # We skip subgoal tuples at distance zero because they do not encode progress towards a goal.\n+                        continue\n+                    for vertex in group:\n+                        # Here we find all simplest single sketch rules for a pair (state, subgoal tuple).\n+                        t_idx = vertex.get_index()\n+                        print(f\"State Transition: s_i = {gfa_state_idx}, s_j = {t_idx}\")\n+                        total_transitions += 1\n+                        start_time = time.time()\n+                        asp_factory = ASPFactory(encoding_type, enable_goal_separating_features, max_num_rules)\n+                        facts = asp_factory.make_facts(preprocessing_data, iteration_data, gfa_state, t_idx)\n+\n+                        print(f\"Factory time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        start_time = time.time()\n+                        asp_factory.ground(facts)\n+                        print(f\"Grounding time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        start_time = time.time()\n+                        symbolss, returncode = asp_factory.solve_all_opt()\n+                        print(f\"Solving time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        if returncode in [ClingoExitCode.UNSATISFIABLE, ClingoExitCode.EXHAUSTED]:\n+                            print(colored(\"ASP is unsatisfiable or exhausted!\", \"red\", \"on_grey\"))\n+                            print(colored(f\"No sketch of width {width} exists that solves all instances!\", \"red\", \"on_grey\"))\n+                            # There might be tuples where we get unsat.\n+                            # If such cases occur, which is very likely in complex domains, then we must look closer into this.\n+                            count_unsat_tuples += 1\n+                            continue\n+                        elif returncode == ClingoExitCode.UNKNOWN:\n+                            print(colored(\"ASP solving throws unknown error!\", \"red\", \"on_grey\"))\n+                            exit(ExitCode.UNKNOWN)\n+                        elif returncode == ClingoExitCode.INTERRUPTED:\n+                            print(colored(\"ASP solving interrupted!\", \"red\", \"on_grey\"))\n+                            exit(ExitCode.INTERRUPTED)\n+                        else:\n+                            logging.info(f\"Solution found with return code: {returncode}\")\n+\n+                        for symbols in symbolss:\n+                            dlplan_policy = ExplicitDlplanPolicyFactory().make_dlplan_policy_from_answer_set(symbols, preprocessing_data, iteration_data)\n+\n+                            sketch = Sketch(dlplan_policy, width)\n+                            sketches.add(sketch)\n+                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_booleans())\n+                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_numericals())\n+                            print(dlplan_policy)\n+\n+    else:\n+        raise Exception(\"No implementation for the given encoding type.\")\n+\n+    preprocessing_timer.stop()\n+    asp_timer.stop()\n+    verification_timer.stop()\n+    total_timer.stop()\n+\n+    # Compute feature histograms by complexity\n+    total_features_by_complexity = defaultdict(int)\n+    for feature in total_features:\n+        total_features_by_complexity[feature.compute_complexity()] += 1\n+    total_features_by_complexity = {key: total_features_by_complexity[key] for key in sorted(total_features_by_complexity)}\n+\n+    sketch_features_by_complexity = defaultdict(int)\n+    for feature in sketch_features:\n+        sketch_features_by_complexity[feature.compute_complexity()] += 1\n+    sketch_features_by_complexity = {key: sketch_features_by_complexity[key] for key in sorted(sketch_features_by_complexity)}\n+\n+    # Output the result\n+    with change_dir(\"output\"):\n+        print_separation_line()\n+        logging.info(colored(\"Summary:\", \"green\", \"on_grey\"))\n+\n+        print(f\"Preprocessing time: {int(preprocessing_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"ASP time: {int(asp_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Verification time: {int(verification_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Total time: {int(total_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Total memory: {int(memory_usage() / 1024)} GiB.\")\n+        print(f\"Total number of transitions: {total_transitions}\")\n+        print(f\"Total number of states: {num_ss_states}\")\n+        print(f\"Total number of abstract states: {num_gfa_states}\")\n+        print(f\"Number of unsat tuples: {count_unsat_tuples}\")\n+        print(f\"Number of sketch rules: {len(sketches)}\")\n+        print(f\"Number of total features: {len(total_features)}\")\n+        print(f\"Number of sketch features: {len(sketch_features)}\")\n+        print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n+        print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n"
                },
                {
                    "date": 1727820749579,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -244,251 +244,4 @@\n         print(f\"Number of total features: {len(total_features)}\")\n         print(f\"Number of sketch features: {len(sketch_features)}\")\n         print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n         print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n-import logging\n-import time\n-from collections import defaultdict\n-from pathlib import Path\n-from termcolor import colored\n-from typing import List, MutableSet, Dict\n-import clingo\n-import pymimir as mm\n-from dlplan.policy import PolicyMinimizer, NamedBoolean, NamedNumerical\n-from collections import defaultdict\n-from .src.exit_codes import ExitCode\n-from .src.iteration import EncodingType, ASPFactory, ClingoExitCode, IterationData, LearningStatistics, Sketch, D2sepDlplanPolicyFactory, ExplicitDlplanPolicyFactory, compute_feature_pool, compute_per_state_feature_valuations, compute_state_pair_equivalences, compute_tuple_graph_equivalences, minimize_tuple_graph_equivalences\n-from .src.util import Timer, create_experiment_workspace, change_working_directory, write_file, change_dir, memory_usage, add_console_handler, print_separation_line\n-from .src.preprocessing import InstanceData, PreprocessingData, StateFinder, compute_instance_datas, compute_tuple_graphs\n-\n-def learn_sketch_for_problem_class(\n-    domain_filepath: Path,\n-    problems_directory: Path,\n-    workspace: Path,\n-    width: int,\n-    disable_closed_Q: bool = True,\n-    max_num_states_per_instance: int = 10000,\n-    max_time_per_instance: int = 10,\n-    encoding_type: EncodingType = EncodingType.EXPLICIT,\n-    max_num_rules: int = 1,\n-    enable_goal_separating_features: bool = True,\n-    disable_feature_generation: bool = True,\n-    enable_incomplete_feature_pruning: bool = False,\n-    concept_complexity_limit: int = 9,\n-    role_complexity_limit: int = 9,\n-    boolean_complexity_limit: int = 9,\n-    count_numerical_complexity_limit: int = 9,\n-    distance_numerical_complexity_limit: int = 9,\n-    feature_limit: int = 1000000,\n-    additional_booleans: List[str] = None,\n-    additional_numericals: List[str] = None,\n-    enable_dump_files: bool = False,\n-):\n-    # Initialize variables and setup\n-    if additional_booleans is None:\n-        additional_booleans = []\n-    if additional_numericals is None:\n-        additional_numericals = []\n-    instance_filepaths = list(problems_directory.iterdir())\n-   \n-\n-    add_console_handler(logging.getLogger(), logging.INFO)\n-    create_experiment_workspace(workspace)\n-    change_working_directory(workspace)\n-\n-    total_timer = Timer()\n-    preprocessing_timer = Timer()\n-    asp_timer = Timer(stopped=True)\n-    verification_timer = Timer(stopped=True)\n-    iteration_data = IterationData()\n-\n-    # Generate data\n-    with change_dir(\"input\"):\n-        logging.info(colored(\"Constructing InstanceDatas...\", \"blue\", \"on_grey\"))\n-        domain_data, instance_datas, num_ss_states, num_gfa_states = compute_instance_datas(domain_filepath, instance_filepaths, disable_closed_Q, max_num_states_per_instance, max_time_per_instance, enable_dump_files)\n-        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-        if instance_datas is None:\n-            raise Exception(\"Failed to create InstanceDatas.\")\n-\n-        state_finder = StateFinder(domain_data, instance_datas)\n-\n-        logging.info(colored(\"Initializing TupleGraphs...\", \"blue\", \"on_grey\"))\n-        gfa_state_id_to_tuple_graph: Dict[int, mm.TupleGraph] = compute_tuple_graphs(domain_data, instance_datas, state_finder, width, enable_dump_files)\n-        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-    preprocessing_data = PreprocessingData(domain_data, instance_datas, state_finder, gfa_state_id_to_tuple_graph)\n-    preprocessing_timer.stop()\n-\n-    if not preprocessing_data.instance_datas:\n-        raise RuntimeError(\"Data is empty\")\n-\n-    sketches = set()\n-    total_features = set()\n-    sketch_features = set()\n-    # Learn sketch\n-    if encoding_type == EncodingType.EXPLICIT:\n-        create_experiment_workspace(workspace)\n-        preprocessing_timer.resume()\n-\n-        for instance_data in preprocessing_data.instance_datas:\n-            print(f\"Instance {instance_data.idx}:\")\n-            for state in instance_data.mimir_ss.get_states():\n-                    state_data = state.get_state()\n-                    fluents = state_data.get_fluent_atoms()\n-                    #print(f\"State {state.get_index()} fluents: {fluents}\")\n-            # TODO: when is the best time to generate features?\n-            # Based on current instance currently looks most reasonable.\n-            # It could be restricted to the states in the tuple graph as well,\n-            # which reduces the number of rules, but not fully sure yet about the consequences in the bigger picture.\n-            iteration_data = IterationData()\n-            iteration_data.instance_datas = [instance_data]\n-            iteration_data.gfa_states = instance_data.gfa.get_states()\n-\n-            # Report some progress\n-            write_file(f\"{instance_data.idx}.dot\", instance_data.dlplan_ss.to_dot(1))\n-            print(\"     \", end=\"\")\n-            print(\"id:\", instance_data.idx,\n-                    \"problem_filepath:\", instance_data.mimir_ss.get_problem().get_filepath(),\n-                    \"num_states:\", instance_data.mimir_ss.get_num_states(),\n-                    \"num_state_equivalences:\", instance_data.gfa.get_num_states())\n-\n-            logging.info(colored(\"Initializing DomainFeatureData...\", \"blue\", \"on_grey\"))\n-            iteration_data.feature_pool = compute_feature_pool(\n-                preprocessing_data,\n-                iteration_data,\n-                gfa_state_id_to_tuple_graph,\n-                state_finder,\n-                disable_feature_generation,\n-                enable_incomplete_feature_pruning,\n-                concept_complexity_limit,\n-                role_complexity_limit,\n-                boolean_complexity_limit,\n-                count_numerical_complexity_limit,\n-                distance_numerical_complexity_limit,\n-                feature_limit,\n-                additional_booleans,\n-                additional_numericals\n-            )\n-            # Add features to features that were generated\n-            total_features.update(feature.dlplan_feature for feature in iteration_data.feature_pool)\n-\n-            logging.info(colored(\"Constructing PerStateFeatureValuations...\", \"blue\", \"on_grey\"))\n-            iteration_data.gfa_state_global_idx_to_feature_evaluations = compute_per_state_feature_valuations(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Constructing StatePairEquivalenceDatas...\", \"blue\", \"on_grey\"))\n-            iteration_data.state_pair_equivalences, iteration_data.gfa_state_global_idx_to_state_pair_equivalence = compute_state_pair_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Constructing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n-            iteration_data.gfa_state_global_idx_to_tuple_graph_equivalence = compute_tuple_graph_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Minimizing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n-            minimize_tuple_graph_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-            preprocessing_timer.stop()\n-            asp_timer.resume()\n-\n-            # We count the number of subgoal tuples for which no rule could be found.\n-            # This usually happens if the pool of features is not sufficiently rich.\n-            count_unsat_tuples = 0\n-            total_transitions = 0 \n-\n-            for gfa_state in instance_data.gfa.get_states():\n-                gfa_state_global_idx = gfa_state.get_global_index()\n-                gfa_state_idx = gfa_state.get_index()\n-\n-                if instance_data.gfa.is_deadend_state(gfa_state_idx):\n-                    continue\n-\n-                #print(f\"Keys in gfa_state_global_idx_to_tuple_graph: {list(preprocessing_data.gfa_state_global_idx_to_tuple_graph.keys())}\")\n-                #print(f\"gfa_state_global_idx: {gfa_state_global_idx}\")\n-\n-                tuple_graph = preprocessing_data.gfa_state_global_idx_to_tuple_graph[gfa_state_global_idx]\n-\n-                for distance, group in enumerate(tuple_graph.get_vertices_grouped_by_distance()):\n-                    if distance == 0:\n-                        # We skip subgoal tuples at distance zero because they do not encode progress towards a goal.\n-                        continue\n-                    for vertex in group:\n-                        # Here we find all simplest single sketch rules for a pair (state, subgoal tuple).\n-                        t_idx = vertex.get_index()\n-                        print(f\"State Transition: s_i = {gfa_state_global_idx}, s_j = {t_idx}\")\n-                        total_transitions += 1\n-                        start_time = time.time()\n-                        asp_factory = ASPFactory(encoding_type, enable_goal_separating_features, max_num_rules)\n-                        facts = asp_factory.make_facts(preprocessing_data, iteration_data, gfa_state, t_idx)\n-\n-                        print(f\"Factory time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        start_time = time.time()\n-                        asp_factory.ground(facts)\n-                        print(f\"Grounding time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        start_time = time.time()\n-                        symbolss, returncode = asp_factory.solve_all_opt()\n-                        print(f\"Solving time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        if returncode in [ClingoExitCode.UNSATISFIABLE, ClingoExitCode.EXHAUSTED]:\n-                            print(colored(\"ASP is unsatisfiable or exhausted!\", \"red\", \"on_grey\"))\n-                            print(colored(f\"No sketch of width {width} exists that solves all instances!\", \"red\", \"on_grey\"))\n-                            # There might be tuples where we get unsat.\n-                            # If such cases occur, which is very likely in complex domains, then we must look closer into this.\n-                            count_unsat_tuples += 1\n-                            continue\n-                        elif returncode == ClingoExitCode.UNKNOWN:\n-                            print(colored(\"ASP solving throws unknown error!\", \"red\", \"on_grey\"))\n-                            exit(ExitCode.UNKNOWN)\n-                        elif returncode == ClingoExitCode.INTERRUPTED:\n-                            print(colored(\"ASP solving interrupted!\", \"red\", \"on_grey\"))\n-                            exit(ExitCode.INTERRUPTED)\n-                        else:\n-                            logging.info(f\"Solution found with return code: {returncode}\")\n-\n-                        for symbols in symbolss:\n-                            dlplan_policy = ExplicitDlplanPolicyFactory().make_dlplan_policy_from_answer_set(symbols, preprocessing_data, iteration_data)\n-\n-                            sketch = Sketch(dlplan_policy, width)\n-                            sketches.add(sketch)\n-                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_booleans())\n-                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_numericals())\n-                            print(dlplan_policy)\n-\n-    else:\n-        raise Exception(\"No implementation for the given encoding type.\")\n-\n-    preprocessing_timer.stop()\n-    asp_timer.stop()\n-    verification_timer.stop()\n-    total_timer.stop()\n-\n-    # Compute feature histograms by complexity\n-    total_features_by_complexity = defaultdict(int)\n-    for feature in total_features:\n-        total_features_by_complexity[feature.compute_complexity()] += 1\n-    total_features_by_complexity = {key: total_features_by_complexity[key] for key in sorted(total_features_by_complexity)}\n-\n-    sketch_features_by_complexity = defaultdict(int)\n-    for feature in sketch_features:\n-        sketch_features_by_complexity[feature.compute_complexity()] += 1\n-    sketch_features_by_complexity = {key: sketch_features_by_complexity[key] for key in sorted(sketch_features_by_complexity)}\n-\n-    # Output the result\n-    with change_dir(\"output\"):\n-        print_separation_line()\n-        logging.info(colored(\"Summary:\", \"green\", \"on_grey\"))\n-\n-        print(f\"Preprocessing time: {int(preprocessing_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"ASP time: {int(asp_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Verification time: {int(verification_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Total time: {int(total_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Total memory: {int(memory_usage() / 1024)} GiB.\")\n-        print(f\"Total number of transitions: {total_transitions}\")\n-        print(f\"Total number of states: {num_ss_states}\")\n-        print(f\"Total number of abstract states: {num_gfa_states}\")\n-        print(f\"Number of unsat tuples: {count_unsat_tuples}\")\n-        print(f\"Number of sketch rules: {len(sketches)}\")\n-        print(f\"Number of total features: {len(total_features)}\")\n-        print(f\"Number of sketch features: {len(sketch_features)}\")\n-        print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n-        print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n"
                },
                {
                    "date": 1727820781984,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,247 @@\n+import logging\n+import time\n+from collections import defaultdict\n+from pathlib import Path\n+from termcolor import colored\n+from typing import List, MutableSet, Dict\n+import clingo\n+import pymimir as mm\n+from dlplan.policy import PolicyMinimizer, NamedBoolean, NamedNumerical\n+from collections import defaultdict\n+from .src.exit_codes import ExitCode\n+from .src.iteration import EncodingType, ASPFactory, ClingoExitCode, IterationData, LearningStatistics, Sketch, D2sepDlplanPolicyFactory, ExplicitDlplanPolicyFactory, compute_feature_pool, compute_per_state_feature_valuations, compute_state_pair_equivalences, compute_tuple_graph_equivalences, minimize_tuple_graph_equivalences\n+from .src.util import Timer, create_experiment_workspace, change_working_directory, write_file, change_dir, memory_usage, add_console_handler, print_separation_line\n+from .src.preprocessing import InstanceData, PreprocessingData, StateFinder, compute_instance_datas, compute_tuple_graphs\n+\n+def learn_sketch_for_problem_class(\n+    domain_filepath: Path,\n+    problems_directory: Path,\n+    workspace: Path,\n+    width: int,\n+    disable_closed_Q: bool = True,\n+    max_num_states_per_instance: int = 10000,\n+    max_time_per_instance: int = 10,\n+    encoding_type: EncodingType = EncodingType.EXPLICIT,\n+    max_num_rules: int = 1,\n+    enable_goal_separating_features: bool = True,\n+    disable_feature_generation: bool = True,\n+    enable_incomplete_feature_pruning: bool = False,\n+    concept_complexity_limit: int = 9,\n+    role_complexity_limit: int = 9,\n+    boolean_complexity_limit: int = 9,\n+    count_numerical_complexity_limit: int = 9,\n+    distance_numerical_complexity_limit: int = 9,\n+    feature_limit: int = 1000000,\n+    additional_booleans: List[str] = None,\n+    additional_numericals: List[str] = None,\n+    enable_dump_files: bool = False,\n+):\n+    # Initialize variables and setup\n+    if additional_booleans is None:\n+        additional_booleans = []\n+    if additional_numericals is None:\n+        additional_numericals = []\n+    instance_filepaths = list(problems_directory.iterdir())\n+   \n+\n+    add_console_handler(logging.getLogger(), logging.INFO)\n+    create_experiment_workspace(workspace)\n+    change_working_directory(workspace)\n+\n+    total_timer = Timer()\n+    preprocessing_timer = Timer()\n+    asp_timer = Timer(stopped=True)\n+    verification_timer = Timer(stopped=True)\n+    iteration_data = IterationData()\n+\n+    # Generate data\n+    with change_dir(\"input\"):\n+        logging.info(colored(\"Constructing InstanceDatas...\", \"blue\", \"on_grey\"))\n+        domain_data, instance_datas, num_ss_states, num_gfa_states = compute_instance_datas(domain_filepath, instance_filepaths, disable_closed_Q, max_num_states_per_instance, max_time_per_instance, enable_dump_files)\n+        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+        if instance_datas is None:\n+            raise Exception(\"Failed to create InstanceDatas.\")\n+\n+        state_finder = StateFinder(domain_data, instance_datas)\n+\n+        logging.info(colored(\"Initializing TupleGraphs...\", \"blue\", \"on_grey\"))\n+        gfa_state_id_to_tuple_graph: Dict[int, mm.TupleGraph] = compute_tuple_graphs(domain_data, instance_datas, state_finder, width, enable_dump_files)\n+        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+    preprocessing_data = PreprocessingData(domain_data, instance_datas, state_finder, gfa_state_id_to_tuple_graph)\n+    preprocessing_timer.stop()\n+\n+    if not preprocessing_data.instance_datas:\n+        raise RuntimeError(\"Data is empty\")\n+\n+    sketches = set()\n+    total_features = set()\n+    sketch_features = set()\n+    # Learn sketch\n+    if encoding_type == EncodingType.EXPLICIT:\n+        create_experiment_workspace(workspace)\n+        preprocessing_timer.resume()\n+\n+        for instance_data in preprocessing_data.instance_datas:\n+            print(f\"Instance {instance_data.idx}:\")\n+            for state in instance_data.mimir_ss.get_states():\n+                    state_data = state.get_state()\n+                    fluents = state_data.get_fluent_atoms()\n+                    #print(f\"State {state.get_index()} fluents: {fluents}\")\n+            # TODO: when is the best time to generate features?\n+            # Based on current instance currently looks most reasonable.\n+            # It could be restricted to the states in the tuple graph as well,\n+            # which reduces the number of rules, but not fully sure yet about the consequences in the bigger picture.\n+            iteration_data = IterationData()\n+            iteration_data.instance_datas = [instance_data]\n+            iteration_data.gfa_states = instance_data.gfa.get_states()\n+\n+            # Report some progress\n+            write_file(f\"{instance_data.idx}.dot\", instance_data.dlplan_ss.to_dot(1))\n+            print(\"     \", end=\"\")\n+            print(\"id:\", instance_data.idx,\n+                    \"problem_filepath:\", instance_data.mimir_ss.get_problem().get_filepath(),\n+                    \"num_states:\", instance_data.mimir_ss.get_num_states(),\n+                    \"num_state_equivalences:\", instance_data.gfa.get_num_states())\n+\n+            logging.info(colored(\"Initializing DomainFeatureData...\", \"blue\", \"on_grey\"))\n+            iteration_data.feature_pool = compute_feature_pool(\n+                preprocessing_data,\n+                iteration_data,\n+                gfa_state_id_to_tuple_graph,\n+                state_finder,\n+                disable_feature_generation,\n+                enable_incomplete_feature_pruning,\n+                concept_complexity_limit,\n+                role_complexity_limit,\n+                boolean_complexity_limit,\n+                count_numerical_complexity_limit,\n+                distance_numerical_complexity_limit,\n+                feature_limit,\n+                additional_booleans,\n+                additional_numericals\n+            )\n+            # Add features to features that were generated\n+            total_features.update(feature.dlplan_feature for feature in iteration_data.feature_pool)\n+\n+            logging.info(colored(\"Constructing PerStateFeatureValuations...\", \"blue\", \"on_grey\"))\n+            iteration_data.gfa_state_global_idx_to_feature_evaluations = compute_per_state_feature_valuations(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Constructing StatePairEquivalenceDatas...\", \"blue\", \"on_grey\"))\n+            iteration_data.state_pair_equivalences, iteration_data.gfa_state_global_idx_to_state_pair_equivalence = compute_state_pair_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Constructing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n+            iteration_data.gfa_state_global_idx_to_tuple_graph_equivalence = compute_tuple_graph_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Minimizing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n+            minimize_tuple_graph_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+            preprocessing_timer.stop()\n+            asp_timer.resume()\n+\n+            # We count the number of subgoal tuples for which no rule could be found.\n+            # This usually happens if the pool of features is not sufficiently rich.\n+            count_unsat_tuples = 0\n+            total_transitions = 0 \n+\n+            for gfa_state in instance_data.gfa.get_states():\n+                gfa_state_global_idx = gfa_state.get_global_index()\n+                gfa_state_idx = gfa_state.get_index()\n+\n+                if instance_data.gfa.is_deadend_state(gfa_state_idx):\n+                    continue\n+\n+                #print(f\"Keys in gfa_state_global_idx_to_tuple_graph: {list(preprocessing_data.gfa_state_global_idx_to_tuple_graph.keys())}\")\n+                #print(f\"gfa_state_global_idx: {gfa_state_global_idx}\")\n+\n+                tuple_graph = preprocessing_data.gfa_state_global_idx_to_tuple_graph[gfa_state_global_idx]\n+\n+                for distance, group in enumerate(tuple_graph.get_vertices_grouped_by_distance()):\n+                    if distance == 0:\n+                        # We skip subgoal tuples at distance zero because they do not encode progress towards a goal.\n+                        continue\n+                    for vertex in group:\n+                        # Here we find all simplest single sketch rules for a pair (state, subgoal tuple).\n+                        t_idx = vertex.get_index()\n+                        #print(f\"State Transition: s_i = {gfa_state_idx}, s_j = {t_idx}\")\n+                        total_transitions += 1\n+                        start_time = time.time()\n+                        asp_factory = ASPFactory(encoding_type, enable_goal_separating_features, max_num_rules)\n+                        facts = asp_factory.make_facts(preprocessing_data, iteration_data, gfa_state, t_idx)\n+\n+                        print(f\"Factory time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        start_time = time.time()\n+                        asp_factory.ground(facts)\n+                        print(f\"Grounding time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        start_time = time.time()\n+                        symbolss, returncode = asp_factory.solve_all_opt()\n+                        print(f\"Solving time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        if returncode in [ClingoExitCode.UNSATISFIABLE, ClingoExitCode.EXHAUSTED]:\n+                            print(colored(\"ASP is unsatisfiable or exhausted!\", \"red\", \"on_grey\"))\n+                            print(colored(f\"No sketch of width {width} exists that solves all instances!\", \"red\", \"on_grey\"))\n+                            # There might be tuples where we get unsat.\n+                            # If such cases occur, which is very likely in complex domains, then we must look closer into this.\n+                            count_unsat_tuples += 1\n+                            continue\n+                        elif returncode == ClingoExitCode.UNKNOWN:\n+                            print(colored(\"ASP solving throws unknown error!\", \"red\", \"on_grey\"))\n+                            exit(ExitCode.UNKNOWN)\n+                        elif returncode == ClingoExitCode.INTERRUPTED:\n+                            print(colored(\"ASP solving interrupted!\", \"red\", \"on_grey\"))\n+                            exit(ExitCode.INTERRUPTED)\n+                        else:\n+                            logging.info(f\"Solution found with return code: {returncode}\")\n+\n+                        for symbols in symbolss:\n+                            dlplan_policy = ExplicitDlplanPolicyFactory().make_dlplan_policy_from_answer_set(symbols, preprocessing_data, iteration_data)\n+\n+                            sketch = Sketch(dlplan_policy, width)\n+                            sketches.add(sketch)\n+                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_booleans())\n+                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_numericals())\n+                            print(dlplan_policy)\n+\n+    else:\n+        raise Exception(\"No implementation for the given encoding type.\")\n+\n+    preprocessing_timer.stop()\n+    asp_timer.stop()\n+    verification_timer.stop()\n+    total_timer.stop()\n+\n+    # Compute feature histograms by complexity\n+    total_features_by_complexity = defaultdict(int)\n+    for feature in total_features:\n+        total_features_by_complexity[feature.compute_complexity()] += 1\n+    total_features_by_complexity = {key: total_features_by_complexity[key] for key in sorted(total_features_by_complexity)}\n+\n+    sketch_features_by_complexity = defaultdict(int)\n+    for feature in sketch_features:\n+        sketch_features_by_complexity[feature.compute_complexity()] += 1\n+    sketch_features_by_complexity = {key: sketch_features_by_complexity[key] for key in sorted(sketch_features_by_complexity)}\n+\n+    # Output the result\n+    with change_dir(\"output\"):\n+        print_separation_line()\n+        logging.info(colored(\"Summary:\", \"green\", \"on_grey\"))\n+\n+        print(f\"Preprocessing time: {int(preprocessing_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"ASP time: {int(asp_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Verification time: {int(verification_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Total time: {int(total_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Total memory: {int(memory_usage() / 1024)} GiB.\")\n+        print(f\"Total number of transitions: {total_transitions}\")\n+        print(f\"Total number of states: {num_ss_states}\")\n+        print(f\"Total number of abstract states: {num_gfa_states}\")\n+        print(f\"Number of unsat tuples: {count_unsat_tuples}\")\n+        print(f\"Number of sketch rules: {len(sketches)}\")\n+        print(f\"Number of total features: {len(total_features)}\")\n+        print(f\"Number of sketch features: {len(sketch_features)}\")\n+        print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n+        print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n"
                },
                {
                    "date": 1727820892368,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -244,251 +244,4 @@\n         print(f\"Number of total features: {len(total_features)}\")\n         print(f\"Number of sketch features: {len(sketch_features)}\")\n         print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n         print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n-import logging\n-import time\n-from collections import defaultdict\n-from pathlib import Path\n-from termcolor import colored\n-from typing import List, MutableSet, Dict\n-import clingo\n-import pymimir as mm\n-from dlplan.policy import PolicyMinimizer, NamedBoolean, NamedNumerical\n-from collections import defaultdict\n-from .src.exit_codes import ExitCode\n-from .src.iteration import EncodingType, ASPFactory, ClingoExitCode, IterationData, LearningStatistics, Sketch, D2sepDlplanPolicyFactory, ExplicitDlplanPolicyFactory, compute_feature_pool, compute_per_state_feature_valuations, compute_state_pair_equivalences, compute_tuple_graph_equivalences, minimize_tuple_graph_equivalences\n-from .src.util import Timer, create_experiment_workspace, change_working_directory, write_file, change_dir, memory_usage, add_console_handler, print_separation_line\n-from .src.preprocessing import InstanceData, PreprocessingData, StateFinder, compute_instance_datas, compute_tuple_graphs\n-\n-def learn_sketch_for_problem_class(\n-    domain_filepath: Path,\n-    problems_directory: Path,\n-    workspace: Path,\n-    width: int,\n-    disable_closed_Q: bool = True,\n-    max_num_states_per_instance: int = 10000,\n-    max_time_per_instance: int = 10,\n-    encoding_type: EncodingType = EncodingType.EXPLICIT,\n-    max_num_rules: int = 1,\n-    enable_goal_separating_features: bool = True,\n-    disable_feature_generation: bool = True,\n-    enable_incomplete_feature_pruning: bool = False,\n-    concept_complexity_limit: int = 9,\n-    role_complexity_limit: int = 9,\n-    boolean_complexity_limit: int = 9,\n-    count_numerical_complexity_limit: int = 9,\n-    distance_numerical_complexity_limit: int = 9,\n-    feature_limit: int = 1000000,\n-    additional_booleans: List[str] = None,\n-    additional_numericals: List[str] = None,\n-    enable_dump_files: bool = False,\n-):\n-    # Initialize variables and setup\n-    if additional_booleans is None:\n-        additional_booleans = []\n-    if additional_numericals is None:\n-        additional_numericals = []\n-    instance_filepaths = list(problems_directory.iterdir())\n-   \n-\n-    add_console_handler(logging.getLogger(), logging.INFO)\n-    create_experiment_workspace(workspace)\n-    change_working_directory(workspace)\n-\n-    total_timer = Timer()\n-    preprocessing_timer = Timer()\n-    asp_timer = Timer(stopped=True)\n-    verification_timer = Timer(stopped=True)\n-    iteration_data = IterationData()\n-\n-    # Generate data\n-    with change_dir(\"input\"):\n-        logging.info(colored(\"Constructing InstanceDatas...\", \"blue\", \"on_grey\"))\n-        domain_data, instance_datas, num_ss_states, num_gfa_states = compute_instance_datas(domain_filepath, instance_filepaths, disable_closed_Q, max_num_states_per_instance, max_time_per_instance, enable_dump_files)\n-        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-        if instance_datas is None:\n-            raise Exception(\"Failed to create InstanceDatas.\")\n-\n-        state_finder = StateFinder(domain_data, instance_datas)\n-\n-        logging.info(colored(\"Initializing TupleGraphs...\", \"blue\", \"on_grey\"))\n-        gfa_state_id_to_tuple_graph: Dict[int, mm.TupleGraph] = compute_tuple_graphs(domain_data, instance_datas, state_finder, width, enable_dump_files)\n-        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-    preprocessing_data = PreprocessingData(domain_data, instance_datas, state_finder, gfa_state_id_to_tuple_graph)\n-    preprocessing_timer.stop()\n-\n-    if not preprocessing_data.instance_datas:\n-        raise RuntimeError(\"Data is empty\")\n-\n-    sketches = set()\n-    total_features = set()\n-    sketch_features = set()\n-    # Learn sketch\n-    if encoding_type == EncodingType.EXPLICIT:\n-        create_experiment_workspace(workspace)\n-        preprocessing_timer.resume()\n-\n-        for instance_data in preprocessing_data.instance_datas:\n-            print(f\"Instance {instance_data.idx}:\")\n-            for state in instance_data.mimir_ss.get_states():\n-                    state_data = state.get_state()\n-                    fluents = state_data.get_fluent_atoms()\n-                    #print(f\"State {state.get_index()} fluents: {fluents}\")\n-            # TODO: when is the best time to generate features?\n-            # Based on current instance currently looks most reasonable.\n-            # It could be restricted to the states in the tuple graph as well,\n-            # which reduces the number of rules, but not fully sure yet about the consequences in the bigger picture.\n-            iteration_data = IterationData()\n-            iteration_data.instance_datas = [instance_data]\n-            iteration_data.gfa_states = instance_data.gfa.get_states()\n-\n-            # Report some progress\n-            write_file(f\"{instance_data.idx}.dot\", instance_data.dlplan_ss.to_dot(1))\n-            print(\"     \", end=\"\")\n-            print(\"id:\", instance_data.idx,\n-                    \"problem_filepath:\", instance_data.mimir_ss.get_problem().get_filepath(),\n-                    \"num_states:\", instance_data.mimir_ss.get_num_states(),\n-                    \"num_state_equivalences:\", instance_data.gfa.get_num_states())\n-\n-            logging.info(colored(\"Initializing DomainFeatureData...\", \"blue\", \"on_grey\"))\n-            iteration_data.feature_pool = compute_feature_pool(\n-                preprocessing_data,\n-                iteration_data,\n-                gfa_state_id_to_tuple_graph,\n-                state_finder,\n-                disable_feature_generation,\n-                enable_incomplete_feature_pruning,\n-                concept_complexity_limit,\n-                role_complexity_limit,\n-                boolean_complexity_limit,\n-                count_numerical_complexity_limit,\n-                distance_numerical_complexity_limit,\n-                feature_limit,\n-                additional_booleans,\n-                additional_numericals\n-            )\n-            # Add features to features that were generated\n-            total_features.update(feature.dlplan_feature for feature in iteration_data.feature_pool)\n-\n-            logging.info(colored(\"Constructing PerStateFeatureValuations...\", \"blue\", \"on_grey\"))\n-            iteration_data.gfa_state_global_idx_to_feature_evaluations = compute_per_state_feature_valuations(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Constructing StatePairEquivalenceDatas...\", \"blue\", \"on_grey\"))\n-            iteration_data.state_pair_equivalences, iteration_data.gfa_state_global_idx_to_state_pair_equivalence = compute_state_pair_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Constructing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n-            iteration_data.gfa_state_global_idx_to_tuple_graph_equivalence = compute_tuple_graph_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Minimizing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n-            minimize_tuple_graph_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-            preprocessing_timer.stop()\n-            asp_timer.resume()\n-\n-            # We count the number of subgoal tuples for which no rule could be found.\n-            # This usually happens if the pool of features is not sufficiently rich.\n-            count_unsat_tuples = 0\n-            total_transitions = 0 \n-\n-            for gfa_state in instance_data.gfa.get_states():\n-                gfa_state_global_idx = gfa_state.get_global_index()\n-                gfa_state_idx = gfa_state.get_index()\n-\n-                if instance_data.gfa.is_deadend_state(gfa_state_idx):\n-                    continue\n-\n-                #print(f\"Keys in gfa_state_global_idx_to_tuple_graph: {list(preprocessing_data.gfa_state_global_idx_to_tuple_graph.keys())}\")\n-                #print(f\"gfa_state_global_idx: {gfa_state_global_idx}\")\n-\n-                tuple_graph = preprocessing_data.gfa_state_global_idx_to_tuple_graph[gfa_state_global_idx]\n-\n-                for distance, group in enumerate(tuple_graph.get_vertices_grouped_by_distance()):\n-                    if distance == 0:\n-                        # We skip subgoal tuples at distance zero because they do not encode progress towards a goal.\n-                        continue\n-                    for vertex in group:\n-                        # Here we find all simplest single sketch rules for a pair (state, subgoal tuple).\n-                        t_idx = vertex.get_index()\n-                        print(f\"State Transition: s_i = {gfa_state_idx}, s_j = {t_idx}\")\n-                        total_transitions += 1\n-                        start_time = time.time()\n-                        asp_factory = ASPFactory(encoding_type, enable_goal_separating_features, max_num_rules)\n-                        facts = asp_factory.make_facts(preprocessing_data, iteration_data, gfa_state, t_idx)\n-\n-                        print(f\"Factory time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        start_time = time.time()\n-                        asp_factory.ground(facts)\n-                        print(f\"Grounding time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        start_time = time.time()\n-                        symbolss, returncode = asp_factory.solve_all_opt()\n-                        print(f\"Solving time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        if returncode in [ClingoExitCode.UNSATISFIABLE, ClingoExitCode.EXHAUSTED]:\n-                            print(colored(\"ASP is unsatisfiable or exhausted!\", \"red\", \"on_grey\"))\n-                            print(colored(f\"No sketch of width {width} exists that solves all instances!\", \"red\", \"on_grey\"))\n-                            # There might be tuples where we get unsat.\n-                            # If such cases occur, which is very likely in complex domains, then we must look closer into this.\n-                            count_unsat_tuples += 1\n-                            continue\n-                        elif returncode == ClingoExitCode.UNKNOWN:\n-                            print(colored(\"ASP solving throws unknown error!\", \"red\", \"on_grey\"))\n-                            exit(ExitCode.UNKNOWN)\n-                        elif returncode == ClingoExitCode.INTERRUPTED:\n-                            print(colored(\"ASP solving interrupted!\", \"red\", \"on_grey\"))\n-                            exit(ExitCode.INTERRUPTED)\n-                        else:\n-                            logging.info(f\"Solution found with return code: {returncode}\")\n-\n-                        for symbols in symbolss:\n-                            dlplan_policy = ExplicitDlplanPolicyFactory().make_dlplan_policy_from_answer_set(symbols, preprocessing_data, iteration_data)\n-\n-                            sketch = Sketch(dlplan_policy, width)\n-                            sketches.add(sketch)\n-                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_booleans())\n-                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_numericals())\n-                            print(dlplan_policy)\n-\n-    else:\n-        raise Exception(\"No implementation for the given encoding type.\")\n-\n-    preprocessing_timer.stop()\n-    asp_timer.stop()\n-    verification_timer.stop()\n-    total_timer.stop()\n-\n-    # Compute feature histograms by complexity\n-    total_features_by_complexity = defaultdict(int)\n-    for feature in total_features:\n-        total_features_by_complexity[feature.compute_complexity()] += 1\n-    total_features_by_complexity = {key: total_features_by_complexity[key] for key in sorted(total_features_by_complexity)}\n-\n-    sketch_features_by_complexity = defaultdict(int)\n-    for feature in sketch_features:\n-        sketch_features_by_complexity[feature.compute_complexity()] += 1\n-    sketch_features_by_complexity = {key: sketch_features_by_complexity[key] for key in sorted(sketch_features_by_complexity)}\n-\n-    # Output the result\n-    with change_dir(\"output\"):\n-        print_separation_line()\n-        logging.info(colored(\"Summary:\", \"green\", \"on_grey\"))\n-\n-        print(f\"Preprocessing time: {int(preprocessing_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"ASP time: {int(asp_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Verification time: {int(verification_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Total time: {int(total_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Total memory: {int(memory_usage() / 1024)} GiB.\")\n-        print(f\"Total number of transitions: {total_transitions}\")\n-        print(f\"Total number of states: {num_ss_states}\")\n-        print(f\"Total number of abstract states: {num_gfa_states}\")\n-        print(f\"Number of unsat tuples: {count_unsat_tuples}\")\n-        print(f\"Number of sketch rules: {len(sketches)}\")\n-        print(f\"Number of total features: {len(total_features)}\")\n-        print(f\"Number of sketch features: {len(sketch_features)}\")\n-        print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n-        print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n"
                },
                {
                    "date": 1727821101177,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -165,9 +165,9 @@\n                         continue\n                     for vertex in group:\n                         # Here we find all simplest single sketch rules for a pair (state, subgoal tuple).\n                         t_idx = vertex.get_index()\n-                        #print(f\"State Transition: s_i = {gfa_state_idx}, s_j = {t_idx}\")\n+                        #print(f\"State Transition: s_i = {gfa_state_global_idx}, s_j = {t_idx}\")\n                         total_transitions += 1\n                         start_time = time.time()\n                         asp_factory = ASPFactory(encoding_type, enable_goal_separating_features, max_num_rules)\n                         facts = asp_factory.make_facts(preprocessing_data, iteration_data, gfa_state, t_idx)\n"
                },
                {
                    "date": 1727821270370,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -144,9 +144,9 @@\n \n             # We count the number of subgoal tuples for which no rule could be found.\n             # This usually happens if the pool of features is not sufficiently rich.\n             count_unsat_tuples = 0\n-            total_transitions = 0 \n+           \n \n             for gfa_state in instance_data.gfa.get_states():\n                 gfa_state_global_idx = gfa_state.get_global_index()\n                 gfa_state_idx = gfa_state.get_index()\n"
                },
                {
                    "date": 1727821279697,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -166,9 +166,9 @@\n                     for vertex in group:\n                         # Here we find all simplest single sketch rules for a pair (state, subgoal tuple).\n                         t_idx = vertex.get_index()\n                         #print(f\"State Transition: s_i = {gfa_state_global_idx}, s_j = {t_idx}\")\n-                        total_transitions += 1\n+                       \n                         start_time = time.time()\n                         asp_factory = ASPFactory(encoding_type, enable_goal_separating_features, max_num_rules)\n                         facts = asp_factory.make_facts(preprocessing_data, iteration_data, gfa_state, t_idx)\n \n"
                },
                {
                    "date": 1727821288425,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -235,9 +235,8 @@\n         print(f\"ASP time: {int(asp_timer.get_elapsed_sec()) + 1} seconds.\")\n         print(f\"Verification time: {int(verification_timer.get_elapsed_sec()) + 1} seconds.\")\n         print(f\"Total time: {int(total_timer.get_elapsed_sec()) + 1} seconds.\")\n         print(f\"Total memory: {int(memory_usage() / 1024)} GiB.\")\n-        print(f\"Total number of transitions: {total_transitions}\")\n         print(f\"Total number of states: {num_ss_states}\")\n         print(f\"Total number of abstract states: {num_gfa_states}\")\n         print(f\"Number of unsat tuples: {count_unsat_tuples}\")\n         print(f\"Number of sketch rules: {len(sketches)}\")\n"
                },
                {
                    "date": 1727821469866,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -144,8 +144,9 @@\n \n             # We count the number of subgoal tuples for which no rule could be found.\n             # This usually happens if the pool of features is not sufficiently rich.\n             count_unsat_tuples = 0\n+            tuple_graph_states = set()\n            \n \n             for gfa_state in instance_data.gfa.get_states():\n                 gfa_state_global_idx = gfa_state.get_global_index()\n"
                },
                {
                    "date": 1727821524003,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -166,10 +166,10 @@\n                         continue\n                     for vertex in group:\n                         # Here we find all simplest single sketch rules for a pair (state, subgoal tuple).\n                         t_idx = vertex.get_index()\n-                        #print(f\"State Transition: s_i = {gfa_state_global_idx}, s_j = {t_idx}\")\n-                       \n+                        tuple_graph_states.add((gfa_state_global_idx, t_idx))\n+    \n                         start_time = time.time()\n                         asp_factory = ASPFactory(encoding_type, enable_goal_separating_features, max_num_rules)\n                         facts = asp_factory.make_facts(preprocessing_data, iteration_data, gfa_state, t_idx)\n \n"
                },
                {
                    "date": 1727821606332,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -206,8 +206,13 @@\n                             sketches.add(sketch)\n                             sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_booleans())\n                             sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_numericals())\n                             print(dlplan_policy)\n+                            \n+                # Print all transitions in the tuple graph\n+                print(\"Tuple Graph States:\")\n+                for (s_i, s_j) in tuple_graph_states:\n+                    print(f\"State {s_i}, state {s_j}\")\n \n     else:\n         raise Exception(\"No implementation for the given encoding type.\")\n \n"
                },
                {
                    "date": 1727821616068,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -207,9 +207,8 @@\n                             sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_booleans())\n                             sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_numericals())\n                             print(dlplan_policy)\n                             \n-                # Print all transitions in the tuple graph\n                 print(\"Tuple Graph States:\")\n                 for (s_i, s_j) in tuple_graph_states:\n                     print(f\"State {s_i}, state {s_j}\")\n \n"
                },
                {
                    "date": 1727821781334,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -210,11 +210,26 @@\n                             \n                 print(\"Tuple Graph States:\")\n                 for (s_i, s_j) in tuple_graph_states:\n                     print(f\"State {s_i}, state {s_j}\")\n+                \n+                state_pair_equivalences = set()\n+                for (s_i, s_j), equivalence_data in iteration_data.state_pair_equivalences.items():\n+                    state_pair_equivalences.add((s_i, s_j))\n+                    state_pair_equivalences.add((s_j, s_i)) \n+                    \n+                print(\"State Pair Equivalences:\")\n+                for (s_i, s_j) in state_pair_equivalences:\n+                    print(f\"Equivalent states: {s_i} and {s_j}\")\n \n-    else:\n-        raise Exception(\"No implementation for the given encoding type.\")\n+                print(\"Comparison between Tuple Graph States and State Pair Equivalences:\")\n+                for (s_i, s_j) in tuple_graph_states:\n+                    if (s_i, s_j) in state_pair_equivalences:\n+                        print(f\"Transition ({s_i} -> {s_j}) matches with state pair equivalence.\")\n+                    else:\n+                        print(f\"Transition ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n+                    else:\n+                        raise Exception(\"No implementation for the given encoding type.\")\n \n     preprocessing_timer.stop()\n     asp_timer.stop()\n     verification_timer.stop()\n"
                },
                {
                    "date": 1727821826092,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -226,10 +226,10 @@\n                     if (s_i, s_j) in state_pair_equivalences:\n                         print(f\"Transition ({s_i} -> {s_j}) matches with state pair equivalence.\")\n                     else:\n                         print(f\"Transition ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n-                    else:\n-                        raise Exception(\"No implementation for the given encoding type.\")\n+    else:\n+        raise Exception(\"No implementation for the given encoding type.\")\n \n     preprocessing_timer.stop()\n     asp_timer.stop()\n     verification_timer.stop()\n"
                },
                {
                    "date": 1727821931296,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,265 @@\n+import logging\n+import time\n+from collections import defaultdict\n+from pathlib import Path\n+from termcolor import colored\n+from typing import List, MutableSet, Dict\n+import clingo\n+import pymimir as mm\n+from dlplan.policy import PolicyMinimizer, NamedBoolean, NamedNumerical\n+from collections import defaultdict\n+from .src.exit_codes import ExitCode\n+from .src.iteration import EncodingType, ASPFactory, ClingoExitCode, IterationData, LearningStatistics, Sketch, D2sepDlplanPolicyFactory, ExplicitDlplanPolicyFactory, compute_feature_pool, compute_per_state_feature_valuations, compute_state_pair_equivalences, compute_tuple_graph_equivalences, minimize_tuple_graph_equivalences\n+from .src.util import Timer, create_experiment_workspace, change_working_directory, write_file, change_dir, memory_usage, add_console_handler, print_separation_line\n+from .src.preprocessing import InstanceData, PreprocessingData, StateFinder, compute_instance_datas, compute_tuple_graphs\n+\n+def learn_sketch_for_problem_class(\n+    domain_filepath: Path,\n+    problems_directory: Path,\n+    workspace: Path,\n+    width: int,\n+    disable_closed_Q: bool = True,\n+    max_num_states_per_instance: int = 10000,\n+    max_time_per_instance: int = 10,\n+    encoding_type: EncodingType = EncodingType.EXPLICIT,\n+    max_num_rules: int = 1,\n+    enable_goal_separating_features: bool = True,\n+    disable_feature_generation: bool = True,\n+    enable_incomplete_feature_pruning: bool = False,\n+    concept_complexity_limit: int = 9,\n+    role_complexity_limit: int = 9,\n+    boolean_complexity_limit: int = 9,\n+    count_numerical_complexity_limit: int = 9,\n+    distance_numerical_complexity_limit: int = 9,\n+    feature_limit: int = 1000000,\n+    additional_booleans: List[str] = None,\n+    additional_numericals: List[str] = None,\n+    enable_dump_files: bool = False,\n+):\n+    # Initialize variables and setup\n+    if additional_booleans is None:\n+        additional_booleans = []\n+    if additional_numericals is None:\n+        additional_numericals = []\n+    instance_filepaths = list(problems_directory.iterdir())\n+   \n+\n+    add_console_handler(logging.getLogger(), logging.INFO)\n+    create_experiment_workspace(workspace)\n+    change_working_directory(workspace)\n+\n+    total_timer = Timer()\n+    preprocessing_timer = Timer()\n+    asp_timer = Timer(stopped=True)\n+    verification_timer = Timer(stopped=True)\n+    iteration_data = IterationData()\n+\n+    # Generate data\n+    with change_dir(\"input\"):\n+        logging.info(colored(\"Constructing InstanceDatas...\", \"blue\", \"on_grey\"))\n+        domain_data, instance_datas, num_ss_states, num_gfa_states = compute_instance_datas(domain_filepath, instance_filepaths, disable_closed_Q, max_num_states_per_instance, max_time_per_instance, enable_dump_files)\n+        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+        if instance_datas is None:\n+            raise Exception(\"Failed to create InstanceDatas.\")\n+\n+        state_finder = StateFinder(domain_data, instance_datas)\n+\n+        logging.info(colored(\"Initializing TupleGraphs...\", \"blue\", \"on_grey\"))\n+        gfa_state_id_to_tuple_graph: Dict[int, mm.TupleGraph] = compute_tuple_graphs(domain_data, instance_datas, state_finder, width, enable_dump_files)\n+        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+    preprocessing_data = PreprocessingData(domain_data, instance_datas, state_finder, gfa_state_id_to_tuple_graph)\n+    preprocessing_timer.stop()\n+\n+    if not preprocessing_data.instance_datas:\n+        raise RuntimeError(\"Data is empty\")\n+\n+    sketches = set()\n+    total_features = set()\n+    sketch_features = set()\n+    # Learn sketch\n+    if encoding_type == EncodingType.EXPLICIT:\n+        create_experiment_workspace(workspace)\n+        preprocessing_timer.resume()\n+\n+        for instance_data in preprocessing_data.instance_datas:\n+            print(f\"Instance {instance_data.idx}:\")\n+            for state in instance_data.mimir_ss.get_states():\n+                    state_data = state.get_state()\n+                    fluents = state_data.get_fluent_atoms()\n+                    #print(f\"State {state.get_index()} fluents: {fluents}\")\n+            # TODO: when is the best time to generate features?\n+            # Based on current instance currently looks most reasonable.\n+            # It could be restricted to the states in the tuple graph as well,\n+            # which reduces the number of rules, but not fully sure yet about the consequences in the bigger picture.\n+            iteration_data = IterationData()\n+            iteration_data.instance_datas = [instance_data]\n+            iteration_data.gfa_states = instance_data.gfa.get_states()\n+\n+            # Report some progress\n+            write_file(f\"{instance_data.idx}.dot\", instance_data.dlplan_ss.to_dot(1))\n+            print(\"     \", end=\"\")\n+            print(\"id:\", instance_data.idx,\n+                    \"problem_filepath:\", instance_data.mimir_ss.get_problem().get_filepath(),\n+                    \"num_states:\", instance_data.mimir_ss.get_num_states(),\n+                    \"num_state_equivalences:\", instance_data.gfa.get_num_states())\n+\n+            logging.info(colored(\"Initializing DomainFeatureData...\", \"blue\", \"on_grey\"))\n+            iteration_data.feature_pool = compute_feature_pool(\n+                preprocessing_data,\n+                iteration_data,\n+                gfa_state_id_to_tuple_graph,\n+                state_finder,\n+                disable_feature_generation,\n+                enable_incomplete_feature_pruning,\n+                concept_complexity_limit,\n+                role_complexity_limit,\n+                boolean_complexity_limit,\n+                count_numerical_complexity_limit,\n+                distance_numerical_complexity_limit,\n+                feature_limit,\n+                additional_booleans,\n+                additional_numericals\n+            )\n+            # Add features to features that were generated\n+            total_features.update(feature.dlplan_feature for feature in iteration_data.feature_pool)\n+\n+            logging.info(colored(\"Constructing PerStateFeatureValuations...\", \"blue\", \"on_grey\"))\n+            iteration_data.gfa_state_global_idx_to_feature_evaluations = compute_per_state_feature_valuations(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Constructing StatePairEquivalenceDatas...\", \"blue\", \"on_grey\"))\n+            iteration_data.state_pair_equivalences, iteration_data.gfa_state_global_idx_to_state_pair_equivalence = compute_state_pair_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Constructing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n+            iteration_data.gfa_state_global_idx_to_tuple_graph_equivalence = compute_tuple_graph_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Minimizing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n+            minimize_tuple_graph_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+            preprocessing_timer.stop()\n+            asp_timer.resume()\n+\n+            # We count the number of subgoal tuples for which no rule could be found.\n+            # This usually happens if the pool of features is not sufficiently rich.\n+            count_unsat_tuples = 0\n+            tuple_graph_states = set()\n+           \n+\n+            for gfa_state in instance_data.gfa.get_states():\n+                gfa_state_global_idx = gfa_state.get_global_index()\n+                gfa_state_idx = gfa_state.get_index()\n+\n+                if instance_data.gfa.is_deadend_state(gfa_state_idx):\n+                    continue\n+\n+                #print(f\"Keys in gfa_state_global_idx_to_tuple_graph: {list(preprocessing_data.gfa_state_global_idx_to_tuple_graph.keys())}\")\n+                #print(f\"gfa_state_global_idx: {gfa_state_global_idx}\")\n+\n+                tuple_graph = preprocessing_data.gfa_state_global_idx_to_tuple_graph[gfa_state_global_idx]\n+\n+                for distance, group in enumerate(tuple_graph.get_vertices_grouped_by_distance()):\n+                    if distance == 0:\n+                        # We skip subgoal tuples at distance zero because they do not encode progress towards a goal.\n+                        continue\n+                    for vertex in group:\n+                        # Here we find all simplest single sketch rules for a pair (state, subgoal tuple).\n+                        t_idx = vertex.get_index()\n+                        tuple_graph_states.add((gfa_state_global_idx, t_idx))\n+    \n+                        start_time = time.time()\n+                        asp_factory = ASPFactory(encoding_type, enable_goal_separating_features, max_num_rules)\n+                        facts = asp_factory.make_facts(preprocessing_data, iteration_data, gfa_state, t_idx)\n+\n+                        print(f\"Factory time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        start_time = time.time()\n+                        asp_factory.ground(facts)\n+                        print(f\"Grounding time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        start_time = time.time()\n+                        symbolss, returncode = asp_factory.solve_all_opt()\n+                        print(f\"Solving time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        if returncode in [ClingoExitCode.UNSATISFIABLE, ClingoExitCode.EXHAUSTED]:\n+                            print(colored(\"ASP is unsatisfiable or exhausted!\", \"red\", \"on_grey\"))\n+                            print(colored(f\"No sketch of width {width} exists that solves all instances!\", \"red\", \"on_grey\"))\n+                            # There might be tuples where we get unsat.\n+                            # If such cases occur, which is very likely in complex domains, then we must look closer into this.\n+                            count_unsat_tuples += 1\n+                            continue\n+                        elif returncode == ClingoExitCode.UNKNOWN:\n+                            print(colored(\"ASP solving throws unknown error!\", \"red\", \"on_grey\"))\n+                            exit(ExitCode.UNKNOWN)\n+                        elif returncode == ClingoExitCode.INTERRUPTED:\n+                            print(colored(\"ASP solving interrupted!\", \"red\", \"on_grey\"))\n+                            exit(ExitCode.INTERRUPTED)\n+                        else:\n+                            logging.info(f\"Solution found with return code: {returncode}\")\n+\n+                        for symbols in symbolss:\n+                            dlplan_policy = ExplicitDlplanPolicyFactory().make_dlplan_policy_from_answer_set(symbols, preprocessing_data, iteration_data)\n+\n+                            sketch = Sketch(dlplan_policy, width)\n+                            sketches.add(sketch)\n+                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_booleans())\n+                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_numericals())\n+                            print(dlplan_policy)\n+                            \n+                print(\"Tuple Graph States:\")\n+                for (s_i, s_j) in tuple_graph_states:\n+                    print(f\"State {s_i}, state {s_j}\")\n+                \n+                state_pair_equivalences = set()\n+                for equivalence in iteration_data.state_pair_equivalences:\n+                    state_pair_equivalences.append((equivalence[0], equivalence[1]))\n+                    \n+                print(\"State Pair Equivalences:\")\n+                for (s_i, s_j) in state_pair_equivalences:\n+                    print(f\"Equivalent states: {s_i} and {s_j}\")\n+\n+                print(\"Comparison between Tuple Graph States and State Pair Equivalences:\")\n+                for (s_i, s_j) in tuple_graph_states:\n+                    if (s_i, s_j) in state_pair_equivalences:\n+                        print(f\"Transition ({s_i} -> {s_j}) matches with state pair equivalence.\")\n+                    else:\n+                        print(f\"Transition ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n+    else:\n+        raise Exception(\"No implementation for the given encoding type.\")\n+\n+    preprocessing_timer.stop()\n+    asp_timer.stop()\n+    verification_timer.stop()\n+    total_timer.stop()\n+\n+    # Compute feature histograms by complexity\n+    total_features_by_complexity = defaultdict(int)\n+    for feature in total_features:\n+        total_features_by_complexity[feature.compute_complexity()] += 1\n+    total_features_by_complexity = {key: total_features_by_complexity[key] for key in sorted(total_features_by_complexity)}\n+\n+    sketch_features_by_complexity = defaultdict(int)\n+    for feature in sketch_features:\n+        sketch_features_by_complexity[feature.compute_complexity()] += 1\n+    sketch_features_by_complexity = {key: sketch_features_by_complexity[key] for key in sorted(sketch_features_by_complexity)}\n+\n+    # Output the result\n+    with change_dir(\"output\"):\n+        print_separation_line()\n+        logging.info(colored(\"Summary:\", \"green\", \"on_grey\"))\n+\n+        print(f\"Preprocessing time: {int(preprocessing_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"ASP time: {int(asp_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Verification time: {int(verification_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Total time: {int(total_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Total memory: {int(memory_usage() / 1024)} GiB.\")\n+        print(f\"Total number of states: {num_ss_states}\")\n+        print(f\"Total number of abstract states: {num_gfa_states}\")\n+        print(f\"Number of unsat tuples: {count_unsat_tuples}\")\n+        print(f\"Number of sketch rules: {len(sketches)}\")\n+        print(f\"Number of total features: {len(total_features)}\")\n+        print(f\"Number of sketch features: {len(sketch_features)}\")\n+        print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n+        print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n"
                },
                {
                    "date": 1727821947898,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -262,270 +262,4 @@\n         print(f\"Number of total features: {len(total_features)}\")\n         print(f\"Number of sketch features: {len(sketch_features)}\")\n         print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n         print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n-import logging\n-import time\n-from collections import defaultdict\n-from pathlib import Path\n-from termcolor import colored\n-from typing import List, MutableSet, Dict\n-import clingo\n-import pymimir as mm\n-from dlplan.policy import PolicyMinimizer, NamedBoolean, NamedNumerical\n-from collections import defaultdict\n-from .src.exit_codes import ExitCode\n-from .src.iteration import EncodingType, ASPFactory, ClingoExitCode, IterationData, LearningStatistics, Sketch, D2sepDlplanPolicyFactory, ExplicitDlplanPolicyFactory, compute_feature_pool, compute_per_state_feature_valuations, compute_state_pair_equivalences, compute_tuple_graph_equivalences, minimize_tuple_graph_equivalences\n-from .src.util import Timer, create_experiment_workspace, change_working_directory, write_file, change_dir, memory_usage, add_console_handler, print_separation_line\n-from .src.preprocessing import InstanceData, PreprocessingData, StateFinder, compute_instance_datas, compute_tuple_graphs\n-\n-def learn_sketch_for_problem_class(\n-    domain_filepath: Path,\n-    problems_directory: Path,\n-    workspace: Path,\n-    width: int,\n-    disable_closed_Q: bool = True,\n-    max_num_states_per_instance: int = 10000,\n-    max_time_per_instance: int = 10,\n-    encoding_type: EncodingType = EncodingType.EXPLICIT,\n-    max_num_rules: int = 1,\n-    enable_goal_separating_features: bool = True,\n-    disable_feature_generation: bool = True,\n-    enable_incomplete_feature_pruning: bool = False,\n-    concept_complexity_limit: int = 9,\n-    role_complexity_limit: int = 9,\n-    boolean_complexity_limit: int = 9,\n-    count_numerical_complexity_limit: int = 9,\n-    distance_numerical_complexity_limit: int = 9,\n-    feature_limit: int = 1000000,\n-    additional_booleans: List[str] = None,\n-    additional_numericals: List[str] = None,\n-    enable_dump_files: bool = False,\n-):\n-    # Initialize variables and setup\n-    if additional_booleans is None:\n-        additional_booleans = []\n-    if additional_numericals is None:\n-        additional_numericals = []\n-    instance_filepaths = list(problems_directory.iterdir())\n-   \n-\n-    add_console_handler(logging.getLogger(), logging.INFO)\n-    create_experiment_workspace(workspace)\n-    change_working_directory(workspace)\n-\n-    total_timer = Timer()\n-    preprocessing_timer = Timer()\n-    asp_timer = Timer(stopped=True)\n-    verification_timer = Timer(stopped=True)\n-    iteration_data = IterationData()\n-\n-    # Generate data\n-    with change_dir(\"input\"):\n-        logging.info(colored(\"Constructing InstanceDatas...\", \"blue\", \"on_grey\"))\n-        domain_data, instance_datas, num_ss_states, num_gfa_states = compute_instance_datas(domain_filepath, instance_filepaths, disable_closed_Q, max_num_states_per_instance, max_time_per_instance, enable_dump_files)\n-        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-        if instance_datas is None:\n-            raise Exception(\"Failed to create InstanceDatas.\")\n-\n-        state_finder = StateFinder(domain_data, instance_datas)\n-\n-        logging.info(colored(\"Initializing TupleGraphs...\", \"blue\", \"on_grey\"))\n-        gfa_state_id_to_tuple_graph: Dict[int, mm.TupleGraph] = compute_tuple_graphs(domain_data, instance_datas, state_finder, width, enable_dump_files)\n-        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-    preprocessing_data = PreprocessingData(domain_data, instance_datas, state_finder, gfa_state_id_to_tuple_graph)\n-    preprocessing_timer.stop()\n-\n-    if not preprocessing_data.instance_datas:\n-        raise RuntimeError(\"Data is empty\")\n-\n-    sketches = set()\n-    total_features = set()\n-    sketch_features = set()\n-    # Learn sketch\n-    if encoding_type == EncodingType.EXPLICIT:\n-        create_experiment_workspace(workspace)\n-        preprocessing_timer.resume()\n-\n-        for instance_data in preprocessing_data.instance_datas:\n-            print(f\"Instance {instance_data.idx}:\")\n-            for state in instance_data.mimir_ss.get_states():\n-                    state_data = state.get_state()\n-                    fluents = state_data.get_fluent_atoms()\n-                    #print(f\"State {state.get_index()} fluents: {fluents}\")\n-            # TODO: when is the best time to generate features?\n-            # Based on current instance currently looks most reasonable.\n-            # It could be restricted to the states in the tuple graph as well,\n-            # which reduces the number of rules, but not fully sure yet about the consequences in the bigger picture.\n-            iteration_data = IterationData()\n-            iteration_data.instance_datas = [instance_data]\n-            iteration_data.gfa_states = instance_data.gfa.get_states()\n-\n-            # Report some progress\n-            write_file(f\"{instance_data.idx}.dot\", instance_data.dlplan_ss.to_dot(1))\n-            print(\"     \", end=\"\")\n-            print(\"id:\", instance_data.idx,\n-                    \"problem_filepath:\", instance_data.mimir_ss.get_problem().get_filepath(),\n-                    \"num_states:\", instance_data.mimir_ss.get_num_states(),\n-                    \"num_state_equivalences:\", instance_data.gfa.get_num_states())\n-\n-            logging.info(colored(\"Initializing DomainFeatureData...\", \"blue\", \"on_grey\"))\n-            iteration_data.feature_pool = compute_feature_pool(\n-                preprocessing_data,\n-                iteration_data,\n-                gfa_state_id_to_tuple_graph,\n-                state_finder,\n-                disable_feature_generation,\n-                enable_incomplete_feature_pruning,\n-                concept_complexity_limit,\n-                role_complexity_limit,\n-                boolean_complexity_limit,\n-                count_numerical_complexity_limit,\n-                distance_numerical_complexity_limit,\n-                feature_limit,\n-                additional_booleans,\n-                additional_numericals\n-            )\n-            # Add features to features that were generated\n-            total_features.update(feature.dlplan_feature for feature in iteration_data.feature_pool)\n-\n-            logging.info(colored(\"Constructing PerStateFeatureValuations...\", \"blue\", \"on_grey\"))\n-            iteration_data.gfa_state_global_idx_to_feature_evaluations = compute_per_state_feature_valuations(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Constructing StatePairEquivalenceDatas...\", \"blue\", \"on_grey\"))\n-            iteration_data.state_pair_equivalences, iteration_data.gfa_state_global_idx_to_state_pair_equivalence = compute_state_pair_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Constructing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n-            iteration_data.gfa_state_global_idx_to_tuple_graph_equivalence = compute_tuple_graph_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Minimizing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n-            minimize_tuple_graph_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-            preprocessing_timer.stop()\n-            asp_timer.resume()\n-\n-            # We count the number of subgoal tuples for which no rule could be found.\n-            # This usually happens if the pool of features is not sufficiently rich.\n-            count_unsat_tuples = 0\n-            tuple_graph_states = set()\n-           \n-\n-            for gfa_state in instance_data.gfa.get_states():\n-                gfa_state_global_idx = gfa_state.get_global_index()\n-                gfa_state_idx = gfa_state.get_index()\n-\n-                if instance_data.gfa.is_deadend_state(gfa_state_idx):\n-                    continue\n-\n-                #print(f\"Keys in gfa_state_global_idx_to_tuple_graph: {list(preprocessing_data.gfa_state_global_idx_to_tuple_graph.keys())}\")\n-                #print(f\"gfa_state_global_idx: {gfa_state_global_idx}\")\n-\n-                tuple_graph = preprocessing_data.gfa_state_global_idx_to_tuple_graph[gfa_state_global_idx]\n-\n-                for distance, group in enumerate(tuple_graph.get_vertices_grouped_by_distance()):\n-                    if distance == 0:\n-                        # We skip subgoal tuples at distance zero because they do not encode progress towards a goal.\n-                        continue\n-                    for vertex in group:\n-                        # Here we find all simplest single sketch rules for a pair (state, subgoal tuple).\n-                        t_idx = vertex.get_index()\n-                        tuple_graph_states.add((gfa_state_global_idx, t_idx))\n-    \n-                        start_time = time.time()\n-                        asp_factory = ASPFactory(encoding_type, enable_goal_separating_features, max_num_rules)\n-                        facts = asp_factory.make_facts(preprocessing_data, iteration_data, gfa_state, t_idx)\n-\n-                        print(f\"Factory time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        start_time = time.time()\n-                        asp_factory.ground(facts)\n-                        print(f\"Grounding time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        start_time = time.time()\n-                        symbolss, returncode = asp_factory.solve_all_opt()\n-                        print(f\"Solving time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        if returncode in [ClingoExitCode.UNSATISFIABLE, ClingoExitCode.EXHAUSTED]:\n-                            print(colored(\"ASP is unsatisfiable or exhausted!\", \"red\", \"on_grey\"))\n-                            print(colored(f\"No sketch of width {width} exists that solves all instances!\", \"red\", \"on_grey\"))\n-                            # There might be tuples where we get unsat.\n-                            # If such cases occur, which is very likely in complex domains, then we must look closer into this.\n-                            count_unsat_tuples += 1\n-                            continue\n-                        elif returncode == ClingoExitCode.UNKNOWN:\n-                            print(colored(\"ASP solving throws unknown error!\", \"red\", \"on_grey\"))\n-                            exit(ExitCode.UNKNOWN)\n-                        elif returncode == ClingoExitCode.INTERRUPTED:\n-                            print(colored(\"ASP solving interrupted!\", \"red\", \"on_grey\"))\n-                            exit(ExitCode.INTERRUPTED)\n-                        else:\n-                            logging.info(f\"Solution found with return code: {returncode}\")\n-\n-                        for symbols in symbolss:\n-                            dlplan_policy = ExplicitDlplanPolicyFactory().make_dlplan_policy_from_answer_set(symbols, preprocessing_data, iteration_data)\n-\n-                            sketch = Sketch(dlplan_policy, width)\n-                            sketches.add(sketch)\n-                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_booleans())\n-                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_numericals())\n-                            print(dlplan_policy)\n-                            \n-                print(\"Tuple Graph States:\")\n-                for (s_i, s_j) in tuple_graph_states:\n-                    print(f\"State {s_i}, state {s_j}\")\n-                \n-                state_pair_equivalences = set()\n-                for (s_i, s_j), equivalence_data in iteration_data.state_pair_equivalences.items():\n-                    state_pair_equivalences.add((s_i, s_j))\n-                    state_pair_equivalences.add((s_j, s_i)) \n-                    \n-                print(\"State Pair Equivalences:\")\n-                for (s_i, s_j) in state_pair_equivalences:\n-                    print(f\"Equivalent states: {s_i} and {s_j}\")\n-\n-                print(\"Comparison between Tuple Graph States and State Pair Equivalences:\")\n-                for (s_i, s_j) in tuple_graph_states:\n-                    if (s_i, s_j) in state_pair_equivalences:\n-                        print(f\"Transition ({s_i} -> {s_j}) matches with state pair equivalence.\")\n-                    else:\n-                        print(f\"Transition ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n-    else:\n-        raise Exception(\"No implementation for the given encoding type.\")\n-\n-    preprocessing_timer.stop()\n-    asp_timer.stop()\n-    verification_timer.stop()\n-    total_timer.stop()\n-\n-    # Compute feature histograms by complexity\n-    total_features_by_complexity = defaultdict(int)\n-    for feature in total_features:\n-        total_features_by_complexity[feature.compute_complexity()] += 1\n-    total_features_by_complexity = {key: total_features_by_complexity[key] for key in sorted(total_features_by_complexity)}\n-\n-    sketch_features_by_complexity = defaultdict(int)\n-    for feature in sketch_features:\n-        sketch_features_by_complexity[feature.compute_complexity()] += 1\n-    sketch_features_by_complexity = {key: sketch_features_by_complexity[key] for key in sorted(sketch_features_by_complexity)}\n-\n-    # Output the result\n-    with change_dir(\"output\"):\n-        print_separation_line()\n-        logging.info(colored(\"Summary:\", \"green\", \"on_grey\"))\n-\n-        print(f\"Preprocessing time: {int(preprocessing_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"ASP time: {int(asp_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Verification time: {int(verification_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Total time: {int(total_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Total memory: {int(memory_usage() / 1024)} GiB.\")\n-        print(f\"Total number of states: {num_ss_states}\")\n-        print(f\"Total number of abstract states: {num_gfa_states}\")\n-        print(f\"Number of unsat tuples: {count_unsat_tuples}\")\n-        print(f\"Number of sketch rules: {len(sketches)}\")\n-        print(f\"Number of total features: {len(total_features)}\")\n-        print(f\"Number of sketch features: {len(sketch_features)}\")\n-        print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n-        print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n"
                },
                {
                    "date": 1727821971652,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -213,9 +213,9 @@\n                     print(f\"State {s_i}, state {s_j}\")\n                 \n                 state_pair_equivalences = set()\n                 for equivalence in iteration_data.state_pair_equivalences:\n-                    state_pair_equivalences.append((equivalence[0], equivalence[1]))\n+                    state_pair_equivalences.add((equivalence[0], equivalence[1]))\n                     \n                 print(\"State Pair Equivalences:\")\n                 for (s_i, s_j) in state_pair_equivalences:\n                     print(f\"Equivalent states: {s_i} and {s_j}\")\n"
                },
                {
                    "date": 1727821982407,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -213,9 +213,9 @@\n                     print(f\"State {s_i}, state {s_j}\")\n                 \n                 state_pair_equivalences = set()\n                 for equivalence in iteration_data.state_pair_equivalences:\n-                    state_pair_equivalences.add((equivalence[0], equivalence[1]))\n+                    state_pair_equivalences.append((equivalence[0], equivalence[1]))\n                     \n                 print(\"State Pair Equivalences:\")\n                 for (s_i, s_j) in state_pair_equivalences:\n                     print(f\"Equivalent states: {s_i} and {s_j}\")\n"
                },
                {
                    "date": 1727822001345,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -211,9 +211,9 @@\n                 print(\"Tuple Graph States:\")\n                 for (s_i, s_j) in tuple_graph_states:\n                     print(f\"State {s_i}, state {s_j}\")\n                 \n-                state_pair_equivalences = set()\n+                state_pair_equivalences = []\n                 for equivalence in iteration_data.state_pair_equivalences:\n                     state_pair_equivalences.append((equivalence[0], equivalence[1]))\n                     \n                 print(\"State Pair Equivalences:\")\n"
                },
                {
                    "date": 1727822111368,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -212,10 +212,13 @@\n                 for (s_i, s_j) in tuple_graph_states:\n                     print(f\"State {s_i}, state {s_j}\")\n                 \n                 state_pair_equivalences = []\n+\n                 for equivalence in iteration_data.state_pair_equivalences:\n-                    state_pair_equivalences.append((equivalence[0], equivalence[1]))\n+                    source_state = equivalence.source_state  \n+                    target_state = equivalence.target_state \n+                    state_pair_equivalences.append((source_state, target_state))\n                     \n                 print(\"State Pair Equivalences:\")\n                 for (s_i, s_j) in state_pair_equivalences:\n                     print(f\"Equivalent states: {s_i} and {s_j}\")\n"
                },
                {
                    "date": 1727822288445,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -212,11 +212,11 @@\n                 for (s_i, s_j) in tuple_graph_states:\n                     print(f\"State {s_i}, state {s_j}\")\n                 \n                 state_pair_equivalences = []\n-\n+        \n                 for equivalence in iteration_data.state_pair_equivalences:\n-                    source_state = equivalence.source_state  \n+                    source_state = equivalence.source_state\n                     target_state = equivalence.target_state \n                     state_pair_equivalences.append((source_state, target_state))\n                     \n                 print(\"State Pair Equivalences:\")\n"
                },
                {
                    "date": 1727822346639,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,268 @@\n+import logging\n+import time\n+from collections import defaultdict\n+from pathlib import Path\n+from termcolor import colored\n+from typing import List, MutableSet, Dict\n+import clingo\n+import pymimir as mm\n+from dlplan.policy import PolicyMinimizer, NamedBoolean, NamedNumerical\n+from collections import defaultdict\n+from .src.exit_codes import ExitCode\n+from .src.iteration import EncodingType, ASPFactory, ClingoExitCode, IterationData, LearningStatistics, Sketch, D2sepDlplanPolicyFactory, ExplicitDlplanPolicyFactory, compute_feature_pool, compute_per_state_feature_valuations, compute_state_pair_equivalences, compute_tuple_graph_equivalences, minimize_tuple_graph_equivalences\n+from .src.util import Timer, create_experiment_workspace, change_working_directory, write_file, change_dir, memory_usage, add_console_handler, print_separation_line\n+from .src.preprocessing import InstanceData, PreprocessingData, StateFinder, compute_instance_datas, compute_tuple_graphs\n+\n+def learn_sketch_for_problem_class(\n+    domain_filepath: Path,\n+    problems_directory: Path,\n+    workspace: Path,\n+    width: int,\n+    disable_closed_Q: bool = True,\n+    max_num_states_per_instance: int = 10000,\n+    max_time_per_instance: int = 10,\n+    encoding_type: EncodingType = EncodingType.EXPLICIT,\n+    max_num_rules: int = 1,\n+    enable_goal_separating_features: bool = True,\n+    disable_feature_generation: bool = True,\n+    enable_incomplete_feature_pruning: bool = False,\n+    concept_complexity_limit: int = 9,\n+    role_complexity_limit: int = 9,\n+    boolean_complexity_limit: int = 9,\n+    count_numerical_complexity_limit: int = 9,\n+    distance_numerical_complexity_limit: int = 9,\n+    feature_limit: int = 1000000,\n+    additional_booleans: List[str] = None,\n+    additional_numericals: List[str] = None,\n+    enable_dump_files: bool = False,\n+):\n+    # Initialize variables and setup\n+    if additional_booleans is None:\n+        additional_booleans = []\n+    if additional_numericals is None:\n+        additional_numericals = []\n+    instance_filepaths = list(problems_directory.iterdir())\n+   \n+\n+    add_console_handler(logging.getLogger(), logging.INFO)\n+    create_experiment_workspace(workspace)\n+    change_working_directory(workspace)\n+\n+    total_timer = Timer()\n+    preprocessing_timer = Timer()\n+    asp_timer = Timer(stopped=True)\n+    verification_timer = Timer(stopped=True)\n+    iteration_data = IterationData()\n+\n+    # Generate data\n+    with change_dir(\"input\"):\n+        logging.info(colored(\"Constructing InstanceDatas...\", \"blue\", \"on_grey\"))\n+        domain_data, instance_datas, num_ss_states, num_gfa_states = compute_instance_datas(domain_filepath, instance_filepaths, disable_closed_Q, max_num_states_per_instance, max_time_per_instance, enable_dump_files)\n+        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+        if instance_datas is None:\n+            raise Exception(\"Failed to create InstanceDatas.\")\n+\n+        state_finder = StateFinder(domain_data, instance_datas)\n+\n+        logging.info(colored(\"Initializing TupleGraphs...\", \"blue\", \"on_grey\"))\n+        gfa_state_id_to_tuple_graph: Dict[int, mm.TupleGraph] = compute_tuple_graphs(domain_data, instance_datas, state_finder, width, enable_dump_files)\n+        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+    preprocessing_data = PreprocessingData(domain_data, instance_datas, state_finder, gfa_state_id_to_tuple_graph)\n+    preprocessing_timer.stop()\n+\n+    if not preprocessing_data.instance_datas:\n+        raise RuntimeError(\"Data is empty\")\n+\n+    sketches = set()\n+    total_features = set()\n+    sketch_features = set()\n+    # Learn sketch\n+    if encoding_type == EncodingType.EXPLICIT:\n+        create_experiment_workspace(workspace)\n+        preprocessing_timer.resume()\n+\n+        for instance_data in preprocessing_data.instance_datas:\n+            print(f\"Instance {instance_data.idx}:\")\n+            for state in instance_data.mimir_ss.get_states():\n+                    state_data = state.get_state()\n+                    fluents = state_data.get_fluent_atoms()\n+                    #print(f\"State {state.get_index()} fluents: {fluents}\")\n+            # TODO: when is the best time to generate features?\n+            # Based on current instance currently looks most reasonable.\n+            # It could be restricted to the states in the tuple graph as well,\n+            # which reduces the number of rules, but not fully sure yet about the consequences in the bigger picture.\n+            iteration_data = IterationData()\n+            iteration_data.instance_datas = [instance_data]\n+            iteration_data.gfa_states = instance_data.gfa.get_states()\n+\n+            # Report some progress\n+            write_file(f\"{instance_data.idx}.dot\", instance_data.dlplan_ss.to_dot(1))\n+            print(\"     \", end=\"\")\n+            print(\"id:\", instance_data.idx,\n+                    \"problem_filepath:\", instance_data.mimir_ss.get_problem().get_filepath(),\n+                    \"num_states:\", instance_data.mimir_ss.get_num_states(),\n+                    \"num_state_equivalences:\", instance_data.gfa.get_num_states())\n+\n+            logging.info(colored(\"Initializing DomainFeatureData...\", \"blue\", \"on_grey\"))\n+            iteration_data.feature_pool = compute_feature_pool(\n+                preprocessing_data,\n+                iteration_data,\n+                gfa_state_id_to_tuple_graph,\n+                state_finder,\n+                disable_feature_generation,\n+                enable_incomplete_feature_pruning,\n+                concept_complexity_limit,\n+                role_complexity_limit,\n+                boolean_complexity_limit,\n+                count_numerical_complexity_limit,\n+                distance_numerical_complexity_limit,\n+                feature_limit,\n+                additional_booleans,\n+                additional_numericals\n+            )\n+            # Add features to features that were generated\n+            total_features.update(feature.dlplan_feature for feature in iteration_data.feature_pool)\n+\n+            logging.info(colored(\"Constructing PerStateFeatureValuations...\", \"blue\", \"on_grey\"))\n+            iteration_data.gfa_state_global_idx_to_feature_evaluations = compute_per_state_feature_valuations(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Constructing StatePairEquivalenceDatas...\", \"blue\", \"on_grey\"))\n+            iteration_data.state_pair_equivalences, iteration_data.gfa_state_global_idx_to_state_pair_equivalence = compute_state_pair_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Constructing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n+            iteration_data.gfa_state_global_idx_to_tuple_graph_equivalence = compute_tuple_graph_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Minimizing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n+            minimize_tuple_graph_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+            preprocessing_timer.stop()\n+            asp_timer.resume()\n+\n+            # We count the number of subgoal tuples for which no rule could be found.\n+            # This usually happens if the pool of features is not sufficiently rich.\n+            count_unsat_tuples = 0\n+            tuple_graph_states = set()\n+           \n+\n+            for gfa_state in instance_data.gfa.get_states():\n+                gfa_state_global_idx = gfa_state.get_global_index()\n+                gfa_state_idx = gfa_state.get_index()\n+\n+                if instance_data.gfa.is_deadend_state(gfa_state_idx):\n+                    continue\n+\n+                #print(f\"Keys in gfa_state_global_idx_to_tuple_graph: {list(preprocessing_data.gfa_state_global_idx_to_tuple_graph.keys())}\")\n+                #print(f\"gfa_state_global_idx: {gfa_state_global_idx}\")\n+\n+                tuple_graph = preprocessing_data.gfa_state_global_idx_to_tuple_graph[gfa_state_global_idx]\n+\n+                for distance, group in enumerate(tuple_graph.get_vertices_grouped_by_distance()):\n+                    if distance == 0:\n+                        # We skip subgoal tuples at distance zero because they do not encode progress towards a goal.\n+                        continue\n+                    for vertex in group:\n+                        # Here we find all simplest single sketch rules for a pair (state, subgoal tuple).\n+                        t_idx = vertex.get_index()\n+                        tuple_graph_states.add((gfa_state_global_idx, t_idx))\n+    \n+                        start_time = time.time()\n+                        asp_factory = ASPFactory(encoding_type, enable_goal_separating_features, max_num_rules)\n+                        facts = asp_factory.make_facts(preprocessing_data, iteration_data, gfa_state, t_idx)\n+\n+                        print(f\"Factory time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        start_time = time.time()\n+                        asp_factory.ground(facts)\n+                        print(f\"Grounding time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        start_time = time.time()\n+                        symbolss, returncode = asp_factory.solve_all_opt()\n+                        print(f\"Solving time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        if returncode in [ClingoExitCode.UNSATISFIABLE, ClingoExitCode.EXHAUSTED]:\n+                            print(colored(\"ASP is unsatisfiable or exhausted!\", \"red\", \"on_grey\"))\n+                            print(colored(f\"No sketch of width {width} exists that solves all instances!\", \"red\", \"on_grey\"))\n+                            # There might be tuples where we get unsat.\n+                            # If such cases occur, which is very likely in complex domains, then we must look closer into this.\n+                            count_unsat_tuples += 1\n+                            continue\n+                        elif returncode == ClingoExitCode.UNKNOWN:\n+                            print(colored(\"ASP solving throws unknown error!\", \"red\", \"on_grey\"))\n+                            exit(ExitCode.UNKNOWN)\n+                        elif returncode == ClingoExitCode.INTERRUPTED:\n+                            print(colored(\"ASP solving interrupted!\", \"red\", \"on_grey\"))\n+                            exit(ExitCode.INTERRUPTED)\n+                        else:\n+                            logging.info(f\"Solution found with return code: {returncode}\")\n+\n+                        for symbols in symbolss:\n+                            dlplan_policy = ExplicitDlplanPolicyFactory().make_dlplan_policy_from_answer_set(symbols, preprocessing_data, iteration_data)\n+\n+                            sketch = Sketch(dlplan_policy, width)\n+                            sketches.add(sketch)\n+                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_booleans())\n+                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_numericals())\n+                            print(dlplan_policy)\n+                            \n+                print(\"Tuple Graph States:\")\n+                for (s_i, s_j) in tuple_graph_states:\n+                    print(f\"State {s_i}, state {s_j}\")\n+                \n+                state_pair_equivalences = []\n+        \n+                for equivalence in iteration_data.state_pair_equivalences:\n+                    source_state = equivalence.get_source_state()\n+                    target_state = equivalence.get_target_state() \n+                    state_pair_equivalences.append((source_state, target_state))\n+                    \n+                print(\"State Pair Equivalences:\")\n+                for (s_i, s_j) in state_pair_equivalences:\n+                    print(f\"Equivalent states: {s_i} and {s_j}\")\n+\n+                print(\"Comparison between Tuple Graph States and State Pair Equivalences:\")\n+                for (s_i, s_j) in tuple_graph_states:\n+                    if (s_i, s_j) in state_pair_equivalences:\n+                        print(f\"Transition ({s_i} -> {s_j}) matches with state pair equivalence.\")\n+                    else:\n+                        print(f\"Transition ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n+    else:\n+        raise Exception(\"No implementation for the given encoding type.\")\n+\n+    preprocessing_timer.stop()\n+    asp_timer.stop()\n+    verification_timer.stop()\n+    total_timer.stop()\n+\n+    # Compute feature histograms by complexity\n+    total_features_by_complexity = defaultdict(int)\n+    for feature in total_features:\n+        total_features_by_complexity[feature.compute_complexity()] += 1\n+    total_features_by_complexity = {key: total_features_by_complexity[key] for key in sorted(total_features_by_complexity)}\n+\n+    sketch_features_by_complexity = defaultdict(int)\n+    for feature in sketch_features:\n+        sketch_features_by_complexity[feature.compute_complexity()] += 1\n+    sketch_features_by_complexity = {key: sketch_features_by_complexity[key] for key in sorted(sketch_features_by_complexity)}\n+\n+    # Output the result\n+    with change_dir(\"output\"):\n+        print_separation_line()\n+        logging.info(colored(\"Summary:\", \"green\", \"on_grey\"))\n+\n+        print(f\"Preprocessing time: {int(preprocessing_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"ASP time: {int(asp_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Verification time: {int(verification_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Total time: {int(total_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Total memory: {int(memory_usage() / 1024)} GiB.\")\n+        print(f\"Total number of states: {num_ss_states}\")\n+        print(f\"Total number of abstract states: {num_gfa_states}\")\n+        print(f\"Number of unsat tuples: {count_unsat_tuples}\")\n+        print(f\"Number of sketch rules: {len(sketches)}\")\n+        print(f\"Number of total features: {len(total_features)}\")\n+        print(f\"Number of sketch features: {len(sketch_features)}\")\n+        print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n+        print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n"
                },
                {
                    "date": 1727822390008,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,266 @@\n+import logging\n+import time\n+from collections import defaultdict\n+from pathlib import Path\n+from termcolor import colored\n+from typing import List, MutableSet, Dict\n+import clingo\n+import pymimir as mm\n+from dlplan.policy import PolicyMinimizer, NamedBoolean, NamedNumerical\n+from collections import defaultdict\n+from .src.exit_codes import ExitCode\n+from .src.iteration import EncodingType, ASPFactory, ClingoExitCode, IterationData, LearningStatistics, Sketch, D2sepDlplanPolicyFactory, ExplicitDlplanPolicyFactory, compute_feature_pool, compute_per_state_feature_valuations, compute_state_pair_equivalences, compute_tuple_graph_equivalences, minimize_tuple_graph_equivalences\n+from .src.util import Timer, create_experiment_workspace, change_working_directory, write_file, change_dir, memory_usage, add_console_handler, print_separation_line\n+from .src.preprocessing import InstanceData, PreprocessingData, StateFinder, compute_instance_datas, compute_tuple_graphs\n+\n+def learn_sketch_for_problem_class(\n+    domain_filepath: Path,\n+    problems_directory: Path,\n+    workspace: Path,\n+    width: int,\n+    disable_closed_Q: bool = True,\n+    max_num_states_per_instance: int = 10000,\n+    max_time_per_instance: int = 10,\n+    encoding_type: EncodingType = EncodingType.EXPLICIT,\n+    max_num_rules: int = 1,\n+    enable_goal_separating_features: bool = True,\n+    disable_feature_generation: bool = True,\n+    enable_incomplete_feature_pruning: bool = False,\n+    concept_complexity_limit: int = 9,\n+    role_complexity_limit: int = 9,\n+    boolean_complexity_limit: int = 9,\n+    count_numerical_complexity_limit: int = 9,\n+    distance_numerical_complexity_limit: int = 9,\n+    feature_limit: int = 1000000,\n+    additional_booleans: List[str] = None,\n+    additional_numericals: List[str] = None,\n+    enable_dump_files: bool = False,\n+):\n+    # Initialize variables and setup\n+    if additional_booleans is None:\n+        additional_booleans = []\n+    if additional_numericals is None:\n+        additional_numericals = []\n+    instance_filepaths = list(problems_directory.iterdir())\n+   \n+\n+    add_console_handler(logging.getLogger(), logging.INFO)\n+    create_experiment_workspace(workspace)\n+    change_working_directory(workspace)\n+\n+    total_timer = Timer()\n+    preprocessing_timer = Timer()\n+    asp_timer = Timer(stopped=True)\n+    verification_timer = Timer(stopped=True)\n+    iteration_data = IterationData()\n+\n+    # Generate data\n+    with change_dir(\"input\"):\n+        logging.info(colored(\"Constructing InstanceDatas...\", \"blue\", \"on_grey\"))\n+        domain_data, instance_datas, num_ss_states, num_gfa_states = compute_instance_datas(domain_filepath, instance_filepaths, disable_closed_Q, max_num_states_per_instance, max_time_per_instance, enable_dump_files)\n+        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+        if instance_datas is None:\n+            raise Exception(\"Failed to create InstanceDatas.\")\n+\n+        state_finder = StateFinder(domain_data, instance_datas)\n+\n+        logging.info(colored(\"Initializing TupleGraphs...\", \"blue\", \"on_grey\"))\n+        gfa_state_id_to_tuple_graph: Dict[int, mm.TupleGraph] = compute_tuple_graphs(domain_data, instance_datas, state_finder, width, enable_dump_files)\n+        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+    preprocessing_data = PreprocessingData(domain_data, instance_datas, state_finder, gfa_state_id_to_tuple_graph)\n+    preprocessing_timer.stop()\n+\n+    if not preprocessing_data.instance_datas:\n+        raise RuntimeError(\"Data is empty\")\n+\n+    sketches = set()\n+    total_features = set()\n+    sketch_features = set()\n+    # Learn sketch\n+    if encoding_type == EncodingType.EXPLICIT:\n+        create_experiment_workspace(workspace)\n+        preprocessing_timer.resume()\n+\n+        for instance_data in preprocessing_data.instance_datas:\n+            print(f\"Instance {instance_data.idx}:\")\n+            for state in instance_data.mimir_ss.get_states():\n+                    state_data = state.get_state()\n+                    fluents = state_data.get_fluent_atoms()\n+                    #print(f\"State {state.get_index()} fluents: {fluents}\")\n+            # TODO: when is the best time to generate features?\n+            # Based on current instance currently looks most reasonable.\n+            # It could be restricted to the states in the tuple graph as well,\n+            # which reduces the number of rules, but not fully sure yet about the consequences in the bigger picture.\n+            iteration_data = IterationData()\n+            iteration_data.instance_datas = [instance_data]\n+            iteration_data.gfa_states = instance_data.gfa.get_states()\n+\n+            # Report some progress\n+            write_file(f\"{instance_data.idx}.dot\", instance_data.dlplan_ss.to_dot(1))\n+            print(\"     \", end=\"\")\n+            print(\"id:\", instance_data.idx,\n+                    \"problem_filepath:\", instance_data.mimir_ss.get_problem().get_filepath(),\n+                    \"num_states:\", instance_data.mimir_ss.get_num_states(),\n+                    \"num_state_equivalences:\", instance_data.gfa.get_num_states())\n+\n+            logging.info(colored(\"Initializing DomainFeatureData...\", \"blue\", \"on_grey\"))\n+            iteration_data.feature_pool = compute_feature_pool(\n+                preprocessing_data,\n+                iteration_data,\n+                gfa_state_id_to_tuple_graph,\n+                state_finder,\n+                disable_feature_generation,\n+                enable_incomplete_feature_pruning,\n+                concept_complexity_limit,\n+                role_complexity_limit,\n+                boolean_complexity_limit,\n+                count_numerical_complexity_limit,\n+                distance_numerical_complexity_limit,\n+                feature_limit,\n+                additional_booleans,\n+                additional_numericals\n+            )\n+            # Add features to features that were generated\n+            total_features.update(feature.dlplan_feature for feature in iteration_data.feature_pool)\n+\n+            logging.info(colored(\"Constructing PerStateFeatureValuations...\", \"blue\", \"on_grey\"))\n+            iteration_data.gfa_state_global_idx_to_feature_evaluations = compute_per_state_feature_valuations(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Constructing StatePairEquivalenceDatas...\", \"blue\", \"on_grey\"))\n+            iteration_data.state_pair_equivalences, iteration_data.gfa_state_global_idx_to_state_pair_equivalence = compute_state_pair_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Constructing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n+            iteration_data.gfa_state_global_idx_to_tuple_graph_equivalence = compute_tuple_graph_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Minimizing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n+            minimize_tuple_graph_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+            preprocessing_timer.stop()\n+            asp_timer.resume()\n+\n+            # We count the number of subgoal tuples for which no rule could be found.\n+            # This usually happens if the pool of features is not sufficiently rich.\n+            count_unsat_tuples = 0\n+            tuple_graph_states = set()\n+           \n+\n+            for gfa_state in instance_data.gfa.get_states():\n+                gfa_state_global_idx = gfa_state.get_global_index()\n+                gfa_state_idx = gfa_state.get_index()\n+\n+                if instance_data.gfa.is_deadend_state(gfa_state_idx):\n+                    continue\n+\n+                #print(f\"Keys in gfa_state_global_idx_to_tuple_graph: {list(preprocessing_data.gfa_state_global_idx_to_tuple_graph.keys())}\")\n+                #print(f\"gfa_state_global_idx: {gfa_state_global_idx}\")\n+\n+                tuple_graph = preprocessing_data.gfa_state_global_idx_to_tuple_graph[gfa_state_global_idx]\n+\n+                for distance, group in enumerate(tuple_graph.get_vertices_grouped_by_distance()):\n+                    if distance == 0:\n+                        # We skip subgoal tuples at distance zero because they do not encode progress towards a goal.\n+                        continue\n+                    for vertex in group:\n+                        # Here we find all simplest single sketch rules for a pair (state, subgoal tuple).\n+                        t_idx = vertex.get_index()\n+                        tuple_graph_states.add((gfa_state_global_idx, t_idx))\n+    \n+                        start_time = time.time()\n+                        asp_factory = ASPFactory(encoding_type, enable_goal_separating_features, max_num_rules)\n+                        facts = asp_factory.make_facts(preprocessing_data, iteration_data, gfa_state, t_idx)\n+\n+                        print(f\"Factory time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        start_time = time.time()\n+                        asp_factory.ground(facts)\n+                        print(f\"Grounding time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        start_time = time.time()\n+                        symbolss, returncode = asp_factory.solve_all_opt()\n+                        print(f\"Solving time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        if returncode in [ClingoExitCode.UNSATISFIABLE, ClingoExitCode.EXHAUSTED]:\n+                            print(colored(\"ASP is unsatisfiable or exhausted!\", \"red\", \"on_grey\"))\n+                            print(colored(f\"No sketch of width {width} exists that solves all instances!\", \"red\", \"on_grey\"))\n+                            # There might be tuples where we get unsat.\n+                            # If such cases occur, which is very likely in complex domains, then we must look closer into this.\n+                            count_unsat_tuples += 1\n+                            continue\n+                        elif returncode == ClingoExitCode.UNKNOWN:\n+                            print(colored(\"ASP solving throws unknown error!\", \"red\", \"on_grey\"))\n+                            exit(ExitCode.UNKNOWN)\n+                        elif returncode == ClingoExitCode.INTERRUPTED:\n+                            print(colored(\"ASP solving interrupted!\", \"red\", \"on_grey\"))\n+                            exit(ExitCode.INTERRUPTED)\n+                        else:\n+                            logging.info(f\"Solution found with return code: {returncode}\")\n+\n+                        for symbols in symbolss:\n+                            dlplan_policy = ExplicitDlplanPolicyFactory().make_dlplan_policy_from_answer_set(symbols, preprocessing_data, iteration_data)\n+\n+                            sketch = Sketch(dlplan_policy, width)\n+                            sketches.add(sketch)\n+                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_booleans())\n+                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_numericals())\n+                            print(dlplan_policy)\n+                            \n+                print(\"Tuple Graph States:\")\n+                for (s_i, s_j) in tuple_graph_states:\n+                    print(f\"State {s_i}, state {s_j}\")\n+                \n+                state_pair_equivalences = []\n+        \n+                for equivalence in iteration_data.state_pair_equivalences:\n+                    print(equivalence)\n+                    \n+                print(\"State Pair Equivalences:\")\n+                for (s_i, s_j) in state_pair_equivalences:\n+                    print(f\"Equivalent states: {s_i} and {s_j}\")\n+\n+                print(\"Comparison between Tuple Graph States and State Pair Equivalences:\")\n+                for (s_i, s_j) in tuple_graph_states:\n+                    if (s_i, s_j) in state_pair_equivalences:\n+                        print(f\"Transition ({s_i} -> {s_j}) matches with state pair equivalence.\")\n+                    else:\n+                        print(f\"Transition ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n+    else:\n+        raise Exception(\"No implementation for the given encoding type.\")\n+\n+    preprocessing_timer.stop()\n+    asp_timer.stop()\n+    verification_timer.stop()\n+    total_timer.stop()\n+\n+    # Compute feature histograms by complexity\n+    total_features_by_complexity = defaultdict(int)\n+    for feature in total_features:\n+        total_features_by_complexity[feature.compute_complexity()] += 1\n+    total_features_by_complexity = {key: total_features_by_complexity[key] for key in sorted(total_features_by_complexity)}\n+\n+    sketch_features_by_complexity = defaultdict(int)\n+    for feature in sketch_features:\n+        sketch_features_by_complexity[feature.compute_complexity()] += 1\n+    sketch_features_by_complexity = {key: sketch_features_by_complexity[key] for key in sorted(sketch_features_by_complexity)}\n+\n+    # Output the result\n+    with change_dir(\"output\"):\n+        print_separation_line()\n+        logging.info(colored(\"Summary:\", \"green\", \"on_grey\"))\n+\n+        print(f\"Preprocessing time: {int(preprocessing_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"ASP time: {int(asp_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Verification time: {int(verification_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Total time: {int(total_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Total memory: {int(memory_usage() / 1024)} GiB.\")\n+        print(f\"Total number of states: {num_ss_states}\")\n+        print(f\"Total number of abstract states: {num_gfa_states}\")\n+        print(f\"Number of unsat tuples: {count_unsat_tuples}\")\n+        print(f\"Number of sketch rules: {len(sketches)}\")\n+        print(f\"Number of total features: {len(total_features)}\")\n+        print(f\"Number of sketch features: {len(sketch_features)}\")\n+        print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n+        print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n"
                },
                {
                    "date": 1727822424055,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -223,11 +223,11 @@\n \n                 print(\"Comparison between Tuple Graph States and State Pair Equivalences:\")\n                 for (s_i, s_j) in tuple_graph_states:\n                     if (s_i, s_j) in state_pair_equivalences:\n-                        print(f\"Transition ({s_i} -> {s_j}) matches with state pair equivalence.\")\n+                        print(f\" ({s_i} -> {s_j}) matches with state pair equivalence.\")\n                     else:\n-                        print(f\"Transition ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n+                        print(f\" ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n     else:\n         raise Exception(\"No implementation for the given encoding type.\")\n \n     preprocessing_timer.stop()\n"
                },
                {
                    "date": 1727822508990,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -263,540 +263,4 @@\n         print(f\"Number of total features: {len(total_features)}\")\n         print(f\"Number of sketch features: {len(sketch_features)}\")\n         print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n         print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n-import logging\n-import time\n-from collections import defaultdict\n-from pathlib import Path\n-from termcolor import colored\n-from typing import List, MutableSet, Dict\n-import clingo\n-import pymimir as mm\n-from dlplan.policy import PolicyMinimizer, NamedBoolean, NamedNumerical\n-from collections import defaultdict\n-from .src.exit_codes import ExitCode\n-from .src.iteration import EncodingType, ASPFactory, ClingoExitCode, IterationData, LearningStatistics, Sketch, D2sepDlplanPolicyFactory, ExplicitDlplanPolicyFactory, compute_feature_pool, compute_per_state_feature_valuations, compute_state_pair_equivalences, compute_tuple_graph_equivalences, minimize_tuple_graph_equivalences\n-from .src.util import Timer, create_experiment_workspace, change_working_directory, write_file, change_dir, memory_usage, add_console_handler, print_separation_line\n-from .src.preprocessing import InstanceData, PreprocessingData, StateFinder, compute_instance_datas, compute_tuple_graphs\n-\n-def learn_sketch_for_problem_class(\n-    domain_filepath: Path,\n-    problems_directory: Path,\n-    workspace: Path,\n-    width: int,\n-    disable_closed_Q: bool = True,\n-    max_num_states_per_instance: int = 10000,\n-    max_time_per_instance: int = 10,\n-    encoding_type: EncodingType = EncodingType.EXPLICIT,\n-    max_num_rules: int = 1,\n-    enable_goal_separating_features: bool = True,\n-    disable_feature_generation: bool = True,\n-    enable_incomplete_feature_pruning: bool = False,\n-    concept_complexity_limit: int = 9,\n-    role_complexity_limit: int = 9,\n-    boolean_complexity_limit: int = 9,\n-    count_numerical_complexity_limit: int = 9,\n-    distance_numerical_complexity_limit: int = 9,\n-    feature_limit: int = 1000000,\n-    additional_booleans: List[str] = None,\n-    additional_numericals: List[str] = None,\n-    enable_dump_files: bool = False,\n-):\n-    # Initialize variables and setup\n-    if additional_booleans is None:\n-        additional_booleans = []\n-    if additional_numericals is None:\n-        additional_numericals = []\n-    instance_filepaths = list(problems_directory.iterdir())\n-   \n-\n-    add_console_handler(logging.getLogger(), logging.INFO)\n-    create_experiment_workspace(workspace)\n-    change_working_directory(workspace)\n-\n-    total_timer = Timer()\n-    preprocessing_timer = Timer()\n-    asp_timer = Timer(stopped=True)\n-    verification_timer = Timer(stopped=True)\n-    iteration_data = IterationData()\n-\n-    # Generate data\n-    with change_dir(\"input\"):\n-        logging.info(colored(\"Constructing InstanceDatas...\", \"blue\", \"on_grey\"))\n-        domain_data, instance_datas, num_ss_states, num_gfa_states = compute_instance_datas(domain_filepath, instance_filepaths, disable_closed_Q, max_num_states_per_instance, max_time_per_instance, enable_dump_files)\n-        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-        if instance_datas is None:\n-            raise Exception(\"Failed to create InstanceDatas.\")\n-\n-        state_finder = StateFinder(domain_data, instance_datas)\n-\n-        logging.info(colored(\"Initializing TupleGraphs...\", \"blue\", \"on_grey\"))\n-        gfa_state_id_to_tuple_graph: Dict[int, mm.TupleGraph] = compute_tuple_graphs(domain_data, instance_datas, state_finder, width, enable_dump_files)\n-        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-    preprocessing_data = PreprocessingData(domain_data, instance_datas, state_finder, gfa_state_id_to_tuple_graph)\n-    preprocessing_timer.stop()\n-\n-    if not preprocessing_data.instance_datas:\n-        raise RuntimeError(\"Data is empty\")\n-\n-    sketches = set()\n-    total_features = set()\n-    sketch_features = set()\n-    # Learn sketch\n-    if encoding_type == EncodingType.EXPLICIT:\n-        create_experiment_workspace(workspace)\n-        preprocessing_timer.resume()\n-\n-        for instance_data in preprocessing_data.instance_datas:\n-            print(f\"Instance {instance_data.idx}:\")\n-            for state in instance_data.mimir_ss.get_states():\n-                    state_data = state.get_state()\n-                    fluents = state_data.get_fluent_atoms()\n-                    #print(f\"State {state.get_index()} fluents: {fluents}\")\n-            # TODO: when is the best time to generate features?\n-            # Based on current instance currently looks most reasonable.\n-            # It could be restricted to the states in the tuple graph as well,\n-            # which reduces the number of rules, but not fully sure yet about the consequences in the bigger picture.\n-            iteration_data = IterationData()\n-            iteration_data.instance_datas = [instance_data]\n-            iteration_data.gfa_states = instance_data.gfa.get_states()\n-\n-            # Report some progress\n-            write_file(f\"{instance_data.idx}.dot\", instance_data.dlplan_ss.to_dot(1))\n-            print(\"     \", end=\"\")\n-            print(\"id:\", instance_data.idx,\n-                    \"problem_filepath:\", instance_data.mimir_ss.get_problem().get_filepath(),\n-                    \"num_states:\", instance_data.mimir_ss.get_num_states(),\n-                    \"num_state_equivalences:\", instance_data.gfa.get_num_states())\n-\n-            logging.info(colored(\"Initializing DomainFeatureData...\", \"blue\", \"on_grey\"))\n-            iteration_data.feature_pool = compute_feature_pool(\n-                preprocessing_data,\n-                iteration_data,\n-                gfa_state_id_to_tuple_graph,\n-                state_finder,\n-                disable_feature_generation,\n-                enable_incomplete_feature_pruning,\n-                concept_complexity_limit,\n-                role_complexity_limit,\n-                boolean_complexity_limit,\n-                count_numerical_complexity_limit,\n-                distance_numerical_complexity_limit,\n-                feature_limit,\n-                additional_booleans,\n-                additional_numericals\n-            )\n-            # Add features to features that were generated\n-            total_features.update(feature.dlplan_feature for feature in iteration_data.feature_pool)\n-\n-            logging.info(colored(\"Constructing PerStateFeatureValuations...\", \"blue\", \"on_grey\"))\n-            iteration_data.gfa_state_global_idx_to_feature_evaluations = compute_per_state_feature_valuations(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Constructing StatePairEquivalenceDatas...\", \"blue\", \"on_grey\"))\n-            iteration_data.state_pair_equivalences, iteration_data.gfa_state_global_idx_to_state_pair_equivalence = compute_state_pair_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Constructing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n-            iteration_data.gfa_state_global_idx_to_tuple_graph_equivalence = compute_tuple_graph_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Minimizing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n-            minimize_tuple_graph_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-            preprocessing_timer.stop()\n-            asp_timer.resume()\n-\n-            # We count the number of subgoal tuples for which no rule could be found.\n-            # This usually happens if the pool of features is not sufficiently rich.\n-            count_unsat_tuples = 0\n-            tuple_graph_states = set()\n-           \n-\n-            for gfa_state in instance_data.gfa.get_states():\n-                gfa_state_global_idx = gfa_state.get_global_index()\n-                gfa_state_idx = gfa_state.get_index()\n-\n-                if instance_data.gfa.is_deadend_state(gfa_state_idx):\n-                    continue\n-\n-                #print(f\"Keys in gfa_state_global_idx_to_tuple_graph: {list(preprocessing_data.gfa_state_global_idx_to_tuple_graph.keys())}\")\n-                #print(f\"gfa_state_global_idx: {gfa_state_global_idx}\")\n-\n-                tuple_graph = preprocessing_data.gfa_state_global_idx_to_tuple_graph[gfa_state_global_idx]\n-\n-                for distance, group in enumerate(tuple_graph.get_vertices_grouped_by_distance()):\n-                    if distance == 0:\n-                        # We skip subgoal tuples at distance zero because they do not encode progress towards a goal.\n-                        continue\n-                    for vertex in group:\n-                        # Here we find all simplest single sketch rules for a pair (state, subgoal tuple).\n-                        t_idx = vertex.get_index()\n-                        tuple_graph_states.add((gfa_state_global_idx, t_idx))\n-    \n-                        start_time = time.time()\n-                        asp_factory = ASPFactory(encoding_type, enable_goal_separating_features, max_num_rules)\n-                        facts = asp_factory.make_facts(preprocessing_data, iteration_data, gfa_state, t_idx)\n-\n-                        print(f\"Factory time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        start_time = time.time()\n-                        asp_factory.ground(facts)\n-                        print(f\"Grounding time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        start_time = time.time()\n-                        symbolss, returncode = asp_factory.solve_all_opt()\n-                        print(f\"Solving time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        if returncode in [ClingoExitCode.UNSATISFIABLE, ClingoExitCode.EXHAUSTED]:\n-                            print(colored(\"ASP is unsatisfiable or exhausted!\", \"red\", \"on_grey\"))\n-                            print(colored(f\"No sketch of width {width} exists that solves all instances!\", \"red\", \"on_grey\"))\n-                            # There might be tuples where we get unsat.\n-                            # If such cases occur, which is very likely in complex domains, then we must look closer into this.\n-                            count_unsat_tuples += 1\n-                            continue\n-                        elif returncode == ClingoExitCode.UNKNOWN:\n-                            print(colored(\"ASP solving throws unknown error!\", \"red\", \"on_grey\"))\n-                            exit(ExitCode.UNKNOWN)\n-                        elif returncode == ClingoExitCode.INTERRUPTED:\n-                            print(colored(\"ASP solving interrupted!\", \"red\", \"on_grey\"))\n-                            exit(ExitCode.INTERRUPTED)\n-                        else:\n-                            logging.info(f\"Solution found with return code: {returncode}\")\n-\n-                        for symbols in symbolss:\n-                            dlplan_policy = ExplicitDlplanPolicyFactory().make_dlplan_policy_from_answer_set(symbols, preprocessing_data, iteration_data)\n-\n-                            sketch = Sketch(dlplan_policy, width)\n-                            sketches.add(sketch)\n-                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_booleans())\n-                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_numericals())\n-                            print(dlplan_policy)\n-                            \n-                print(\"Tuple Graph States:\")\n-                for (s_i, s_j) in tuple_graph_states:\n-                    print(f\"State {s_i}, state {s_j}\")\n-                \n-                state_pair_equivalences = []\n-        \n-                for equivalence in iteration_data.state_pair_equivalences:\n-                    source_state = equivalence.get_source_state()\n-                    target_state = equivalence.get_target_state() \n-                    state_pair_equivalences.append((source_state, target_state))\n-                    \n-                print(\"State Pair Equivalences:\")\n-                for (s_i, s_j) in state_pair_equivalences:\n-                    print(f\"Equivalent states: {s_i} and {s_j}\")\n-\n-                print(\"Comparison between Tuple Graph States and State Pair Equivalences:\")\n-                for (s_i, s_j) in tuple_graph_states:\n-                    if (s_i, s_j) in state_pair_equivalences:\n-                        print(f\"Transition ({s_i} -> {s_j}) matches with state pair equivalence.\")\n-                    else:\n-                        print(f\"Transition ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n-    else:\n-        raise Exception(\"No implementation for the given encoding type.\")\n-\n-    preprocessing_timer.stop()\n-    asp_timer.stop()\n-    verification_timer.stop()\n-    total_timer.stop()\n-\n-    # Compute feature histograms by complexity\n-    total_features_by_complexity = defaultdict(int)\n-    for feature in total_features:\n-        total_features_by_complexity[feature.compute_complexity()] += 1\n-    total_features_by_complexity = {key: total_features_by_complexity[key] for key in sorted(total_features_by_complexity)}\n-\n-    sketch_features_by_complexity = defaultdict(int)\n-    for feature in sketch_features:\n-        sketch_features_by_complexity[feature.compute_complexity()] += 1\n-    sketch_features_by_complexity = {key: sketch_features_by_complexity[key] for key in sorted(sketch_features_by_complexity)}\n-\n-    # Output the result\n-    with change_dir(\"output\"):\n-        print_separation_line()\n-        logging.info(colored(\"Summary:\", \"green\", \"on_grey\"))\n-\n-        print(f\"Preprocessing time: {int(preprocessing_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"ASP time: {int(asp_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Verification time: {int(verification_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Total time: {int(total_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Total memory: {int(memory_usage() / 1024)} GiB.\")\n-        print(f\"Total number of states: {num_ss_states}\")\n-        print(f\"Total number of abstract states: {num_gfa_states}\")\n-        print(f\"Number of unsat tuples: {count_unsat_tuples}\")\n-        print(f\"Number of sketch rules: {len(sketches)}\")\n-        print(f\"Number of total features: {len(total_features)}\")\n-        print(f\"Number of sketch features: {len(sketch_features)}\")\n-        print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n-        print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n-import logging\n-import time\n-from collections import defaultdict\n-from pathlib import Path\n-from termcolor import colored\n-from typing import List, MutableSet, Dict\n-import clingo\n-import pymimir as mm\n-from dlplan.policy import PolicyMinimizer, NamedBoolean, NamedNumerical\n-from collections import defaultdict\n-from .src.exit_codes import ExitCode\n-from .src.iteration import EncodingType, ASPFactory, ClingoExitCode, IterationData, LearningStatistics, Sketch, D2sepDlplanPolicyFactory, ExplicitDlplanPolicyFactory, compute_feature_pool, compute_per_state_feature_valuations, compute_state_pair_equivalences, compute_tuple_graph_equivalences, minimize_tuple_graph_equivalences\n-from .src.util import Timer, create_experiment_workspace, change_working_directory, write_file, change_dir, memory_usage, add_console_handler, print_separation_line\n-from .src.preprocessing import InstanceData, PreprocessingData, StateFinder, compute_instance_datas, compute_tuple_graphs\n-\n-def learn_sketch_for_problem_class(\n-    domain_filepath: Path,\n-    problems_directory: Path,\n-    workspace: Path,\n-    width: int,\n-    disable_closed_Q: bool = True,\n-    max_num_states_per_instance: int = 10000,\n-    max_time_per_instance: int = 10,\n-    encoding_type: EncodingType = EncodingType.EXPLICIT,\n-    max_num_rules: int = 1,\n-    enable_goal_separating_features: bool = True,\n-    disable_feature_generation: bool = True,\n-    enable_incomplete_feature_pruning: bool = False,\n-    concept_complexity_limit: int = 9,\n-    role_complexity_limit: int = 9,\n-    boolean_complexity_limit: int = 9,\n-    count_numerical_complexity_limit: int = 9,\n-    distance_numerical_complexity_limit: int = 9,\n-    feature_limit: int = 1000000,\n-    additional_booleans: List[str] = None,\n-    additional_numericals: List[str] = None,\n-    enable_dump_files: bool = False,\n-):\n-    # Initialize variables and setup\n-    if additional_booleans is None:\n-        additional_booleans = []\n-    if additional_numericals is None:\n-        additional_numericals = []\n-    instance_filepaths = list(problems_directory.iterdir())\n-   \n-\n-    add_console_handler(logging.getLogger(), logging.INFO)\n-    create_experiment_workspace(workspace)\n-    change_working_directory(workspace)\n-\n-    total_timer = Timer()\n-    preprocessing_timer = Timer()\n-    asp_timer = Timer(stopped=True)\n-    verification_timer = Timer(stopped=True)\n-    iteration_data = IterationData()\n-\n-    # Generate data\n-    with change_dir(\"input\"):\n-        logging.info(colored(\"Constructing InstanceDatas...\", \"blue\", \"on_grey\"))\n-        domain_data, instance_datas, num_ss_states, num_gfa_states = compute_instance_datas(domain_filepath, instance_filepaths, disable_closed_Q, max_num_states_per_instance, max_time_per_instance, enable_dump_files)\n-        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-        if instance_datas is None:\n-            raise Exception(\"Failed to create InstanceDatas.\")\n-\n-        state_finder = StateFinder(domain_data, instance_datas)\n-\n-        logging.info(colored(\"Initializing TupleGraphs...\", \"blue\", \"on_grey\"))\n-        gfa_state_id_to_tuple_graph: Dict[int, mm.TupleGraph] = compute_tuple_graphs(domain_data, instance_datas, state_finder, width, enable_dump_files)\n-        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-    preprocessing_data = PreprocessingData(domain_data, instance_datas, state_finder, gfa_state_id_to_tuple_graph)\n-    preprocessing_timer.stop()\n-\n-    if not preprocessing_data.instance_datas:\n-        raise RuntimeError(\"Data is empty\")\n-\n-    sketches = set()\n-    total_features = set()\n-    sketch_features = set()\n-    # Learn sketch\n-    if encoding_type == EncodingType.EXPLICIT:\n-        create_experiment_workspace(workspace)\n-        preprocessing_timer.resume()\n-\n-        for instance_data in preprocessing_data.instance_datas:\n-            print(f\"Instance {instance_data.idx}:\")\n-            for state in instance_data.mimir_ss.get_states():\n-                    state_data = state.get_state()\n-                    fluents = state_data.get_fluent_atoms()\n-                    #print(f\"State {state.get_index()} fluents: {fluents}\")\n-            # TODO: when is the best time to generate features?\n-            # Based on current instance currently looks most reasonable.\n-            # It could be restricted to the states in the tuple graph as well,\n-            # which reduces the number of rules, but not fully sure yet about the consequences in the bigger picture.\n-            iteration_data = IterationData()\n-            iteration_data.instance_datas = [instance_data]\n-            iteration_data.gfa_states = instance_data.gfa.get_states()\n-\n-            # Report some progress\n-            write_file(f\"{instance_data.idx}.dot\", instance_data.dlplan_ss.to_dot(1))\n-            print(\"     \", end=\"\")\n-            print(\"id:\", instance_data.idx,\n-                    \"problem_filepath:\", instance_data.mimir_ss.get_problem().get_filepath(),\n-                    \"num_states:\", instance_data.mimir_ss.get_num_states(),\n-                    \"num_state_equivalences:\", instance_data.gfa.get_num_states())\n-\n-            logging.info(colored(\"Initializing DomainFeatureData...\", \"blue\", \"on_grey\"))\n-            iteration_data.feature_pool = compute_feature_pool(\n-                preprocessing_data,\n-                iteration_data,\n-                gfa_state_id_to_tuple_graph,\n-                state_finder,\n-                disable_feature_generation,\n-                enable_incomplete_feature_pruning,\n-                concept_complexity_limit,\n-                role_complexity_limit,\n-                boolean_complexity_limit,\n-                count_numerical_complexity_limit,\n-                distance_numerical_complexity_limit,\n-                feature_limit,\n-                additional_booleans,\n-                additional_numericals\n-            )\n-            # Add features to features that were generated\n-            total_features.update(feature.dlplan_feature for feature in iteration_data.feature_pool)\n-\n-            logging.info(colored(\"Constructing PerStateFeatureValuations...\", \"blue\", \"on_grey\"))\n-            iteration_data.gfa_state_global_idx_to_feature_evaluations = compute_per_state_feature_valuations(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Constructing StatePairEquivalenceDatas...\", \"blue\", \"on_grey\"))\n-            iteration_data.state_pair_equivalences, iteration_data.gfa_state_global_idx_to_state_pair_equivalence = compute_state_pair_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Constructing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n-            iteration_data.gfa_state_global_idx_to_tuple_graph_equivalence = compute_tuple_graph_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Minimizing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n-            minimize_tuple_graph_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-            preprocessing_timer.stop()\n-            asp_timer.resume()\n-\n-            # We count the number of subgoal tuples for which no rule could be found.\n-            # This usually happens if the pool of features is not sufficiently rich.\n-            count_unsat_tuples = 0\n-            tuple_graph_states = set()\n-           \n-\n-            for gfa_state in instance_data.gfa.get_states():\n-                gfa_state_global_idx = gfa_state.get_global_index()\n-                gfa_state_idx = gfa_state.get_index()\n-\n-                if instance_data.gfa.is_deadend_state(gfa_state_idx):\n-                    continue\n-\n-                #print(f\"Keys in gfa_state_global_idx_to_tuple_graph: {list(preprocessing_data.gfa_state_global_idx_to_tuple_graph.keys())}\")\n-                #print(f\"gfa_state_global_idx: {gfa_state_global_idx}\")\n-\n-                tuple_graph = preprocessing_data.gfa_state_global_idx_to_tuple_graph[gfa_state_global_idx]\n-\n-                for distance, group in enumerate(tuple_graph.get_vertices_grouped_by_distance()):\n-                    if distance == 0:\n-                        # We skip subgoal tuples at distance zero because they do not encode progress towards a goal.\n-                        continue\n-                    for vertex in group:\n-                        # Here we find all simplest single sketch rules for a pair (state, subgoal tuple).\n-                        t_idx = vertex.get_index()\n-                        tuple_graph_states.add((gfa_state_global_idx, t_idx))\n-    \n-                        start_time = time.time()\n-                        asp_factory = ASPFactory(encoding_type, enable_goal_separating_features, max_num_rules)\n-                        facts = asp_factory.make_facts(preprocessing_data, iteration_data, gfa_state, t_idx)\n-\n-                        print(f\"Factory time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        start_time = time.time()\n-                        asp_factory.ground(facts)\n-                        print(f\"Grounding time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        start_time = time.time()\n-                        symbolss, returncode = asp_factory.solve_all_opt()\n-                        print(f\"Solving time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        if returncode in [ClingoExitCode.UNSATISFIABLE, ClingoExitCode.EXHAUSTED]:\n-                            print(colored(\"ASP is unsatisfiable or exhausted!\", \"red\", \"on_grey\"))\n-                            print(colored(f\"No sketch of width {width} exists that solves all instances!\", \"red\", \"on_grey\"))\n-                            # There might be tuples where we get unsat.\n-                            # If such cases occur, which is very likely in complex domains, then we must look closer into this.\n-                            count_unsat_tuples += 1\n-                            continue\n-                        elif returncode == ClingoExitCode.UNKNOWN:\n-                            print(colored(\"ASP solving throws unknown error!\", \"red\", \"on_grey\"))\n-                            exit(ExitCode.UNKNOWN)\n-                        elif returncode == ClingoExitCode.INTERRUPTED:\n-                            print(colored(\"ASP solving interrupted!\", \"red\", \"on_grey\"))\n-                            exit(ExitCode.INTERRUPTED)\n-                        else:\n-                            logging.info(f\"Solution found with return code: {returncode}\")\n-\n-                        for symbols in symbolss:\n-                            dlplan_policy = ExplicitDlplanPolicyFactory().make_dlplan_policy_from_answer_set(symbols, preprocessing_data, iteration_data)\n-\n-                            sketch = Sketch(dlplan_policy, width)\n-                            sketches.add(sketch)\n-                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_booleans())\n-                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_numericals())\n-                            print(dlplan_policy)\n-                            \n-                print(\"Tuple Graph States:\")\n-                for (s_i, s_j) in tuple_graph_states:\n-                    print(f\"State {s_i}, state {s_j}\")\n-                \n-                state_pair_equivalences = []\n-        \n-                for equivalence in iteration_data.state_pair_equivalences:\n-                    source_state = equivalence.source_state\n-                    target_state = equivalence.target_state \n-                    state_pair_equivalences.append((source_state, target_state))\n-                    \n-                print(\"State Pair Equivalences:\")\n-                for (s_i, s_j) in state_pair_equivalences:\n-                    print(f\"Equivalent states: {s_i} and {s_j}\")\n-\n-                print(\"Comparison between Tuple Graph States and State Pair Equivalences:\")\n-                for (s_i, s_j) in tuple_graph_states:\n-                    if (s_i, s_j) in state_pair_equivalences:\n-                        print(f\"Transition ({s_i} -> {s_j}) matches with state pair equivalence.\")\n-                    else:\n-                        print(f\"Transition ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n-    else:\n-        raise Exception(\"No implementation for the given encoding type.\")\n-\n-    preprocessing_timer.stop()\n-    asp_timer.stop()\n-    verification_timer.stop()\n-    total_timer.stop()\n-\n-    # Compute feature histograms by complexity\n-    total_features_by_complexity = defaultdict(int)\n-    for feature in total_features:\n-        total_features_by_complexity[feature.compute_complexity()] += 1\n-    total_features_by_complexity = {key: total_features_by_complexity[key] for key in sorted(total_features_by_complexity)}\n-\n-    sketch_features_by_complexity = defaultdict(int)\n-    for feature in sketch_features:\n-        sketch_features_by_complexity[feature.compute_complexity()] += 1\n-    sketch_features_by_complexity = {key: sketch_features_by_complexity[key] for key in sorted(sketch_features_by_complexity)}\n-\n-    # Output the result\n-    with change_dir(\"output\"):\n-        print_separation_line()\n-        logging.info(colored(\"Summary:\", \"green\", \"on_grey\"))\n-\n-        print(f\"Preprocessing time: {int(preprocessing_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"ASP time: {int(asp_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Verification time: {int(verification_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Total time: {int(total_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Total memory: {int(memory_usage() / 1024)} GiB.\")\n-        print(f\"Total number of states: {num_ss_states}\")\n-        print(f\"Total number of abstract states: {num_gfa_states}\")\n-        print(f\"Number of unsat tuples: {count_unsat_tuples}\")\n-        print(f\"Number of sketch rules: {len(sketches)}\")\n-        print(f\"Number of total features: {len(total_features)}\")\n-        print(f\"Number of sketch features: {len(sketch_features)}\")\n-        print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n-        print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n"
                },
                {
                    "date": 1727822633497,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -213,10 +213,10 @@\n                     print(f\"State {s_i}, state {s_j}\")\n                 \n                 state_pair_equivalences = []\n         \n-                for equivalence in iteration_data.state_pair_equivalences:\n-                    print(equivalence)\n+                #for equivalence in iteration_data.state_pair_equivalences:\n+                #    print(equivalence)\n                     \n                 print(\"State Pair Equivalences:\")\n                 for (s_i, s_j) in state_pair_equivalences:\n                     print(f\"Equivalent states: {s_i} and {s_j}\")\n"
                },
                {
                    "date": 1727822774848,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -211,10 +211,13 @@\n                 print(\"Tuple Graph States:\")\n                 for (s_i, s_j) in tuple_graph_states:\n                     print(f\"State {s_i}, state {s_j}\")\n                 \n-                state_pair_equivalences = []\n-        \n+                r_idx_to_distance = dict()\n+                r_idx_to_subgoal_gfa_state_global_idxs = defaultdict(set)\n+                subgoal_gfa_state_global_idx_to_r_idx = dict()\n+                inverse_pairs = defaultdict(set)\n+                    \n                 #for equivalence in iteration_data.state_pair_equivalences:\n                 #    print(equivalence)\n                     \n                 print(\"State Pair Equivalences:\")\n"
                },
                {
                    "date": 1727823050516,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -211,25 +211,20 @@\n                 print(\"Tuple Graph States:\")\n                 for (s_i, s_j) in tuple_graph_states:\n                     print(f\"State {s_i}, state {s_j}\")\n                 \n-                r_idx_to_distance = dict()\n-                r_idx_to_subgoal_gfa_state_global_idxs = defaultdict(set)\n-                subgoal_gfa_state_global_idx_to_r_idx = dict()\n-                inverse_pairs = defaultdict(set)\n-                    \n-                #for equivalence in iteration_data.state_pair_equivalences:\n-                #    print(equivalence)\n-                    \n-                print(\"State Pair Equivalences:\")\n-                for (s_i, s_j) in state_pair_equivalences:\n-                    print(f\"Equivalent states: {s_i} and {s_j}\")\n-\n+                state_pair_equivalences = iteration_data.state_pair_equivalences\n                 print(\"Comparison between Tuple Graph States and State Pair Equivalences:\")\n                 for (s_i, s_j) in tuple_graph_states:\n-                    if (s_i, s_j) in state_pair_equivalences:\n-                        print(f\" ({s_i} -> {s_j}) matches with state pair equivalence.\")\n-                    else:\n+                    match_found = False\n+                    for equivalence in state_pair_equivalences:\n+                        # Assuming equivalence is a tuple (s_i, s_j) or similar\n+                        if equivalence.source_state == s_i and equivalence.target_state == s_j:\n+                            print(f\" ({s_i} -> {s_j}) matches with state pair equivalence.\")\n+                            match_found = True\n+                            break\n+                    \n+                    if not match_found:\n                         print(f\" ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n     else:\n         raise Exception(\"No implementation for the given encoding type.\")\n \n"
                },
                {
                    "date": 1727823062250,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -211,20 +211,28 @@\n                 print(\"Tuple Graph States:\")\n                 for (s_i, s_j) in tuple_graph_states:\n                     print(f\"State {s_i}, state {s_j}\")\n                 \n-                state_pair_equivalences = iteration_data.state_pair_equivalences\n+                r_idx_to_distance = dict()\n+                r_idx_to_subgoal_gfa_state_global_idxs = defaultdict(set)\n+                subgoal_gfa_state_global_idx_to_r_idx = dict()\n+                inverse_pairs = defaultdict(set)\n+                    \n+                for gfa_state_global_idx, state_pair_equivalence in gfa_state_global_idx_to_state_pair_equivalence.items():\n+                    print(f\"State: {gfa_state_global_idx}\")\n+                    \n+                    for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n+                        print(f\"  Rule Index: {r_idx} -> Subgoal States: {subgoal_gfa_state_global_idxs}\")\n+                        \n+                        for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n+                            print(f\"    State Pair: ({gfa_state_global_idx}, {subgoal_gfa_state_global_idx})\")\n+                            inverse_pairs[(subgoal_gfa_state_global_idx, gfa_state_global_idx)].add((subgoal_gfa_state_global_idx, gfa_state_global_idx))\n+\n                 print(\"Comparison between Tuple Graph States and State Pair Equivalences:\")\n                 for (s_i, s_j) in tuple_graph_states:\n-                    match_found = False\n-                    for equivalence in state_pair_equivalences:\n-                        # Assuming equivalence is a tuple (s_i, s_j) or similar\n-                        if equivalence.source_state == s_i and equivalence.target_state == s_j:\n-                            print(f\" ({s_i} -> {s_j}) matches with state pair equivalence.\")\n-                            match_found = True\n-                            break\n-                    \n-                    if not match_found:\n+                    if (s_i, s_j) in state_pair_equivalences:\n+                        print(f\" ({s_i} -> {s_j}) matches with state pair equivalence.\")\n+                    else:\n                         print(f\" ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n     else:\n         raise Exception(\"No implementation for the given encoding type.\")\n \n"
                },
                {
                    "date": 1727823160025,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -207,33 +207,24 @@\n                             sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_booleans())\n                             sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_numericals())\n                             print(dlplan_policy)\n                             \n-                print(\"Tuple Graph States:\")\n+                print(\"Comparison between Tuple Graph States and State Pair Equivalences:\")\n                 for (s_i, s_j) in tuple_graph_states:\n-                    print(f\"State {s_i}, state {s_j}\")\n-                \n-                r_idx_to_distance = dict()\n-                r_idx_to_subgoal_gfa_state_global_idxs = defaultdict(set)\n-                subgoal_gfa_state_global_idx_to_r_idx = dict()\n-                inverse_pairs = defaultdict(set)\n-                    \n-                for gfa_state_global_idx, state_pair_equivalence in gfa_state_global_idx_to_state_pair_equivalence.items():\n-                    print(f\"State: {gfa_state_global_idx}\")\n-                    \n-                    for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n-                        print(f\"  Rule Index: {r_idx} -> Subgoal States: {subgoal_gfa_state_global_idxs}\")\n-                        \n-                        for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n-                            print(f\"    State Pair: ({gfa_state_global_idx}, {subgoal_gfa_state_global_idx})\")\n-                            inverse_pairs[(subgoal_gfa_state_global_idx, gfa_state_global_idx)].add((subgoal_gfa_state_global_idx, gfa_state_global_idx))\n+                    match_found = False\n+                    for gfa_state_global_idx, state_pair_equivalence in gfa_state_global_idx_to_state_pair_equivalence.items():\n+                        for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n+                            for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n+                                if gfa_state_global_idx == s_i and subgoal_gfa_state_global_idx == s_j:\n+                                    print(f\" ({s_i} -> {s_j}) matches with state pair equivalence.\")\n+                                    match_found = True\n+                                    break\n+                        if match_found:\n+                            break\n \n-                print(\"Comparison between Tuple Graph States and State Pair Equivalences:\")\n-                for (s_i, s_j) in tuple_graph_states:\n-                    if (s_i, s_j) in state_pair_equivalences:\n-                        print(f\" ({s_i} -> {s_j}) matches with state pair equivalence.\")\n-                    else:\n+                    if not match_found:\n                         print(f\" ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n+\n     else:\n         raise Exception(\"No implementation for the given encoding type.\")\n \n     preprocessing_timer.stop()\n"
                },
                {
                    "date": 1727823194733,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -207,24 +207,9 @@\n                             sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_booleans())\n                             sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_numericals())\n                             print(dlplan_policy)\n                             \n-                print(\"Comparison between Tuple Graph States and State Pair Equivalences:\")\n-                for (s_i, s_j) in tuple_graph_states:\n-                    match_found = False\n-                    for gfa_state_global_idx, state_pair_equivalence in gfa_state_global_idx_to_state_pair_equivalence.items():\n-                        for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n-                            for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n-                                if gfa_state_global_idx == s_i and subgoal_gfa_state_global_idx == s_j:\n-                                    print(f\" ({s_i} -> {s_j}) matches with state pair equivalence.\")\n-                                    match_found = True\n-                                    break\n-                        if match_found:\n-                            break\n \n-                    if not match_found:\n-                        print(f\" ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n-\n     else:\n         raise Exception(\"No implementation for the given encoding type.\")\n \n     preprocessing_timer.stop()\n"
                },
                {
                    "date": 1727823257441,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,263 @@\n+import logging\n+import time\n+from collections import defaultdict\n+from pathlib import Path\n+from termcolor import colored\n+from typing import List, MutableSet, Dict\n+import clingo\n+import pymimir as mm\n+from dlplan.policy import PolicyMinimizer, NamedBoolean, NamedNumerical\n+from collections import defaultdict\n+from .src.exit_codes import ExitCode\n+from .src.iteration import EncodingType, ASPFactory, ClingoExitCode, IterationData, LearningStatistics, Sketch, D2sepDlplanPolicyFactory, ExplicitDlplanPolicyFactory, compute_feature_pool, compute_per_state_feature_valuations, compute_state_pair_equivalences, compute_tuple_graph_equivalences, minimize_tuple_graph_equivalences\n+from .src.util import Timer, create_experiment_workspace, change_working_directory, write_file, change_dir, memory_usage, add_console_handler, print_separation_line\n+from .src.preprocessing import InstanceData, PreprocessingData, StateFinder, compute_instance_datas, compute_tuple_graphs\n+\n+def learn_sketch_for_problem_class(\n+    domain_filepath: Path,\n+    problems_directory: Path,\n+    workspace: Path,\n+    width: int,\n+    disable_closed_Q: bool = True,\n+    max_num_states_per_instance: int = 10000,\n+    max_time_per_instance: int = 10,\n+    encoding_type: EncodingType = EncodingType.EXPLICIT,\n+    max_num_rules: int = 1,\n+    enable_goal_separating_features: bool = True,\n+    disable_feature_generation: bool = True,\n+    enable_incomplete_feature_pruning: bool = False,\n+    concept_complexity_limit: int = 9,\n+    role_complexity_limit: int = 9,\n+    boolean_complexity_limit: int = 9,\n+    count_numerical_complexity_limit: int = 9,\n+    distance_numerical_complexity_limit: int = 9,\n+    feature_limit: int = 1000000,\n+    additional_booleans: List[str] = None,\n+    additional_numericals: List[str] = None,\n+    enable_dump_files: bool = False,\n+):\n+    # Initialize variables and setup\n+    if additional_booleans is None:\n+        additional_booleans = []\n+    if additional_numericals is None:\n+        additional_numericals = []\n+    instance_filepaths = list(problems_directory.iterdir())\n+   \n+\n+    add_console_handler(logging.getLogger(), logging.INFO)\n+    create_experiment_workspace(workspace)\n+    change_working_directory(workspace)\n+\n+    total_timer = Timer()\n+    preprocessing_timer = Timer()\n+    asp_timer = Timer(stopped=True)\n+    verification_timer = Timer(stopped=True)\n+    iteration_data = IterationData()\n+\n+    # Generate data\n+    with change_dir(\"input\"):\n+        logging.info(colored(\"Constructing InstanceDatas...\", \"blue\", \"on_grey\"))\n+        domain_data, instance_datas, num_ss_states, num_gfa_states = compute_instance_datas(domain_filepath, instance_filepaths, disable_closed_Q, max_num_states_per_instance, max_time_per_instance, enable_dump_files)\n+        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+        if instance_datas is None:\n+            raise Exception(\"Failed to create InstanceDatas.\")\n+\n+        state_finder = StateFinder(domain_data, instance_datas)\n+\n+        logging.info(colored(\"Initializing TupleGraphs...\", \"blue\", \"on_grey\"))\n+        gfa_state_id_to_tuple_graph: Dict[int, mm.TupleGraph] = compute_tuple_graphs(domain_data, instance_datas, state_finder, width, enable_dump_files)\n+        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+    preprocessing_data = PreprocessingData(domain_data, instance_datas, state_finder, gfa_state_id_to_tuple_graph)\n+    preprocessing_timer.stop()\n+\n+    if not preprocessing_data.instance_datas:\n+        raise RuntimeError(\"Data is empty\")\n+\n+    sketches = set()\n+    total_features = set()\n+    sketch_features = set()\n+    # Learn sketch\n+    if encoding_type == EncodingType.EXPLICIT:\n+        create_experiment_workspace(workspace)\n+        preprocessing_timer.resume()\n+\n+        for instance_data in preprocessing_data.instance_datas:\n+            print(f\"Instance {instance_data.idx}:\")\n+            for state in instance_data.mimir_ss.get_states():\n+                    state_data = state.get_state()\n+                    fluents = state_data.get_fluent_atoms()\n+                    #print(f\"State {state.get_index()} fluents: {fluents}\")\n+            # TODO: when is the best time to generate features?\n+            # Based on current instance currently looks most reasonable.\n+            # It could be restricted to the states in the tuple graph as well,\n+            # which reduces the number of rules, but not fully sure yet about the consequences in the bigger picture.\n+            iteration_data = IterationData()\n+            iteration_data.instance_datas = [instance_data]\n+            iteration_data.gfa_states = instance_data.gfa.get_states()\n+\n+            # Report some progress\n+            write_file(f\"{instance_data.idx}.dot\", instance_data.dlplan_ss.to_dot(1))\n+            print(\"     \", end=\"\")\n+            print(\"id:\", instance_data.idx,\n+                    \"problem_filepath:\", instance_data.mimir_ss.get_problem().get_filepath(),\n+                    \"num_states:\", instance_data.mimir_ss.get_num_states(),\n+                    \"num_state_equivalences:\", instance_data.gfa.get_num_states())\n+\n+            logging.info(colored(\"Initializing DomainFeatureData...\", \"blue\", \"on_grey\"))\n+            iteration_data.feature_pool = compute_feature_pool(\n+                preprocessing_data,\n+                iteration_data,\n+                gfa_state_id_to_tuple_graph,\n+                state_finder,\n+                disable_feature_generation,\n+                enable_incomplete_feature_pruning,\n+                concept_complexity_limit,\n+                role_complexity_limit,\n+                boolean_complexity_limit,\n+                count_numerical_complexity_limit,\n+                distance_numerical_complexity_limit,\n+                feature_limit,\n+                additional_booleans,\n+                additional_numericals\n+            )\n+            # Add features to features that were generated\n+            total_features.update(feature.dlplan_feature for feature in iteration_data.feature_pool)\n+\n+            logging.info(colored(\"Constructing PerStateFeatureValuations...\", \"blue\", \"on_grey\"))\n+            iteration_data.gfa_state_global_idx_to_feature_evaluations = compute_per_state_feature_valuations(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Constructing StatePairEquivalenceDatas...\", \"blue\", \"on_grey\"))\n+            iteration_data.state_pair_equivalences, iteration_data.gfa_state_global_idx_to_state_pair_equivalence = compute_state_pair_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Constructing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n+            iteration_data.gfa_state_global_idx_to_tuple_graph_equivalence = compute_tuple_graph_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Minimizing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n+            minimize_tuple_graph_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+            preprocessing_timer.stop()\n+            asp_timer.resume()\n+\n+            # We count the number of subgoal tuples for which no rule could be found.\n+            # This usually happens if the pool of features is not sufficiently rich.\n+            count_unsat_tuples = 0\n+            tuple_graph_states = set()\n+           \n+\n+            for gfa_state in instance_data.gfa.get_states():\n+                gfa_state_global_idx = gfa_state.get_global_index()\n+                gfa_state_idx = gfa_state.get_index()\n+\n+                if instance_data.gfa.is_deadend_state(gfa_state_idx):\n+                    continue\n+\n+                #print(f\"Keys in gfa_state_global_idx_to_tuple_graph: {list(preprocessing_data.gfa_state_global_idx_to_tuple_graph.keys())}\")\n+                #print(f\"gfa_state_global_idx: {gfa_state_global_idx}\")\n+\n+                tuple_graph = preprocessing_data.gfa_state_global_idx_to_tuple_graph[gfa_state_global_idx]\n+\n+                for distance, group in enumerate(tuple_graph.get_vertices_grouped_by_distance()):\n+                    if distance == 0:\n+                        # We skip subgoal tuples at distance zero because they do not encode progress towards a goal.\n+                        continue\n+                    for vertex in group:\n+                        # Here we find all simplest single sketch rules for a pair (state, subgoal tuple).\n+                        t_idx = vertex.get_index()\n+                        tuple_graph_states.add((gfa_state_global_idx, t_idx))\n+    \n+                        start_time = time.time()\n+                        asp_factory = ASPFactory(encoding_type, enable_goal_separating_features, max_num_rules)\n+                        facts = asp_factory.make_facts(preprocessing_data, iteration_data, gfa_state, t_idx)\n+\n+                        print(f\"Factory time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        start_time = time.time()\n+                        asp_factory.ground(facts)\n+                        print(f\"Grounding time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        start_time = time.time()\n+                        symbolss, returncode = asp_factory.solve_all_opt()\n+                        print(f\"Solving time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        if returncode in [ClingoExitCode.UNSATISFIABLE, ClingoExitCode.EXHAUSTED]:\n+                            print(colored(\"ASP is unsatisfiable or exhausted!\", \"red\", \"on_grey\"))\n+                            print(colored(f\"No sketch of width {width} exists that solves all instances!\", \"red\", \"on_grey\"))\n+                            # There might be tuples where we get unsat.\n+                            # If such cases occur, which is very likely in complex domains, then we must look closer into this.\n+                            count_unsat_tuples += 1\n+                            continue\n+                        elif returncode == ClingoExitCode.UNKNOWN:\n+                            print(colored(\"ASP solving throws unknown error!\", \"red\", \"on_grey\"))\n+                            exit(ExitCode.UNKNOWN)\n+                        elif returncode == ClingoExitCode.INTERRUPTED:\n+                            print(colored(\"ASP solving interrupted!\", \"red\", \"on_grey\"))\n+                            exit(ExitCode.INTERRUPTED)\n+                        else:\n+                            logging.info(f\"Solution found with return code: {returncode}\")\n+\n+                        for symbols in symbolss:\n+                            dlplan_policy = ExplicitDlplanPolicyFactory().make_dlplan_policy_from_answer_set(symbols, preprocessing_data, iteration_data)\n+\n+                            sketch = Sketch(dlplan_policy, width)\n+                            sketches.add(sketch)\n+                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_booleans())\n+                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_numericals())\n+                            print(dlplan_policy)\n+                            \n+\n+    else:\n+        raise Exception(\"No implementation for the given encoding type.\")\n+\n+    preprocessing_timer.stop()\n+    asp_timer.stop()\n+    verification_timer.stop()\n+    total_timer.stop()\n+\n+    print(\"Comparison between Tuple Graph States and State Pair Equivalences:\")\n+    for (s_i, s_j) in tuple_graph_states:\n+        match_found = False\n+        for gfa_state_global_idx, state_pair_equivalence in gfa_state_global_idx_to_state_pair_equivalence.items():\n+            for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n+                for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n+                    if gfa_state_global_idx == s_i and subgoal_gfa_state_global_idx == s_j:\n+                        print(f\" ({s_i} -> {s_j}) matches with state pair equivalence.\")\n+                        match_found = True\n+                        break\n+                    if match_found:\n+                        break\n+\n+                    if not match_found:\n+                        print(f\" ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n+    # Compute feature histograms by complexity\n+    total_features_by_complexity = defaultdict(int)\n+    for feature in total_features:\n+        total_features_by_complexity[feature.compute_complexity()] += 1\n+    total_features_by_complexity = {key: total_features_by_complexity[key] for key in sorted(total_features_by_complexity)}\n+\n+    sketch_features_by_complexity = defaultdict(int)\n+    for feature in sketch_features:\n+        sketch_features_by_complexity[feature.compute_complexity()] += 1\n+    sketch_features_by_complexity = {key: sketch_features_by_complexity[key] for key in sorted(sketch_features_by_complexity)}\n+\n+    # Output the result\n+    with change_dir(\"output\"):\n+        print_separation_line()\n+        logging.info(colored(\"Summary:\", \"green\", \"on_grey\"))\n+\n+        print(f\"Preprocessing time: {int(preprocessing_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"ASP time: {int(asp_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Verification time: {int(verification_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Total time: {int(total_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Total memory: {int(memory_usage() / 1024)} GiB.\")\n+        print(f\"Total number of states: {num_ss_states}\")\n+        print(f\"Total number of abstract states: {num_gfa_states}\")\n+        print(f\"Number of unsat tuples: {count_unsat_tuples}\")\n+        print(f\"Number of sketch rules: {len(sketches)}\")\n+        print(f\"Number of total features: {len(total_features)}\")\n+        print(f\"Number of sketch features: {len(sketch_features)}\")\n+        print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n+        print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n"
                },
                {
                    "date": 1727823266515,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -260,252 +260,4 @@\n         print(f\"Number of total features: {len(total_features)}\")\n         print(f\"Number of sketch features: {len(sketch_features)}\")\n         print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n         print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n-import logging\n-import time\n-from collections import defaultdict\n-from pathlib import Path\n-from termcolor import colored\n-from typing import List, MutableSet, Dict\n-import clingo\n-import pymimir as mm\n-from dlplan.policy import PolicyMinimizer, NamedBoolean, NamedNumerical\n-from collections import defaultdict\n-from .src.exit_codes import ExitCode\n-from .src.iteration import EncodingType, ASPFactory, ClingoExitCode, IterationData, LearningStatistics, Sketch, D2sepDlplanPolicyFactory, ExplicitDlplanPolicyFactory, compute_feature_pool, compute_per_state_feature_valuations, compute_state_pair_equivalences, compute_tuple_graph_equivalences, minimize_tuple_graph_equivalences\n-from .src.util import Timer, create_experiment_workspace, change_working_directory, write_file, change_dir, memory_usage, add_console_handler, print_separation_line\n-from .src.preprocessing import InstanceData, PreprocessingData, StateFinder, compute_instance_datas, compute_tuple_graphs\n-\n-def learn_sketch_for_problem_class(\n-    domain_filepath: Path,\n-    problems_directory: Path,\n-    workspace: Path,\n-    width: int,\n-    disable_closed_Q: bool = True,\n-    max_num_states_per_instance: int = 10000,\n-    max_time_per_instance: int = 10,\n-    encoding_type: EncodingType = EncodingType.EXPLICIT,\n-    max_num_rules: int = 1,\n-    enable_goal_separating_features: bool = True,\n-    disable_feature_generation: bool = True,\n-    enable_incomplete_feature_pruning: bool = False,\n-    concept_complexity_limit: int = 9,\n-    role_complexity_limit: int = 9,\n-    boolean_complexity_limit: int = 9,\n-    count_numerical_complexity_limit: int = 9,\n-    distance_numerical_complexity_limit: int = 9,\n-    feature_limit: int = 1000000,\n-    additional_booleans: List[str] = None,\n-    additional_numericals: List[str] = None,\n-    enable_dump_files: bool = False,\n-):\n-    # Initialize variables and setup\n-    if additional_booleans is None:\n-        additional_booleans = []\n-    if additional_numericals is None:\n-        additional_numericals = []\n-    instance_filepaths = list(problems_directory.iterdir())\n-   \n-\n-    add_console_handler(logging.getLogger(), logging.INFO)\n-    create_experiment_workspace(workspace)\n-    change_working_directory(workspace)\n-\n-    total_timer = Timer()\n-    preprocessing_timer = Timer()\n-    asp_timer = Timer(stopped=True)\n-    verification_timer = Timer(stopped=True)\n-    iteration_data = IterationData()\n-\n-    # Generate data\n-    with change_dir(\"input\"):\n-        logging.info(colored(\"Constructing InstanceDatas...\", \"blue\", \"on_grey\"))\n-        domain_data, instance_datas, num_ss_states, num_gfa_states = compute_instance_datas(domain_filepath, instance_filepaths, disable_closed_Q, max_num_states_per_instance, max_time_per_instance, enable_dump_files)\n-        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-        if instance_datas is None:\n-            raise Exception(\"Failed to create InstanceDatas.\")\n-\n-        state_finder = StateFinder(domain_data, instance_datas)\n-\n-        logging.info(colored(\"Initializing TupleGraphs...\", \"blue\", \"on_grey\"))\n-        gfa_state_id_to_tuple_graph: Dict[int, mm.TupleGraph] = compute_tuple_graphs(domain_data, instance_datas, state_finder, width, enable_dump_files)\n-        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-    preprocessing_data = PreprocessingData(domain_data, instance_datas, state_finder, gfa_state_id_to_tuple_graph)\n-    preprocessing_timer.stop()\n-\n-    if not preprocessing_data.instance_datas:\n-        raise RuntimeError(\"Data is empty\")\n-\n-    sketches = set()\n-    total_features = set()\n-    sketch_features = set()\n-    # Learn sketch\n-    if encoding_type == EncodingType.EXPLICIT:\n-        create_experiment_workspace(workspace)\n-        preprocessing_timer.resume()\n-\n-        for instance_data in preprocessing_data.instance_datas:\n-            print(f\"Instance {instance_data.idx}:\")\n-            for state in instance_data.mimir_ss.get_states():\n-                    state_data = state.get_state()\n-                    fluents = state_data.get_fluent_atoms()\n-                    #print(f\"State {state.get_index()} fluents: {fluents}\")\n-            # TODO: when is the best time to generate features?\n-            # Based on current instance currently looks most reasonable.\n-            # It could be restricted to the states in the tuple graph as well,\n-            # which reduces the number of rules, but not fully sure yet about the consequences in the bigger picture.\n-            iteration_data = IterationData()\n-            iteration_data.instance_datas = [instance_data]\n-            iteration_data.gfa_states = instance_data.gfa.get_states()\n-\n-            # Report some progress\n-            write_file(f\"{instance_data.idx}.dot\", instance_data.dlplan_ss.to_dot(1))\n-            print(\"     \", end=\"\")\n-            print(\"id:\", instance_data.idx,\n-                    \"problem_filepath:\", instance_data.mimir_ss.get_problem().get_filepath(),\n-                    \"num_states:\", instance_data.mimir_ss.get_num_states(),\n-                    \"num_state_equivalences:\", instance_data.gfa.get_num_states())\n-\n-            logging.info(colored(\"Initializing DomainFeatureData...\", \"blue\", \"on_grey\"))\n-            iteration_data.feature_pool = compute_feature_pool(\n-                preprocessing_data,\n-                iteration_data,\n-                gfa_state_id_to_tuple_graph,\n-                state_finder,\n-                disable_feature_generation,\n-                enable_incomplete_feature_pruning,\n-                concept_complexity_limit,\n-                role_complexity_limit,\n-                boolean_complexity_limit,\n-                count_numerical_complexity_limit,\n-                distance_numerical_complexity_limit,\n-                feature_limit,\n-                additional_booleans,\n-                additional_numericals\n-            )\n-            # Add features to features that were generated\n-            total_features.update(feature.dlplan_feature for feature in iteration_data.feature_pool)\n-\n-            logging.info(colored(\"Constructing PerStateFeatureValuations...\", \"blue\", \"on_grey\"))\n-            iteration_data.gfa_state_global_idx_to_feature_evaluations = compute_per_state_feature_valuations(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Constructing StatePairEquivalenceDatas...\", \"blue\", \"on_grey\"))\n-            iteration_data.state_pair_equivalences, iteration_data.gfa_state_global_idx_to_state_pair_equivalence = compute_state_pair_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Constructing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n-            iteration_data.gfa_state_global_idx_to_tuple_graph_equivalence = compute_tuple_graph_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Minimizing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n-            minimize_tuple_graph_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-            preprocessing_timer.stop()\n-            asp_timer.resume()\n-\n-            # We count the number of subgoal tuples for which no rule could be found.\n-            # This usually happens if the pool of features is not sufficiently rich.\n-            count_unsat_tuples = 0\n-            tuple_graph_states = set()\n-           \n-\n-            for gfa_state in instance_data.gfa.get_states():\n-                gfa_state_global_idx = gfa_state.get_global_index()\n-                gfa_state_idx = gfa_state.get_index()\n-\n-                if instance_data.gfa.is_deadend_state(gfa_state_idx):\n-                    continue\n-\n-                #print(f\"Keys in gfa_state_global_idx_to_tuple_graph: {list(preprocessing_data.gfa_state_global_idx_to_tuple_graph.keys())}\")\n-                #print(f\"gfa_state_global_idx: {gfa_state_global_idx}\")\n-\n-                tuple_graph = preprocessing_data.gfa_state_global_idx_to_tuple_graph[gfa_state_global_idx]\n-\n-                for distance, group in enumerate(tuple_graph.get_vertices_grouped_by_distance()):\n-                    if distance == 0:\n-                        # We skip subgoal tuples at distance zero because they do not encode progress towards a goal.\n-                        continue\n-                    for vertex in group:\n-                        # Here we find all simplest single sketch rules for a pair (state, subgoal tuple).\n-                        t_idx = vertex.get_index()\n-                        tuple_graph_states.add((gfa_state_global_idx, t_idx))\n-    \n-                        start_time = time.time()\n-                        asp_factory = ASPFactory(encoding_type, enable_goal_separating_features, max_num_rules)\n-                        facts = asp_factory.make_facts(preprocessing_data, iteration_data, gfa_state, t_idx)\n-\n-                        print(f\"Factory time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        start_time = time.time()\n-                        asp_factory.ground(facts)\n-                        print(f\"Grounding time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        start_time = time.time()\n-                        symbolss, returncode = asp_factory.solve_all_opt()\n-                        print(f\"Solving time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        if returncode in [ClingoExitCode.UNSATISFIABLE, ClingoExitCode.EXHAUSTED]:\n-                            print(colored(\"ASP is unsatisfiable or exhausted!\", \"red\", \"on_grey\"))\n-                            print(colored(f\"No sketch of width {width} exists that solves all instances!\", \"red\", \"on_grey\"))\n-                            # There might be tuples where we get unsat.\n-                            # If such cases occur, which is very likely in complex domains, then we must look closer into this.\n-                            count_unsat_tuples += 1\n-                            continue\n-                        elif returncode == ClingoExitCode.UNKNOWN:\n-                            print(colored(\"ASP solving throws unknown error!\", \"red\", \"on_grey\"))\n-                            exit(ExitCode.UNKNOWN)\n-                        elif returncode == ClingoExitCode.INTERRUPTED:\n-                            print(colored(\"ASP solving interrupted!\", \"red\", \"on_grey\"))\n-                            exit(ExitCode.INTERRUPTED)\n-                        else:\n-                            logging.info(f\"Solution found with return code: {returncode}\")\n-\n-                        for symbols in symbolss:\n-                            dlplan_policy = ExplicitDlplanPolicyFactory().make_dlplan_policy_from_answer_set(symbols, preprocessing_data, iteration_data)\n-\n-                            sketch = Sketch(dlplan_policy, width)\n-                            sketches.add(sketch)\n-                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_booleans())\n-                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_numericals())\n-                            print(dlplan_policy)\n-                            \n-\n-    else:\n-        raise Exception(\"No implementation for the given encoding type.\")\n-\n-    preprocessing_timer.stop()\n-    asp_timer.stop()\n-    verification_timer.stop()\n-    total_timer.stop()\n-\n-    # Compute feature histograms by complexity\n-    total_features_by_complexity = defaultdict(int)\n-    for feature in total_features:\n-        total_features_by_complexity[feature.compute_complexity()] += 1\n-    total_features_by_complexity = {key: total_features_by_complexity[key] for key in sorted(total_features_by_complexity)}\n-\n-    sketch_features_by_complexity = defaultdict(int)\n-    for feature in sketch_features:\n-        sketch_features_by_complexity[feature.compute_complexity()] += 1\n-    sketch_features_by_complexity = {key: sketch_features_by_complexity[key] for key in sorted(sketch_features_by_complexity)}\n-\n-    # Output the result\n-    with change_dir(\"output\"):\n-        print_separation_line()\n-        logging.info(colored(\"Summary:\", \"green\", \"on_grey\"))\n-\n-        print(f\"Preprocessing time: {int(preprocessing_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"ASP time: {int(asp_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Verification time: {int(verification_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Total time: {int(total_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Total memory: {int(memory_usage() / 1024)} GiB.\")\n-        print(f\"Total number of states: {num_ss_states}\")\n-        print(f\"Total number of abstract states: {num_gfa_states}\")\n-        print(f\"Number of unsat tuples: {count_unsat_tuples}\")\n-        print(f\"Number of sketch rules: {len(sketches)}\")\n-        print(f\"Number of total features: {len(total_features)}\")\n-        print(f\"Number of sketch features: {len(sketch_features)}\")\n-        print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n-        print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n"
                },
                {
                    "date": 1727823352187,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -219,9 +219,9 @@\n \n     print(\"Comparison between Tuple Graph States and State Pair Equivalences:\")\n     for (s_i, s_j) in tuple_graph_states:\n         match_found = False\n-        for gfa_state_global_idx, state_pair_equivalence in gfa_state_global_idx_to_state_pair_equivalence.items():\n+        for gfa_state_global_idx, state_pair_equivalence in  iteration_data.state_pair_equivalences.items():\n             for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n                 for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n                     if gfa_state_global_idx == s_i and subgoal_gfa_state_global_idx == s_j:\n                         print(f\" ({s_i} -> {s_j}) matches with state pair equivalence.\")\n"
                },
                {
                    "date": 1727823365652,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -219,9 +219,9 @@\n \n     print(\"Comparison between Tuple Graph States and State Pair Equivalences:\")\n     for (s_i, s_j) in tuple_graph_states:\n         match_found = False\n-        for gfa_state_global_idx, state_pair_equivalence in  iteration_data.state_pair_equivalences.items():\n+        for gfa_state_global_idx, state_pair_equivalence in  iteration_data.state_pair_equivalences:\n             for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n                 for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n                     if gfa_state_global_idx == s_i and subgoal_gfa_state_global_idx == s_j:\n                         print(f\" ({s_i} -> {s_j}) matches with state pair equivalence.\")\n"
                },
                {
                    "date": 1727823423787,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -219,9 +219,9 @@\n \n     print(\"Comparison between Tuple Graph States and State Pair Equivalences:\")\n     for (s_i, s_j) in tuple_graph_states:\n         match_found = False\n-        for gfa_state_global_idx, state_pair_equivalence in  iteration_data.state_pair_equivalences:\n+        for gfa_state_global_idx, state_pair_equivalence in iteration_data.gfa_state_global_idx_to_state_pair_equivalence:\n             for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n                 for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n                     if gfa_state_global_idx == s_i and subgoal_gfa_state_global_idx == s_j:\n                         print(f\" ({s_i} -> {s_j}) matches with state pair equivalence.\")\n"
                },
                {
                    "date": 1727823453935,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -219,9 +219,9 @@\n \n     print(\"Comparison between Tuple Graph States and State Pair Equivalences:\")\n     for (s_i, s_j) in tuple_graph_states:\n         match_found = False\n-        for gfa_state_global_idx, state_pair_equivalence in iteration_data.gfa_state_global_idx_to_state_pair_equivalence:\n+        for gfa_state_global_idx, state_pair_equivalence in iteration_data.gfa_state_global_idx_to_state_pair_equivalence.items():\n             for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n                 for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n                     if gfa_state_global_idx == s_i and subgoal_gfa_state_global_idx == s_j:\n                         print(f\" ({s_i} -> {s_j}) matches with state pair equivalence.\")\n"
                },
                {
                    "date": 1727823507962,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -226,13 +226,13 @@\n                     if gfa_state_global_idx == s_i and subgoal_gfa_state_global_idx == s_j:\n                         print(f\" ({s_i} -> {s_j}) matches with state pair equivalence.\")\n                         match_found = True\n                         break\n-                    if match_found:\n-                        break\n+                if match_found:\n+                    break\n \n-                    if not match_found:\n-                        print(f\" ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n+            if not match_found:\n+                print(f\" ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n     # Compute feature histograms by complexity\n     total_features_by_complexity = defaultdict(int)\n     for feature in total_features:\n         total_features_by_complexity[feature.compute_complexity()] += 1\n"
                },
                {
                    "date": 1727823627664,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -229,10 +229,10 @@\n                         break\n                 if match_found:\n                     break\n \n-            if not match_found:\n-                print(f\" ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n+        if not match_found:\n+            print(f\" ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n     # Compute feature histograms by complexity\n     total_features_by_complexity = defaultdict(int)\n     for feature in total_features:\n         total_features_by_complexity[feature.compute_complexity()] += 1\n"
                },
                {
                    "date": 1727880756318,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,265 @@\n+import logging\n+import time\n+from collections import defaultdict\n+from pathlib import Path\n+from termcolor import colored\n+from typing import List, MutableSet, Dict\n+import clingo\n+import pymimir as mm\n+from dlplan.policy import PolicyMinimizer, NamedBoolean, NamedNumerical\n+from collections import defaultdict\n+from .src.exit_codes import ExitCode\n+from .src.iteration import EncodingType, ASPFactory, ClingoExitCode, IterationData, LearningStatistics, Sketch, D2sepDlplanPolicyFactory, ExplicitDlplanPolicyFactory, compute_feature_pool, compute_per_state_feature_valuations, compute_state_pair_equivalences, compute_tuple_graph_equivalences, minimize_tuple_graph_equivalences\n+from .src.util import Timer, create_experiment_workspace, change_working_directory, write_file, change_dir, memory_usage, add_console_handler, print_separation_line\n+from .src.preprocessing import InstanceData, PreprocessingData, StateFinder, compute_instance_datas, compute_tuple_graphs\n+\n+def learn_sketch_for_problem_class(\n+    domain_filepath: Path,\n+    problems_directory: Path,\n+    workspace: Path,\n+    width: int,\n+    disable_closed_Q: bool = True,\n+    max_num_states_per_instance: int = 10000,\n+    max_time_per_instance: int = 10,\n+    encoding_type: EncodingType = EncodingType.EXPLICIT,\n+    max_num_rules: int = 1,\n+    enable_goal_separating_features: bool = True,\n+    disable_feature_generation: bool = True,\n+    enable_incomplete_feature_pruning: bool = False,\n+    concept_complexity_limit: int = 9,\n+    role_complexity_limit: int = 9,\n+    boolean_complexity_limit: int = 9,\n+    count_numerical_complexity_limit: int = 9,\n+    distance_numerical_complexity_limit: int = 9,\n+    feature_limit: int = 1000000,\n+    additional_booleans: List[str] = None,\n+    additional_numericals: List[str] = None,\n+    enable_dump_files: bool = False,\n+):\n+    # Initialize variables and setup\n+    if additional_booleans is None:\n+        additional_booleans = []\n+    if additional_numericals is None:\n+        additional_numericals = []\n+    instance_filepaths = list(problems_directory.iterdir())\n+   \n+\n+    add_console_handler(logging.getLogger(), logging.INFO)\n+    create_experiment_workspace(workspace)\n+    change_working_directory(workspace)\n+\n+    total_timer = Timer()\n+    preprocessing_timer = Timer()\n+    asp_timer = Timer(stopped=True)\n+    verification_timer = Timer(stopped=True)\n+    iteration_data = IterationData()\n+\n+    # Generate data\n+    with change_dir(\"input\"):\n+        logging.info(colored(\"Constructing InstanceDatas...\", \"blue\", \"on_grey\"))\n+        domain_data, instance_datas, num_ss_states, num_gfa_states = compute_instance_datas(domain_filepath, instance_filepaths, disable_closed_Q, max_num_states_per_instance, max_time_per_instance, enable_dump_files)\n+        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+        if instance_datas is None:\n+            raise Exception(\"Failed to create InstanceDatas.\")\n+\n+        state_finder = StateFinder(domain_data, instance_datas)\n+\n+        logging.info(colored(\"Initializing TupleGraphs...\", \"blue\", \"on_grey\"))\n+        gfa_state_id_to_tuple_graph: Dict[int, mm.TupleGraph] = compute_tuple_graphs(domain_data, instance_datas, state_finder, width, enable_dump_files)\n+        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+    preprocessing_data = PreprocessingData(domain_data, instance_datas, state_finder, gfa_state_id_to_tuple_graph)\n+    preprocessing_timer.stop()\n+\n+    if not preprocessing_data.instance_datas:\n+        raise RuntimeError(\"Data is empty\")\n+\n+    sketches = set()\n+    total_features = set()\n+    sketch_features = set()\n+    # Learn sketch\n+    if encoding_type == EncodingType.EXPLICIT:\n+        create_experiment_workspace(workspace)\n+        preprocessing_timer.resume()\n+\n+        for instance_data in preprocessing_data.instance_datas:\n+            print(f\"Instance {instance_data.idx}:\")\n+            for state in instance_data.mimir_ss.get_states():\n+                    state_data = state.get_state()\n+                    fluents = state_data.get_fluent_atoms()\n+                    #print(f\"State {state.get_index()} fluents: {fluents}\")\n+            # TODO: when is the best time to generate features?\n+            # Based on current instance currently looks most reasonable.\n+            # It could be restricted to the states in the tuple graph as well,\n+            # which reduces the number of rules, but not fully sure yet about the consequences in the bigger picture.\n+            iteration_data = IterationData()\n+            iteration_data.instance_datas = [instance_data]\n+            iteration_data.gfa_states = instance_data.gfa.get_states()\n+\n+            # Report some progress\n+            write_file(f\"{instance_data.idx}.dot\", instance_data.dlplan_ss.to_dot(1))\n+            print(\"     \", end=\"\")\n+            print(\"id:\", instance_data.idx,\n+                    \"problem_filepath:\", instance_data.mimir_ss.get_problem().get_filepath(),\n+                    \"num_states:\", instance_data.mimir_ss.get_num_states(),\n+                    \"num_state_equivalences:\", instance_data.gfa.get_num_states())\n+\n+            logging.info(colored(\"Initializing DomainFeatureData...\", \"blue\", \"on_grey\"))\n+            iteration_data.feature_pool = compute_feature_pool(\n+                preprocessing_data,\n+                iteration_data,\n+                gfa_state_id_to_tuple_graph,\n+                state_finder,\n+                disable_feature_generation,\n+                enable_incomplete_feature_pruning,\n+                concept_complexity_limit,\n+                role_complexity_limit,\n+                boolean_complexity_limit,\n+                count_numerical_complexity_limit,\n+                distance_numerical_complexity_limit,\n+                feature_limit,\n+                additional_booleans,\n+                additional_numericals\n+            )\n+            # Add features to features that were generated\n+            total_features.update(feature.dlplan_feature for feature in iteration_data.feature_pool)\n+\n+            logging.info(colored(\"Constructing PerStateFeatureValuations...\", \"blue\", \"on_grey\"))\n+            iteration_data.gfa_state_global_idx_to_feature_evaluations = compute_per_state_feature_valuations(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Constructing StatePairEquivalenceDatas...\", \"blue\", \"on_grey\"))\n+            iteration_data.state_pair_equivalences, iteration_data.gfa_state_global_idx_to_state_pair_equivalence = compute_state_pair_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Constructing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n+            iteration_data.gfa_state_global_idx_to_tuple_graph_equivalence = compute_tuple_graph_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Minimizing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n+            minimize_tuple_graph_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+            preprocessing_timer.stop()\n+            asp_timer.resume()\n+            for gfa_state_id, tuple_graph in gfa_state_id_to_tuple_graph.items():\n+                states_in_graph = tuple_graph.get_states()  # Adjust this line based on your actual tuple graph API\n+                print(f\"GFA State ID: {gfa_state_id} -> States in Tuple Graph: {states_in_graph}\")\n+            # We count the number of subgoal tuples for which no rule could be found.\n+            # This usually happens if the pool of features is not sufficiently rich.\n+            count_unsat_tuples = 0\n+            tuple_graph_states = set()\n+           \n+\n+            for gfa_state in instance_data.gfa.get_states():\n+                gfa_state_global_idx = gfa_state.get_global_index()\n+                gfa_state_idx = gfa_state.get_index()\n+\n+                if instance_data.gfa.is_deadend_state(gfa_state_idx):\n+                    continue\n+\n+                #print(f\"Keys in gfa_state_global_idx_to_tuple_graph: {list(preprocessing_data.gfa_state_global_idx_to_tuple_graph.keys())}\")\n+                #print(f\"gfa_state_global_idx: {gfa_state_global_idx}\")\n+\n+                tuple_graph = preprocessing_data.gfa_state_global_idx_to_tuple_graph[gfa_state_global_idx]\n+\n+                for distance, group in enumerate(tuple_graph.get_vertices_grouped_by_distance()):\n+                    if distance == 0:\n+                        # We skip subgoal tuples at distance zero because they do not encode progress towards a goal.\n+                        continue\n+                    for vertex in group:\n+                        # Here we find all simplest single sketch rules for a pair (state, subgoal tuple).\n+                        t_idx = vertex.get_index()\n+                        tuple_graph_states.add((gfa_state_global_idx, t_idx))\n+    \n+                        start_time = time.time()\n+                        asp_factory = ASPFactory(encoding_type, enable_goal_separating_features, max_num_rules)\n+                        facts = asp_factory.make_facts(preprocessing_data, iteration_data, gfa_state, t_idx)\n+\n+                        print(f\"Factory time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        start_time = time.time()\n+                        asp_factory.ground(facts)\n+                        print(f\"Grounding time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        start_time = time.time()\n+                        symbolss, returncode = asp_factory.solve_all_opt()\n+                        print(f\"Solving time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        if returncode in [ClingoExitCode.UNSATISFIABLE, ClingoExitCode.EXHAUSTED]:\n+                            print(colored(\"ASP is unsatisfiable or exhausted!\", \"red\", \"on_grey\"))\n+                            print(colored(f\"No sketch of width {width} exists that solves all instances!\", \"red\", \"on_grey\"))\n+                            # There might be tuples where we get unsat.\n+                            # If such cases occur, which is very likely in complex domains, then we must look closer into this.\n+                            count_unsat_tuples += 1\n+                            continue\n+                        elif returncode == ClingoExitCode.UNKNOWN:\n+                            print(colored(\"ASP solving throws unknown error!\", \"red\", \"on_grey\"))\n+                            exit(ExitCode.UNKNOWN)\n+                        elif returncode == ClingoExitCode.INTERRUPTED:\n+                            print(colored(\"ASP solving interrupted!\", \"red\", \"on_grey\"))\n+                            exit(ExitCode.INTERRUPTED)\n+                        else:\n+                            logging.info(f\"Solution found with return code: {returncode}\")\n+\n+                        for symbols in symbolss:\n+                            dlplan_policy = ExplicitDlplanPolicyFactory().make_dlplan_policy_from_answer_set(symbols, preprocessing_data, iteration_data)\n+\n+                            sketch = Sketch(dlplan_policy, width)\n+                            sketches.add(sketch)\n+                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_booleans())\n+                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_numericals())\n+                            print(dlplan_policy)\n+                            \n+\n+    else:\n+        raise Exception(\"No implementation for the given encoding type.\")\n+\n+    preprocessing_timer.stop()\n+    asp_timer.stop()\n+    verification_timer.stop()\n+    total_timer.stop()\n+\n+    print(\"Comparison between Tuple Graph States and State Pair Equivalences:\")\n+    for (s_i, s_j) in tuple_graph_states:\n+        match_found = False\n+        for gfa_state_global_idx, state_pair_equivalence in iteration_data.gfa_state_global_idx_to_state_pair_equivalence.items():\n+            for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n+                for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n+                    if gfa_state_global_idx == s_i and subgoal_gfa_state_global_idx == s_j:\n+                        print(f\" ({s_i} -> {s_j}) matches with state pair equivalence.\")\n+                        match_found = True\n+                        break\n+                if match_found:\n+                    break\n+\n+        if not match_found:\n+            print(f\" ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n+    # Compute feature histograms by complexity\n+    total_features_by_complexity = defaultdict(int)\n+    for feature in total_features:\n+        total_features_by_complexity[feature.compute_complexity()] += 1\n+    total_features_by_complexity = {key: total_features_by_complexity[key] for key in sorted(total_features_by_complexity)}\n+\n+    sketch_features_by_complexity = defaultdict(int)\n+    for feature in sketch_features:\n+        sketch_features_by_complexity[feature.compute_complexity()] += 1\n+    sketch_features_by_complexity = {key: sketch_features_by_complexity[key] for key in sorted(sketch_features_by_complexity)}\n+\n+    # Output the result\n+    with change_dir(\"output\"):\n+        print_separation_line()\n+        logging.info(colored(\"Summary:\", \"green\", \"on_grey\"))\n+\n+        print(f\"Preprocessing time: {int(preprocessing_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"ASP time: {int(asp_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Verification time: {int(verification_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Total time: {int(total_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Total memory: {int(memory_usage() / 1024)} GiB.\")\n+        print(f\"Total number of states: {num_ss_states}\")\n+        print(f\"Total number of abstract states: {num_gfa_states}\")\n+        print(f\"Number of unsat tuples: {count_unsat_tuples}\")\n+        print(f\"Number of sketch rules: {len(sketches)}\")\n+        print(f\"Number of total features: {len(total_features)}\")\n+        print(f\"Number of sketch features: {len(sketch_features)}\")\n+        print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n+        print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n"
                },
                {
                    "date": 1727880775502,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -141,9 +141,9 @@\n             logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n             preprocessing_timer.stop()\n             asp_timer.resume()\n             for gfa_state_id, tuple_graph in gfa_state_id_to_tuple_graph.items():\n-                states_in_graph = tuple_graph.get_states()  # Adjust this line based on your actual tuple graph API\n+                states_in_graph = tuple_graph.get_states()  \n                 print(f\"GFA State ID: {gfa_state_id} -> States in Tuple Graph: {states_in_graph}\")\n             # We count the number of subgoal tuples for which no rule could be found.\n             # This usually happens if the pool of features is not sufficiently rich.\n             count_unsat_tuples = 0\n"
                },
                {
                    "date": 1727880804933,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -141,9 +141,9 @@\n             logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n             preprocessing_timer.stop()\n             asp_timer.resume()\n             for gfa_state_id, tuple_graph in gfa_state_id_to_tuple_graph.items():\n-                states_in_graph = tuple_graph.get_states()  \n+                states_in_graph = tuple_graph.get_state_space()\n                 print(f\"GFA State ID: {gfa_state_id} -> States in Tuple Graph: {states_in_graph}\")\n             # We count the number of subgoal tuples for which no rule could be found.\n             # This usually happens if the pool of features is not sufficiently rich.\n             count_unsat_tuples = 0\n@@ -262,267 +262,4 @@\n         print(f\"Number of total features: {len(total_features)}\")\n         print(f\"Number of sketch features: {len(sketch_features)}\")\n         print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n         print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n-import logging\n-import time\n-from collections import defaultdict\n-from pathlib import Path\n-from termcolor import colored\n-from typing import List, MutableSet, Dict\n-import clingo\n-import pymimir as mm\n-from dlplan.policy import PolicyMinimizer, NamedBoolean, NamedNumerical\n-from collections import defaultdict\n-from .src.exit_codes import ExitCode\n-from .src.iteration import EncodingType, ASPFactory, ClingoExitCode, IterationData, LearningStatistics, Sketch, D2sepDlplanPolicyFactory, ExplicitDlplanPolicyFactory, compute_feature_pool, compute_per_state_feature_valuations, compute_state_pair_equivalences, compute_tuple_graph_equivalences, minimize_tuple_graph_equivalences\n-from .src.util import Timer, create_experiment_workspace, change_working_directory, write_file, change_dir, memory_usage, add_console_handler, print_separation_line\n-from .src.preprocessing import InstanceData, PreprocessingData, StateFinder, compute_instance_datas, compute_tuple_graphs\n-\n-def learn_sketch_for_problem_class(\n-    domain_filepath: Path,\n-    problems_directory: Path,\n-    workspace: Path,\n-    width: int,\n-    disable_closed_Q: bool = True,\n-    max_num_states_per_instance: int = 10000,\n-    max_time_per_instance: int = 10,\n-    encoding_type: EncodingType = EncodingType.EXPLICIT,\n-    max_num_rules: int = 1,\n-    enable_goal_separating_features: bool = True,\n-    disable_feature_generation: bool = True,\n-    enable_incomplete_feature_pruning: bool = False,\n-    concept_complexity_limit: int = 9,\n-    role_complexity_limit: int = 9,\n-    boolean_complexity_limit: int = 9,\n-    count_numerical_complexity_limit: int = 9,\n-    distance_numerical_complexity_limit: int = 9,\n-    feature_limit: int = 1000000,\n-    additional_booleans: List[str] = None,\n-    additional_numericals: List[str] = None,\n-    enable_dump_files: bool = False,\n-):\n-    # Initialize variables and setup\n-    if additional_booleans is None:\n-        additional_booleans = []\n-    if additional_numericals is None:\n-        additional_numericals = []\n-    instance_filepaths = list(problems_directory.iterdir())\n-   \n-\n-    add_console_handler(logging.getLogger(), logging.INFO)\n-    create_experiment_workspace(workspace)\n-    change_working_directory(workspace)\n-\n-    total_timer = Timer()\n-    preprocessing_timer = Timer()\n-    asp_timer = Timer(stopped=True)\n-    verification_timer = Timer(stopped=True)\n-    iteration_data = IterationData()\n-\n-    # Generate data\n-    with change_dir(\"input\"):\n-        logging.info(colored(\"Constructing InstanceDatas...\", \"blue\", \"on_grey\"))\n-        domain_data, instance_datas, num_ss_states, num_gfa_states = compute_instance_datas(domain_filepath, instance_filepaths, disable_closed_Q, max_num_states_per_instance, max_time_per_instance, enable_dump_files)\n-        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-        if instance_datas is None:\n-            raise Exception(\"Failed to create InstanceDatas.\")\n-\n-        state_finder = StateFinder(domain_data, instance_datas)\n-\n-        logging.info(colored(\"Initializing TupleGraphs...\", \"blue\", \"on_grey\"))\n-        gfa_state_id_to_tuple_graph: Dict[int, mm.TupleGraph] = compute_tuple_graphs(domain_data, instance_datas, state_finder, width, enable_dump_files)\n-        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-    preprocessing_data = PreprocessingData(domain_data, instance_datas, state_finder, gfa_state_id_to_tuple_graph)\n-    preprocessing_timer.stop()\n-\n-    if not preprocessing_data.instance_datas:\n-        raise RuntimeError(\"Data is empty\")\n-\n-    sketches = set()\n-    total_features = set()\n-    sketch_features = set()\n-    # Learn sketch\n-    if encoding_type == EncodingType.EXPLICIT:\n-        create_experiment_workspace(workspace)\n-        preprocessing_timer.resume()\n-\n-        for instance_data in preprocessing_data.instance_datas:\n-            print(f\"Instance {instance_data.idx}:\")\n-            for state in instance_data.mimir_ss.get_states():\n-                    state_data = state.get_state()\n-                    fluents = state_data.get_fluent_atoms()\n-                    #print(f\"State {state.get_index()} fluents: {fluents}\")\n-            # TODO: when is the best time to generate features?\n-            # Based on current instance currently looks most reasonable.\n-            # It could be restricted to the states in the tuple graph as well,\n-            # which reduces the number of rules, but not fully sure yet about the consequences in the bigger picture.\n-            iteration_data = IterationData()\n-            iteration_data.instance_datas = [instance_data]\n-            iteration_data.gfa_states = instance_data.gfa.get_states()\n-\n-            # Report some progress\n-            write_file(f\"{instance_data.idx}.dot\", instance_data.dlplan_ss.to_dot(1))\n-            print(\"     \", end=\"\")\n-            print(\"id:\", instance_data.idx,\n-                    \"problem_filepath:\", instance_data.mimir_ss.get_problem().get_filepath(),\n-                    \"num_states:\", instance_data.mimir_ss.get_num_states(),\n-                    \"num_state_equivalences:\", instance_data.gfa.get_num_states())\n-\n-            logging.info(colored(\"Initializing DomainFeatureData...\", \"blue\", \"on_grey\"))\n-            iteration_data.feature_pool = compute_feature_pool(\n-                preprocessing_data,\n-                iteration_data,\n-                gfa_state_id_to_tuple_graph,\n-                state_finder,\n-                disable_feature_generation,\n-                enable_incomplete_feature_pruning,\n-                concept_complexity_limit,\n-                role_complexity_limit,\n-                boolean_complexity_limit,\n-                count_numerical_complexity_limit,\n-                distance_numerical_complexity_limit,\n-                feature_limit,\n-                additional_booleans,\n-                additional_numericals\n-            )\n-            # Add features to features that were generated\n-            total_features.update(feature.dlplan_feature for feature in iteration_data.feature_pool)\n-\n-            logging.info(colored(\"Constructing PerStateFeatureValuations...\", \"blue\", \"on_grey\"))\n-            iteration_data.gfa_state_global_idx_to_feature_evaluations = compute_per_state_feature_valuations(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Constructing StatePairEquivalenceDatas...\", \"blue\", \"on_grey\"))\n-            iteration_data.state_pair_equivalences, iteration_data.gfa_state_global_idx_to_state_pair_equivalence = compute_state_pair_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Constructing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n-            iteration_data.gfa_state_global_idx_to_tuple_graph_equivalence = compute_tuple_graph_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Minimizing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n-            minimize_tuple_graph_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-            preprocessing_timer.stop()\n-            asp_timer.resume()\n-\n-            # We count the number of subgoal tuples for which no rule could be found.\n-            # This usually happens if the pool of features is not sufficiently rich.\n-            count_unsat_tuples = 0\n-            tuple_graph_states = set()\n-           \n-\n-            for gfa_state in instance_data.gfa.get_states():\n-                gfa_state_global_idx = gfa_state.get_global_index()\n-                gfa_state_idx = gfa_state.get_index()\n-\n-                if instance_data.gfa.is_deadend_state(gfa_state_idx):\n-                    continue\n-\n-                #print(f\"Keys in gfa_state_global_idx_to_tuple_graph: {list(preprocessing_data.gfa_state_global_idx_to_tuple_graph.keys())}\")\n-                #print(f\"gfa_state_global_idx: {gfa_state_global_idx}\")\n-\n-                tuple_graph = preprocessing_data.gfa_state_global_idx_to_tuple_graph[gfa_state_global_idx]\n-\n-                for distance, group in enumerate(tuple_graph.get_vertices_grouped_by_distance()):\n-                    if distance == 0:\n-                        # We skip subgoal tuples at distance zero because they do not encode progress towards a goal.\n-                        continue\n-                    for vertex in group:\n-                        # Here we find all simplest single sketch rules for a pair (state, subgoal tuple).\n-                        t_idx = vertex.get_index()\n-                        tuple_graph_states.add((gfa_state_global_idx, t_idx))\n-    \n-                        start_time = time.time()\n-                        asp_factory = ASPFactory(encoding_type, enable_goal_separating_features, max_num_rules)\n-                        facts = asp_factory.make_facts(preprocessing_data, iteration_data, gfa_state, t_idx)\n-\n-                        print(f\"Factory time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        start_time = time.time()\n-                        asp_factory.ground(facts)\n-                        print(f\"Grounding time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        start_time = time.time()\n-                        symbolss, returncode = asp_factory.solve_all_opt()\n-                        print(f\"Solving time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        if returncode in [ClingoExitCode.UNSATISFIABLE, ClingoExitCode.EXHAUSTED]:\n-                            print(colored(\"ASP is unsatisfiable or exhausted!\", \"red\", \"on_grey\"))\n-                            print(colored(f\"No sketch of width {width} exists that solves all instances!\", \"red\", \"on_grey\"))\n-                            # There might be tuples where we get unsat.\n-                            # If such cases occur, which is very likely in complex domains, then we must look closer into this.\n-                            count_unsat_tuples += 1\n-                            continue\n-                        elif returncode == ClingoExitCode.UNKNOWN:\n-                            print(colored(\"ASP solving throws unknown error!\", \"red\", \"on_grey\"))\n-                            exit(ExitCode.UNKNOWN)\n-                        elif returncode == ClingoExitCode.INTERRUPTED:\n-                            print(colored(\"ASP solving interrupted!\", \"red\", \"on_grey\"))\n-                            exit(ExitCode.INTERRUPTED)\n-                        else:\n-                            logging.info(f\"Solution found with return code: {returncode}\")\n-\n-                        for symbols in symbolss:\n-                            dlplan_policy = ExplicitDlplanPolicyFactory().make_dlplan_policy_from_answer_set(symbols, preprocessing_data, iteration_data)\n-\n-                            sketch = Sketch(dlplan_policy, width)\n-                            sketches.add(sketch)\n-                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_booleans())\n-                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_numericals())\n-                            print(dlplan_policy)\n-                            \n-\n-    else:\n-        raise Exception(\"No implementation for the given encoding type.\")\n-\n-    preprocessing_timer.stop()\n-    asp_timer.stop()\n-    verification_timer.stop()\n-    total_timer.stop()\n-\n-    print(\"Comparison between Tuple Graph States and State Pair Equivalences:\")\n-    for (s_i, s_j) in tuple_graph_states:\n-        match_found = False\n-        for gfa_state_global_idx, state_pair_equivalence in iteration_data.gfa_state_global_idx_to_state_pair_equivalence.items():\n-            for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n-                for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n-                    if gfa_state_global_idx == s_i and subgoal_gfa_state_global_idx == s_j:\n-                        print(f\" ({s_i} -> {s_j}) matches with state pair equivalence.\")\n-                        match_found = True\n-                        break\n-                if match_found:\n-                    break\n-\n-        if not match_found:\n-            print(f\" ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n-    # Compute feature histograms by complexity\n-    total_features_by_complexity = defaultdict(int)\n-    for feature in total_features:\n-        total_features_by_complexity[feature.compute_complexity()] += 1\n-    total_features_by_complexity = {key: total_features_by_complexity[key] for key in sorted(total_features_by_complexity)}\n-\n-    sketch_features_by_complexity = defaultdict(int)\n-    for feature in sketch_features:\n-        sketch_features_by_complexity[feature.compute_complexity()] += 1\n-    sketch_features_by_complexity = {key: sketch_features_by_complexity[key] for key in sorted(sketch_features_by_complexity)}\n-\n-    # Output the result\n-    with change_dir(\"output\"):\n-        print_separation_line()\n-        logging.info(colored(\"Summary:\", \"green\", \"on_grey\"))\n-\n-        print(f\"Preprocessing time: {int(preprocessing_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"ASP time: {int(asp_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Verification time: {int(verification_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Total time: {int(total_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Total memory: {int(memory_usage() / 1024)} GiB.\")\n-        print(f\"Total number of states: {num_ss_states}\")\n-        print(f\"Total number of abstract states: {num_gfa_states}\")\n-        print(f\"Number of unsat tuples: {count_unsat_tuples}\")\n-        print(f\"Number of sketch rules: {len(sketches)}\")\n-        print(f\"Number of total features: {len(total_features)}\")\n-        print(f\"Number of sketch features: {len(sketch_features)}\")\n-        print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n-        print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n"
                },
                {
                    "date": 1727880902002,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -142,9 +142,9 @@\n             preprocessing_timer.stop()\n             asp_timer.resume()\n             for gfa_state_id, tuple_graph in gfa_state_id_to_tuple_graph.items():\n                 states_in_graph = tuple_graph.get_state_space()\n-                print(f\"GFA State ID: {gfa_state_id} -> States in Tuple Graph: {states_in_graph}\")\n+                print(f\" States in Tuple Graph: {states_in_graph}\")\n             # We count the number of subgoal tuples for which no rule could be found.\n             # This usually happens if the pool of features is not sufficiently rich.\n             count_unsat_tuples = 0\n             tuple_graph_states = set()\n"
                },
                {
                    "date": 1727880916702,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -142,9 +142,9 @@\n             preprocessing_timer.stop()\n             asp_timer.resume()\n             for gfa_state_id, tuple_graph in gfa_state_id_to_tuple_graph.items():\n                 states_in_graph = tuple_graph.get_state_space()\n-                print(f\" States in Tuple Graph: {states_in_graph}\")\n+                print(f\"GFA State ID: {gfa_state_id} -> States in Tuple Graph: {states_in_graph}\")\n             # We count the number of subgoal tuples for which no rule could be found.\n             # This usually happens if the pool of features is not sufficiently rich.\n             count_unsat_tuples = 0\n             tuple_graph_states = set()\n"
                },
                {
                    "date": 1727880995803,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,265 @@\n+import logging\n+import time\n+from collections import defaultdict\n+from pathlib import Path\n+from termcolor import colored\n+from typing import List, MutableSet, Dict\n+import clingo\n+import pymimir as mm\n+from dlplan.policy import PolicyMinimizer, NamedBoolean, NamedNumerical\n+from collections import defaultdict\n+from .src.exit_codes import ExitCode\n+from .src.iteration import EncodingType, ASPFactory, ClingoExitCode, IterationData, LearningStatistics, Sketch, D2sepDlplanPolicyFactory, ExplicitDlplanPolicyFactory, compute_feature_pool, compute_per_state_feature_valuations, compute_state_pair_equivalences, compute_tuple_graph_equivalences, minimize_tuple_graph_equivalences\n+from .src.util import Timer, create_experiment_workspace, change_working_directory, write_file, change_dir, memory_usage, add_console_handler, print_separation_line\n+from .src.preprocessing import InstanceData, PreprocessingData, StateFinder, compute_instance_datas, compute_tuple_graphs\n+\n+def learn_sketch_for_problem_class(\n+    domain_filepath: Path,\n+    problems_directory: Path,\n+    workspace: Path,\n+    width: int,\n+    disable_closed_Q: bool = True,\n+    max_num_states_per_instance: int = 10000,\n+    max_time_per_instance: int = 10,\n+    encoding_type: EncodingType = EncodingType.EXPLICIT,\n+    max_num_rules: int = 1,\n+    enable_goal_separating_features: bool = True,\n+    disable_feature_generation: bool = True,\n+    enable_incomplete_feature_pruning: bool = False,\n+    concept_complexity_limit: int = 9,\n+    role_complexity_limit: int = 9,\n+    boolean_complexity_limit: int = 9,\n+    count_numerical_complexity_limit: int = 9,\n+    distance_numerical_complexity_limit: int = 9,\n+    feature_limit: int = 1000000,\n+    additional_booleans: List[str] = None,\n+    additional_numericals: List[str] = None,\n+    enable_dump_files: bool = False,\n+):\n+    # Initialize variables and setup\n+    if additional_booleans is None:\n+        additional_booleans = []\n+    if additional_numericals is None:\n+        additional_numericals = []\n+    instance_filepaths = list(problems_directory.iterdir())\n+   \n+\n+    add_console_handler(logging.getLogger(), logging.INFO)\n+    create_experiment_workspace(workspace)\n+    change_working_directory(workspace)\n+\n+    total_timer = Timer()\n+    preprocessing_timer = Timer()\n+    asp_timer = Timer(stopped=True)\n+    verification_timer = Timer(stopped=True)\n+    iteration_data = IterationData()\n+\n+    # Generate data\n+    with change_dir(\"input\"):\n+        logging.info(colored(\"Constructing InstanceDatas...\", \"blue\", \"on_grey\"))\n+        domain_data, instance_datas, num_ss_states, num_gfa_states = compute_instance_datas(domain_filepath, instance_filepaths, disable_closed_Q, max_num_states_per_instance, max_time_per_instance, enable_dump_files)\n+        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+        if instance_datas is None:\n+            raise Exception(\"Failed to create InstanceDatas.\")\n+\n+        state_finder = StateFinder(domain_data, instance_datas)\n+\n+        logging.info(colored(\"Initializing TupleGraphs...\", \"blue\", \"on_grey\"))\n+        gfa_state_id_to_tuple_graph: Dict[int, mm.TupleGraph] = compute_tuple_graphs(domain_data, instance_datas, state_finder, width, enable_dump_files)\n+        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+    preprocessing_data = PreprocessingData(domain_data, instance_datas, state_finder, gfa_state_id_to_tuple_graph)\n+    preprocessing_timer.stop()\n+\n+    if not preprocessing_data.instance_datas:\n+        raise RuntimeError(\"Data is empty\")\n+\n+    sketches = set()\n+    total_features = set()\n+    sketch_features = set()\n+    # Learn sketch\n+    if encoding_type == EncodingType.EXPLICIT:\n+        create_experiment_workspace(workspace)\n+        preprocessing_timer.resume()\n+\n+        for instance_data in preprocessing_data.instance_datas:\n+            print(f\"Instance {instance_data.idx}:\")\n+            for state in instance_data.mimir_ss.get_states():\n+                    state_data = state.get_state()\n+                    fluents = state_data.get_fluent_atoms()\n+                    #print(f\"State {state.get_index()} fluents: {fluents}\")\n+            # TODO: when is the best time to generate features?\n+            # Based on current instance currently looks most reasonable.\n+            # It could be restricted to the states in the tuple graph as well,\n+            # which reduces the number of rules, but not fully sure yet about the consequences in the bigger picture.\n+            iteration_data = IterationData()\n+            iteration_data.instance_datas = [instance_data]\n+            iteration_data.gfa_states = instance_data.gfa.get_states()\n+\n+            # Report some progress\n+            write_file(f\"{instance_data.idx}.dot\", instance_data.dlplan_ss.to_dot(1))\n+            print(\"     \", end=\"\")\n+            print(\"id:\", instance_data.idx,\n+                    \"problem_filepath:\", instance_data.mimir_ss.get_problem().get_filepath(),\n+                    \"num_states:\", instance_data.mimir_ss.get_num_states(),\n+                    \"num_state_equivalences:\", instance_data.gfa.get_num_states())\n+\n+            logging.info(colored(\"Initializing DomainFeatureData...\", \"blue\", \"on_grey\"))\n+            iteration_data.feature_pool = compute_feature_pool(\n+                preprocessing_data,\n+                iteration_data,\n+                gfa_state_id_to_tuple_graph,\n+                state_finder,\n+                disable_feature_generation,\n+                enable_incomplete_feature_pruning,\n+                concept_complexity_limit,\n+                role_complexity_limit,\n+                boolean_complexity_limit,\n+                count_numerical_complexity_limit,\n+                distance_numerical_complexity_limit,\n+                feature_limit,\n+                additional_booleans,\n+                additional_numericals\n+            )\n+            # Add features to features that were generated\n+            total_features.update(feature.dlplan_feature for feature in iteration_data.feature_pool)\n+\n+            logging.info(colored(\"Constructing PerStateFeatureValuations...\", \"blue\", \"on_grey\"))\n+            iteration_data.gfa_state_global_idx_to_feature_evaluations = compute_per_state_feature_valuations(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Constructing StatePairEquivalenceDatas...\", \"blue\", \"on_grey\"))\n+            iteration_data.state_pair_equivalences, iteration_data.gfa_state_global_idx_to_state_pair_equivalence = compute_state_pair_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Constructing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n+            iteration_data.gfa_state_global_idx_to_tuple_graph_equivalence = compute_tuple_graph_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Minimizing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n+            minimize_tuple_graph_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+            preprocessing_timer.stop()\n+            asp_timer.resume()\n+            for gfa_state_id, tuple_graph in gfa_state_id_to_tuple_graph.items():\n+                states_in_graph = tuple_graph.get_state_space()\n+                print(f\"GFA State ID: {gfa_state_id} -> States in Tuple Graph: {states_in_graph}\")\n+            # We count the number of subgoal tuples for which no rule could be found.\n+            # This usually happens if the pool of features is not sufficiently rich.\n+            count_unsat_tuples = 0\n+            tuple_graph_states = set()\n+           \n+\n+            for gfa_state in instance_data.gfa.get_states():\n+                gfa_state_global_idx = gfa_state.get_global_index()\n+                gfa_state_idx = gfa_state.get_index()\n+\n+                if instance_data.gfa.is_deadend_state(gfa_state_idx):\n+                    continue\n+\n+                #print(f\"Keys in gfa_state_global_idx_to_tuple_graph: {list(preprocessing_data.gfa_state_global_idx_to_tuple_graph.keys())}\")\n+                #print(f\"gfa_state_global_idx: {gfa_state_global_idx}\")\n+\n+                tuple_graph = preprocessing_data.gfa_state_global_idx_to_tuple_graph[gfa_state_global_idx]\n+\n+                for distance, group in enumerate(tuple_graph.get_vertices_grouped_by_distance()):\n+                    if distance == 0:\n+                        # We skip subgoal tuples at distance zero because they do not encode progress towards a goal.\n+                        continue\n+                    for vertex in group:\n+                        # Here we find all simplest single sketch rules for a pair (state, subgoal tuple).\n+                        t_idx = vertex.get_index()\n+                        tuple_graph_states.add((gfa_state_global_idx, t_idx))\n+    \n+                        start_time = time.time()\n+                        asp_factory = ASPFactory(encoding_type, enable_goal_separating_features, max_num_rules)\n+                        facts = asp_factory.make_facts(preprocessing_data, iteration_data, gfa_state, t_idx)\n+\n+                        print(f\"Factory time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        start_time = time.time()\n+                        asp_factory.ground(facts)\n+                        print(f\"Grounding time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        start_time = time.time()\n+                        symbolss, returncode = asp_factory.solve_all_opt()\n+                        print(f\"Solving time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        if returncode in [ClingoExitCode.UNSATISFIABLE, ClingoExitCode.EXHAUSTED]:\n+                            print(colored(\"ASP is unsatisfiable or exhausted!\", \"red\", \"on_grey\"))\n+                            print(colored(f\"No sketch of width {width} exists that solves all instances!\", \"red\", \"on_grey\"))\n+                            # There might be tuples where we get unsat.\n+                            # If such cases occur, which is very likely in complex domains, then we must look closer into this.\n+                            count_unsat_tuples += 1\n+                            continue\n+                        elif returncode == ClingoExitCode.UNKNOWN:\n+                            print(colored(\"ASP solving throws unknown error!\", \"red\", \"on_grey\"))\n+                            exit(ExitCode.UNKNOWN)\n+                        elif returncode == ClingoExitCode.INTERRUPTED:\n+                            print(colored(\"ASP solving interrupted!\", \"red\", \"on_grey\"))\n+                            exit(ExitCode.INTERRUPTED)\n+                        else:\n+                            logging.info(f\"Solution found with return code: {returncode}\")\n+\n+                        for symbols in symbolss:\n+                            dlplan_policy = ExplicitDlplanPolicyFactory().make_dlplan_policy_from_answer_set(symbols, preprocessing_data, iteration_data)\n+\n+                            sketch = Sketch(dlplan_policy, width)\n+                            sketches.add(sketch)\n+                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_booleans())\n+                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_numericals())\n+                            print(dlplan_policy)\n+                            \n+\n+    else:\n+        raise Exception(\"No implementation for the given encoding type.\")\n+\n+    preprocessing_timer.stop()\n+    asp_timer.stop()\n+    verification_timer.stop()\n+    total_timer.stop()\n+\n+    print(\"Comparison between Tuple Graph States and State Pair Equivalences:\")\n+    for (s_i, s_j) in tuple_graph_states:\n+        match_found = False\n+        for gfa_state_global_idx, state_pair_equivalence in iteration_data.gfa_state_global_idx_to_state_pair_equivalence.items():\n+            for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n+                for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n+                    if gfa_state_global_idx == s_i and subgoal_gfa_state_global_idx == s_j:\n+                        print(f\" ({s_i} -> {s_j}) matches with state pair equivalence.\")\n+                        match_found = True\n+                        break\n+                if match_found:\n+                    break\n+\n+        if not match_found:\n+            print(f\" ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n+    # Compute feature histograms by complexity\n+    total_features_by_complexity = defaultdict(int)\n+    for feature in total_features:\n+        total_features_by_complexity[feature.compute_complexity()] += 1\n+    total_features_by_complexity = {key: total_features_by_complexity[key] for key in sorted(total_features_by_complexity)}\n+\n+    sketch_features_by_complexity = defaultdict(int)\n+    for feature in sketch_features:\n+        sketch_features_by_complexity[feature.compute_complexity()] += 1\n+    sketch_features_by_complexity = {key: sketch_features_by_complexity[key] for key in sorted(sketch_features_by_complexity)}\n+\n+    # Output the result\n+    with change_dir(\"output\"):\n+        print_separation_line()\n+        logging.info(colored(\"Summary:\", \"green\", \"on_grey\"))\n+\n+        print(f\"Preprocessing time: {int(preprocessing_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"ASP time: {int(asp_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Verification time: {int(verification_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Total time: {int(total_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Total memory: {int(memory_usage() / 1024)} GiB.\")\n+        print(f\"Total number of states: {num_ss_states}\")\n+        print(f\"Total number of abstract states: {num_gfa_states}\")\n+        print(f\"Number of unsat tuples: {count_unsat_tuples}\")\n+        print(f\"Number of sketch rules: {len(sketches)}\")\n+        print(f\"Number of total features: {len(total_features)}\")\n+        print(f\"Number of sketch features: {len(sketch_features)}\")\n+        print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n+        print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n"
                },
                {
                    "date": 1727881032622,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -262,269 +262,4 @@\n         print(f\"Number of total features: {len(total_features)}\")\n         print(f\"Number of sketch features: {len(sketch_features)}\")\n         print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n         print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n-import logging\n-import time\n-from collections import defaultdict\n-from pathlib import Path\n-from termcolor import colored\n-from typing import List, MutableSet, Dict\n-import clingo\n-import pymimir as mm\n-from dlplan.policy import PolicyMinimizer, NamedBoolean, NamedNumerical\n-from collections import defaultdict\n-from .src.exit_codes import ExitCode\n-from .src.iteration import EncodingType, ASPFactory, ClingoExitCode, IterationData, LearningStatistics, Sketch, D2sepDlplanPolicyFactory, ExplicitDlplanPolicyFactory, compute_feature_pool, compute_per_state_feature_valuations, compute_state_pair_equivalences, compute_tuple_graph_equivalences, minimize_tuple_graph_equivalences\n-from .src.util import Timer, create_experiment_workspace, change_working_directory, write_file, change_dir, memory_usage, add_console_handler, print_separation_line\n-from .src.preprocessing import InstanceData, PreprocessingData, StateFinder, compute_instance_datas, compute_tuple_graphs\n-\n-def learn_sketch_for_problem_class(\n-    domain_filepath: Path,\n-    problems_directory: Path,\n-    workspace: Path,\n-    width: int,\n-    disable_closed_Q: bool = True,\n-    max_num_states_per_instance: int = 10000,\n-    max_time_per_instance: int = 10,\n-    encoding_type: EncodingType = EncodingType.EXPLICIT,\n-    max_num_rules: int = 1,\n-    enable_goal_separating_features: bool = True,\n-    disable_feature_generation: bool = True,\n-    enable_incomplete_feature_pruning: bool = False,\n-    concept_complexity_limit: int = 9,\n-    role_complexity_limit: int = 9,\n-    boolean_complexity_limit: int = 9,\n-    count_numerical_complexity_limit: int = 9,\n-    distance_numerical_complexity_limit: int = 9,\n-    feature_limit: int = 1000000,\n-    additional_booleans: List[str] = None,\n-    additional_numericals: List[str] = None,\n-    enable_dump_files: bool = False,\n-):\n-    # Initialize variables and setup\n-    if additional_booleans is None:\n-        additional_booleans = []\n-    if additional_numericals is None:\n-        additional_numericals = []\n-    instance_filepaths = list(problems_directory.iterdir())\n-   \n-\n-    add_console_handler(logging.getLogger(), logging.INFO)\n-    create_experiment_workspace(workspace)\n-    change_working_directory(workspace)\n-\n-    total_timer = Timer()\n-    preprocessing_timer = Timer()\n-    asp_timer = Timer(stopped=True)\n-    verification_timer = Timer(stopped=True)\n-    iteration_data = IterationData()\n-\n-    # Generate data\n-    with change_dir(\"input\"):\n-        logging.info(colored(\"Constructing InstanceDatas...\", \"blue\", \"on_grey\"))\n-        domain_data, instance_datas, num_ss_states, num_gfa_states = compute_instance_datas(domain_filepath, instance_filepaths, disable_closed_Q, max_num_states_per_instance, max_time_per_instance, enable_dump_files)\n-        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-        if instance_datas is None:\n-            raise Exception(\"Failed to create InstanceDatas.\")\n-\n-        state_finder = StateFinder(domain_data, instance_datas)\n-\n-        logging.info(colored(\"Initializing TupleGraphs...\", \"blue\", \"on_grey\"))\n-        gfa_state_id_to_tuple_graph: Dict[int, mm.TupleGraph] = compute_tuple_graphs(domain_data, instance_datas, state_finder, width, enable_dump_files)\n-        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-    preprocessing_data = PreprocessingData(domain_data, instance_datas, state_finder, gfa_state_id_to_tuple_graph)\n-    preprocessing_timer.stop()\n-\n-    if not preprocessing_data.instance_datas:\n-        raise RuntimeError(\"Data is empty\")\n-\n-    sketches = set()\n-    total_features = set()\n-    sketch_features = set()\n-    # Learn sketch\n-    if encoding_type == EncodingType.EXPLICIT:\n-        create_experiment_workspace(workspace)\n-        preprocessing_timer.resume()\n-\n-        for instance_data in preprocessing_data.instance_datas:\n-            print(f\"Instance {instance_data.idx}:\")\n-            for state in instance_data.mimir_ss.get_states():\n-                    state_data = state.get_state()\n-                    fluents = state_data.get_fluent_atoms()\n-                    #print(f\"State {state.get_index()} fluents: {fluents}\")\n-            # TODO: when is the best time to generate features?\n-            # Based on current instance currently looks most reasonable.\n-            # It could be restricted to the states in the tuple graph as well,\n-            # which reduces the number of rules, but not fully sure yet about the consequences in the bigger picture.\n-            iteration_data = IterationData()\n-            iteration_data.instance_datas = [instance_data]\n-            iteration_data.gfa_states = instance_data.gfa.get_states()\n-\n-            # Report some progress\n-            write_file(f\"{instance_data.idx}.dot\", instance_data.dlplan_ss.to_dot(1))\n-            print(\"     \", end=\"\")\n-            print(\"id:\", instance_data.idx,\n-                    \"problem_filepath:\", instance_data.mimir_ss.get_problem().get_filepath(),\n-                    \"num_states:\", instance_data.mimir_ss.get_num_states(),\n-                    \"num_state_equivalences:\", instance_data.gfa.get_num_states())\n-\n-            logging.info(colored(\"Initializing DomainFeatureData...\", \"blue\", \"on_grey\"))\n-            iteration_data.feature_pool = compute_feature_pool(\n-                preprocessing_data,\n-                iteration_data,\n-                gfa_state_id_to_tuple_graph,\n-                state_finder,\n-                disable_feature_generation,\n-                enable_incomplete_feature_pruning,\n-                concept_complexity_limit,\n-                role_complexity_limit,\n-                boolean_complexity_limit,\n-                count_numerical_complexity_limit,\n-                distance_numerical_complexity_limit,\n-                feature_limit,\n-                additional_booleans,\n-                additional_numericals\n-            )\n-            # Add features to features that were generated\n-            total_features.update(feature.dlplan_feature for feature in iteration_data.feature_pool)\n-\n-            logging.info(colored(\"Constructing PerStateFeatureValuations...\", \"blue\", \"on_grey\"))\n-            iteration_data.gfa_state_global_idx_to_feature_evaluations = compute_per_state_feature_valuations(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Constructing StatePairEquivalenceDatas...\", \"blue\", \"on_grey\"))\n-            iteration_data.state_pair_equivalences, iteration_data.gfa_state_global_idx_to_state_pair_equivalence = compute_state_pair_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Constructing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n-            iteration_data.gfa_state_global_idx_to_tuple_graph_equivalence = compute_tuple_graph_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Minimizing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n-            minimize_tuple_graph_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-            preprocessing_timer.stop()\n-            asp_timer.resume()\n-            for gfa_state_id, tuple_graph in gfa_state_id_to_tuple_graph.items():\n-                states_in_graph = tuple_graph.get_state_space()\n-                print(f\"GFA State ID: {gfa_state_id} -> States in Tuple Graph: {states_in_graph}\")\n-            # We count the number of subgoal tuples for which no rule could be found.\n-            # This usually happens if the pool of features is not sufficiently rich.\n-            count_unsat_tuples = 0\n-            tuple_graph_states = set()\n-           \n-\n-            for gfa_state in instance_data.gfa.get_states():\n-                gfa_state_global_idx = gfa_state.get_global_index()\n-                gfa_state_idx = gfa_state.get_index()\n-\n-                if instance_data.gfa.is_deadend_state(gfa_state_idx):\n-                    continue\n-\n-                #print(f\"Keys in gfa_state_global_idx_to_tuple_graph: {list(preprocessing_data.gfa_state_global_idx_to_tuple_graph.keys())}\")\n-                #print(f\"gfa_state_global_idx: {gfa_state_global_idx}\")\n-\n-                tuple_graph = preprocessing_data.gfa_state_global_idx_to_tuple_graph[gfa_state_global_idx]\n-\n-                for distance, group in enumerate(tuple_graph.get_vertices_grouped_by_distance()):\n-                    if distance == 0:\n-                        # We skip subgoal tuples at distance zero because they do not encode progress towards a goal.\n-                        continue\n-                    for vertex in group:\n-                        # Here we find all simplest single sketch rules for a pair (state, subgoal tuple).\n-                        t_idx = vertex.get_index()\n-                        tuple_graph_states.add((gfa_state_global_idx, t_idx))\n-    \n-                        start_time = time.time()\n-                        asp_factory = ASPFactory(encoding_type, enable_goal_separating_features, max_num_rules)\n-                        facts = asp_factory.make_facts(preprocessing_data, iteration_data, gfa_state, t_idx)\n-\n-                        print(f\"Factory time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        start_time = time.time()\n-                        asp_factory.ground(facts)\n-                        print(f\"Grounding time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        start_time = time.time()\n-                        symbolss, returncode = asp_factory.solve_all_opt()\n-                        print(f\"Solving time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        if returncode in [ClingoExitCode.UNSATISFIABLE, ClingoExitCode.EXHAUSTED]:\n-                            print(colored(\"ASP is unsatisfiable or exhausted!\", \"red\", \"on_grey\"))\n-                            print(colored(f\"No sketch of width {width} exists that solves all instances!\", \"red\", \"on_grey\"))\n-                            # There might be tuples where we get unsat.\n-                            # If such cases occur, which is very likely in complex domains, then we must look closer into this.\n-                            count_unsat_tuples += 1\n-                            continue\n-                        elif returncode == ClingoExitCode.UNKNOWN:\n-                            print(colored(\"ASP solving throws unknown error!\", \"red\", \"on_grey\"))\n-                            exit(ExitCode.UNKNOWN)\n-                        elif returncode == ClingoExitCode.INTERRUPTED:\n-                            print(colored(\"ASP solving interrupted!\", \"red\", \"on_grey\"))\n-                            exit(ExitCode.INTERRUPTED)\n-                        else:\n-                            logging.info(f\"Solution found with return code: {returncode}\")\n-\n-                        for symbols in symbolss:\n-                            dlplan_policy = ExplicitDlplanPolicyFactory().make_dlplan_policy_from_answer_set(symbols, preprocessing_data, iteration_data)\n-\n-                            sketch = Sketch(dlplan_policy, width)\n-                            sketches.add(sketch)\n-                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_booleans())\n-                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_numericals())\n-                            print(dlplan_policy)\n-                            \n-\n-    else:\n-        raise Exception(\"No implementation for the given encoding type.\")\n-\n-    preprocessing_timer.stop()\n-    asp_timer.stop()\n-    verification_timer.stop()\n-    total_timer.stop()\n-\n-    print(\"Comparison between Tuple Graph States and State Pair Equivalences:\")\n-    for (s_i, s_j) in tuple_graph_states:\n-        match_found = False\n-        for gfa_state_global_idx, state_pair_equivalence in iteration_data.gfa_state_global_idx_to_state_pair_equivalence.items():\n-            for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n-                for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n-                    if gfa_state_global_idx == s_i and subgoal_gfa_state_global_idx == s_j:\n-                        print(f\" ({s_i} -> {s_j}) matches with state pair equivalence.\")\n-                        match_found = True\n-                        break\n-                if match_found:\n-                    break\n-\n-        if not match_found:\n-            print(f\" ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n-    # Compute feature histograms by complexity\n-    total_features_by_complexity = defaultdict(int)\n-    for feature in total_features:\n-        total_features_by_complexity[feature.compute_complexity()] += 1\n-    total_features_by_complexity = {key: total_features_by_complexity[key] for key in sorted(total_features_by_complexity)}\n-\n-    sketch_features_by_complexity = defaultdict(int)\n-    for feature in sketch_features:\n-        sketch_features_by_complexity[feature.compute_complexity()] += 1\n-    sketch_features_by_complexity = {key: sketch_features_by_complexity[key] for key in sorted(sketch_features_by_complexity)}\n-\n-    # Output the result\n-    with change_dir(\"output\"):\n-        print_separation_line()\n-        logging.info(colored(\"Summary:\", \"green\", \"on_grey\"))\n-\n-        print(f\"Preprocessing time: {int(preprocessing_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"ASP time: {int(asp_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Verification time: {int(verification_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Total time: {int(total_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Total memory: {int(memory_usage() / 1024)} GiB.\")\n-        print(f\"Total number of states: {num_ss_states}\")\n-        print(f\"Total number of abstract states: {num_gfa_states}\")\n-        print(f\"Number of unsat tuples: {count_unsat_tuples}\")\n-        print(f\"Number of sketch rules: {len(sketches)}\")\n-        print(f\"Number of total features: {len(total_features)}\")\n-        print(f\"Number of sketch features: {len(sketch_features)}\")\n-        print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n-        print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n"
                },
                {
                    "date": 1727881135034,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -142,9 +142,9 @@\n             preprocessing_timer.stop()\n             asp_timer.resume()\n             for gfa_state_id, tuple_graph in gfa_state_id_to_tuple_graph.items():\n                 states_in_graph = tuple_graph.get_state_space()\n-                print(f\"GFA State ID: {gfa_state_id} -> States in Tuple Graph: {states_in_graph}\")\n+                #print(f\"GFA State ID: {gfa_state_id} -> States in Tuple Graph: {states_in_graph}\")\n             # We count the number of subgoal tuples for which no rule could be found.\n             # This usually happens if the pool of features is not sufficiently rich.\n             count_unsat_tuples = 0\n             tuple_graph_states = set()\n"
                },
                {
                    "date": 1727881165852,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -233,8 +233,28 @@\n                     break\n \n         if not match_found:\n             print(f\" ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n+            \n+        inverse_pairs = set()\n+\n+        # Populate inverse pairs from state pair equivalences\n+        for gfa_state_global_idx, state_pair_equivalence in iteration_data.gfa_state_global_idx_to_state_pair_equivalence.items():\n+            for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n+                for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n+                    inverse_pairs.add((gfa_state_global_idx, subgoal_gfa_state_global_idx))\n+                    inverse_pairs.add((subgoal_gfa_state_global_idx, gfa_state_global_idx))\n+\n+        # Filter states in the tuple graph\n+        filtered_tuple_graph_states = set()\n+        for (s_i, s_j) in tuple_graph_states:\n+            if (s_i, s_j) not in inverse_pairs:\n+                filtered_tuple_graph_states.add((s_i, s_j))\n+\n+        # Update your tuple graph or log filtered states\n+        print(\"Filtered Tuple Graph States (without inverse pairs):\")\n+        for state in filtered_tuple_graph_states:\n+            print(state)\n     # Compute feature histograms by complexity\n     total_features_by_complexity = defaultdict(int)\n     for feature in total_features:\n         total_features_by_complexity[feature.compute_complexity()] += 1\n"
                },
                {
                    "date": 1727881174890,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -236,9 +236,8 @@\n             print(f\" ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n             \n         inverse_pairs = set()\n \n-        # Populate inverse pairs from state pair equivalences\n         for gfa_state_global_idx, state_pair_equivalence in iteration_data.gfa_state_global_idx_to_state_pair_equivalence.items():\n             for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n                 for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n                     inverse_pairs.add((gfa_state_global_idx, subgoal_gfa_state_global_idx))\n"
                },
                {
                    "date": 1727881182426,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -242,9 +242,8 @@\n                 for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n                     inverse_pairs.add((gfa_state_global_idx, subgoal_gfa_state_global_idx))\n                     inverse_pairs.add((subgoal_gfa_state_global_idx, gfa_state_global_idx))\n \n-        # Filter states in the tuple graph\n         filtered_tuple_graph_states = set()\n         for (s_i, s_j) in tuple_graph_states:\n             if (s_i, s_j) not in inverse_pairs:\n                 filtered_tuple_graph_states.add((s_i, s_j))\n"
                },
                {
                    "date": 1727881189594,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -246,10 +246,9 @@\n         filtered_tuple_graph_states = set()\n         for (s_i, s_j) in tuple_graph_states:\n             if (s_i, s_j) not in inverse_pairs:\n                 filtered_tuple_graph_states.add((s_i, s_j))\n-\n-        # Update your tuple graph or log filtered states\n+                \n         print(\"Filtered Tuple Graph States (without inverse pairs):\")\n         for state in filtered_tuple_graph_states:\n             print(state)\n     # Compute feature histograms by complexity\n"
                },
                {
                    "date": 1727881209465,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -247,9 +247,9 @@\n         for (s_i, s_j) in tuple_graph_states:\n             if (s_i, s_j) not in inverse_pairs:\n                 filtered_tuple_graph_states.add((s_i, s_j))\n                 \n-        print(\"Filtered Tuple Graph States (without inverse pairs):\")\n+        print(\"Tuple Graph States (without inverse pairs):\")\n         for state in filtered_tuple_graph_states:\n             print(state)\n     # Compute feature histograms by complexity\n     total_features_by_complexity = defaultdict(int)\n"
                },
                {
                    "date": 1727881245548,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -247,9 +247,9 @@\n         for (s_i, s_j) in tuple_graph_states:\n             if (s_i, s_j) not in inverse_pairs:\n                 filtered_tuple_graph_states.add((s_i, s_j))\n                 \n-        print(\"Tuple Graph States (without inverse pairs):\")\n+        print(\"Tuple Graph States without inverse pairs:\")\n         for state in filtered_tuple_graph_states:\n             print(state)\n     # Compute feature histograms by complexity\n     total_features_by_complexity = defaultdict(int)\n"
                },
                {
                    "date": 1727881281829,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -225,16 +225,16 @@\n         for gfa_state_global_idx, state_pair_equivalence in iteration_data.gfa_state_global_idx_to_state_pair_equivalence.items():\n             for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n                 for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n                     if gfa_state_global_idx == s_i and subgoal_gfa_state_global_idx == s_j:\n-                        print(f\" ({s_i} -> {s_j}) matches with state pair equivalence.\")\n+                        #print(f\" ({s_i} -> {s_j}) matches with state pair equivalence.\")\n                         match_found = True\n                         break\n                 if match_found:\n                     break\n \n-        if not match_found:\n-            print(f\" ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n+        #if not match_found:\n+            #print(f\" ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n             \n         inverse_pairs = set()\n \n         for gfa_state_global_idx, state_pair_equivalence in iteration_data.gfa_state_global_idx_to_state_pair_equivalence.items():\n"
                },
                {
                    "date": 1727881407785,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -248,9 +248,9 @@\n             if (s_i, s_j) not in inverse_pairs:\n                 filtered_tuple_graph_states.add((s_i, s_j))\n                 \n         print(\"Tuple Graph States without inverse pairs:\")\n-        for state in filtered_tuple_graph_states:\n+        if state in filtered_tuple_graph_states:\n             print(state)\n     # Compute feature histograms by complexity\n     total_features_by_complexity = defaultdict(int)\n     for feature in total_features:\n"
                },
                {
                    "date": 1727881426134,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -248,9 +248,9 @@\n             if (s_i, s_j) not in inverse_pairs:\n                 filtered_tuple_graph_states.add((s_i, s_j))\n                 \n         print(\"Tuple Graph States without inverse pairs:\")\n-        if state in filtered_tuple_graph_states:\n+        for state in filtered_tuple_graph_states:\n             print(state)\n     # Compute feature histograms by complexity\n     total_features_by_complexity = defaultdict(int)\n     for feature in total_features:\n"
                },
                {
                    "date": 1727881468976,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -247,11 +247,14 @@\n         for (s_i, s_j) in tuple_graph_states:\n             if (s_i, s_j) not in inverse_pairs:\n                 filtered_tuple_graph_states.add((s_i, s_j))\n                 \n-        print(\"Tuple Graph States without inverse pairs:\")\n-        for state in filtered_tuple_graph_states:\n-            print(state)\n+        if filtered_tuple_graph_states:\n+            print(\"Filtered Tuple Graph States (without inverse pairs):\")\n+            for state in filtered_tuple_graph_states:\n+                print(state)\n+        else:\n+            print(\"No valid tuple graph states found without inverse pairs.\")\n     # Compute feature histograms by complexity\n     total_features_by_complexity = defaultdict(int)\n     for feature in total_features:\n         total_features_by_complexity[feature.compute_complexity()] += 1\n"
                },
                {
                    "date": 1727881485455,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -247,14 +247,11 @@\n         for (s_i, s_j) in tuple_graph_states:\n             if (s_i, s_j) not in inverse_pairs:\n                 filtered_tuple_graph_states.add((s_i, s_j))\n                 \n-        if filtered_tuple_graph_states:\n-            print(\"Filtered Tuple Graph States (without inverse pairs):\")\n-            for state in filtered_tuple_graph_states:\n-                print(state)\n-        else:\n-            print(\"No valid tuple graph states found without inverse pairs.\")\n+        print(\"Tuple Graph States without inverse pairs:\")\n+        for state in filtered_tuple_graph_states:\n+            print(state)\n     # Compute feature histograms by complexity\n     total_features_by_complexity = defaultdict(int)\n     for feature in total_features:\n         total_features_by_complexity[feature.compute_complexity()] += 1\n"
                },
                {
                    "date": 1727881620161,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -242,15 +242,19 @@\n                 for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n                     inverse_pairs.add((gfa_state_global_idx, subgoal_gfa_state_global_idx))\n                     inverse_pairs.add((subgoal_gfa_state_global_idx, gfa_state_global_idx))\n \n-        filtered_tuple_graph_states = set()\n+        tuple_graph_states_without = set()\n+        tuple_graph_states_with_inverse = []\n+       \n+                \n         for (s_i, s_j) in tuple_graph_states:\n             if (s_i, s_j) not in inverse_pairs:\n-                filtered_tuple_graph_states.add((s_i, s_j))\n-                \n+                tuple_graph_states_without.add((s_i, s_j))\n+            else:\n+                tuple_graph_states_with_inverse.append((s_i, s_j))\n         print(\"Tuple Graph States without inverse pairs:\")\n-        for state in filtered_tuple_graph_states:\n+        for state in tuple_graph_states_without:\n             print(state)\n     # Compute feature histograms by complexity\n     total_features_by_complexity = defaultdict(int)\n     for feature in total_features:\n"
                },
                {
                    "date": 1727881625527,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -251,8 +251,9 @@\n             if (s_i, s_j) not in inverse_pairs:\n                 tuple_graph_states_without.add((s_i, s_j))\n             else:\n                 tuple_graph_states_with_inverse.append((s_i, s_j))\n+                \n         print(\"Tuple Graph States without inverse pairs:\")\n         for state in tuple_graph_states_without:\n             print(state)\n     # Compute feature histograms by complexity\n"
                },
                {
                    "date": 1727881744541,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -253,10 +253,20 @@\n             else:\n                 tuple_graph_states_with_inverse.append((s_i, s_j))\n                 \n         print(\"Tuple Graph States without inverse pairs:\")\n-        for state in tuple_graph_states_without:\n-            print(state)\n+        if tuple_graph_states_without:\n+            for state in tuple_graph_states_without:\n+                print(state)\n+        else:\n+            print(\"No tuple graph states without inverse pairs.\")\n+\n+        print(\"\\nTuple Graph States with Inverse Pairs:\")\n+        if tuple_graph_states_with_inverse:\n+            for state in tuple_graph_states_with_inverse:\n+                print(state)\n+        else:\n+            print(\"No tuple graph states with inverse pairs.\")\n     # Compute feature histograms by complexity\n     total_features_by_complexity = defaultdict(int)\n     for feature in total_features:\n         total_features_by_complexity[feature.compute_complexity()] += 1\n"
                },
                {
                    "date": 1727881750282,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -265,8 +265,9 @@\n             for state in tuple_graph_states_with_inverse:\n                 print(state)\n         else:\n             print(\"No tuple graph states with inverse pairs.\")\n+            \n     # Compute feature histograms by complexity\n     total_features_by_complexity = defaultdict(int)\n     for feature in total_features:\n         total_features_by_complexity[feature.compute_complexity()] += 1\n"
                },
                {
                    "date": 1727881797413,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -239,9 +239,9 @@\n \n         for gfa_state_global_idx, state_pair_equivalence in iteration_data.gfa_state_global_idx_to_state_pair_equivalence.items():\n             for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n                 for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n-                    inverse_pairs.add((gfa_state_global_idx, subgoal_gfa_state_global_idx))\n+                    #inverse_pairs.add((gfa_state_global_idx, subgoal_gfa_state_global_idx))\n                     inverse_pairs.add((subgoal_gfa_state_global_idx, gfa_state_global_idx))\n \n         tuple_graph_states_without = set()\n         tuple_graph_states_with_inverse = []\n"
                },
                {
                    "date": 1727881816534,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,298 @@\n+import logging\n+import time\n+from collections import defaultdict\n+from pathlib import Path\n+from termcolor import colored\n+from typing import List, MutableSet, Dict\n+import clingo\n+import pymimir as mm\n+from dlplan.policy import PolicyMinimizer, NamedBoolean, NamedNumerical\n+from collections import defaultdict\n+from .src.exit_codes import ExitCode\n+from .src.iteration import EncodingType, ASPFactory, ClingoExitCode, IterationData, LearningStatistics, Sketch, D2sepDlplanPolicyFactory, ExplicitDlplanPolicyFactory, compute_feature_pool, compute_per_state_feature_valuations, compute_state_pair_equivalences, compute_tuple_graph_equivalences, minimize_tuple_graph_equivalences\n+from .src.util import Timer, create_experiment_workspace, change_working_directory, write_file, change_dir, memory_usage, add_console_handler, print_separation_line\n+from .src.preprocessing import InstanceData, PreprocessingData, StateFinder, compute_instance_datas, compute_tuple_graphs\n+\n+def learn_sketch_for_problem_class(\n+    domain_filepath: Path,\n+    problems_directory: Path,\n+    workspace: Path,\n+    width: int,\n+    disable_closed_Q: bool = True,\n+    max_num_states_per_instance: int = 10000,\n+    max_time_per_instance: int = 10,\n+    encoding_type: EncodingType = EncodingType.EXPLICIT,\n+    max_num_rules: int = 1,\n+    enable_goal_separating_features: bool = True,\n+    disable_feature_generation: bool = True,\n+    enable_incomplete_feature_pruning: bool = False,\n+    concept_complexity_limit: int = 9,\n+    role_complexity_limit: int = 9,\n+    boolean_complexity_limit: int = 9,\n+    count_numerical_complexity_limit: int = 9,\n+    distance_numerical_complexity_limit: int = 9,\n+    feature_limit: int = 1000000,\n+    additional_booleans: List[str] = None,\n+    additional_numericals: List[str] = None,\n+    enable_dump_files: bool = False,\n+):\n+    # Initialize variables and setup\n+    if additional_booleans is None:\n+        additional_booleans = []\n+    if additional_numericals is None:\n+        additional_numericals = []\n+    instance_filepaths = list(problems_directory.iterdir())\n+   \n+\n+    add_console_handler(logging.getLogger(), logging.INFO)\n+    create_experiment_workspace(workspace)\n+    change_working_directory(workspace)\n+\n+    total_timer = Timer()\n+    preprocessing_timer = Timer()\n+    asp_timer = Timer(stopped=True)\n+    verification_timer = Timer(stopped=True)\n+    iteration_data = IterationData()\n+\n+    # Generate data\n+    with change_dir(\"input\"):\n+        logging.info(colored(\"Constructing InstanceDatas...\", \"blue\", \"on_grey\"))\n+        domain_data, instance_datas, num_ss_states, num_gfa_states = compute_instance_datas(domain_filepath, instance_filepaths, disable_closed_Q, max_num_states_per_instance, max_time_per_instance, enable_dump_files)\n+        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+        if instance_datas is None:\n+            raise Exception(\"Failed to create InstanceDatas.\")\n+\n+        state_finder = StateFinder(domain_data, instance_datas)\n+\n+        logging.info(colored(\"Initializing TupleGraphs...\", \"blue\", \"on_grey\"))\n+        gfa_state_id_to_tuple_graph: Dict[int, mm.TupleGraph] = compute_tuple_graphs(domain_data, instance_datas, state_finder, width, enable_dump_files)\n+        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+    preprocessing_data = PreprocessingData(domain_data, instance_datas, state_finder, gfa_state_id_to_tuple_graph)\n+    preprocessing_timer.stop()\n+\n+    if not preprocessing_data.instance_datas:\n+        raise RuntimeError(\"Data is empty\")\n+\n+    sketches = set()\n+    total_features = set()\n+    sketch_features = set()\n+    # Learn sketch\n+    if encoding_type == EncodingType.EXPLICIT:\n+        create_experiment_workspace(workspace)\n+        preprocessing_timer.resume()\n+\n+        for instance_data in preprocessing_data.instance_datas:\n+            print(f\"Instance {instance_data.idx}:\")\n+            for state in instance_data.mimir_ss.get_states():\n+                    state_data = state.get_state()\n+                    fluents = state_data.get_fluent_atoms()\n+                    #print(f\"State {state.get_index()} fluents: {fluents}\")\n+            # TODO: when is the best time to generate features?\n+            # Based on current instance currently looks most reasonable.\n+            # It could be restricted to the states in the tuple graph as well,\n+            # which reduces the number of rules, but not fully sure yet about the consequences in the bigger picture.\n+            iteration_data = IterationData()\n+            iteration_data.instance_datas = [instance_data]\n+            iteration_data.gfa_states = instance_data.gfa.get_states()\n+\n+            # Report some progress\n+            write_file(f\"{instance_data.idx}.dot\", instance_data.dlplan_ss.to_dot(1))\n+            print(\"     \", end=\"\")\n+            print(\"id:\", instance_data.idx,\n+                    \"problem_filepath:\", instance_data.mimir_ss.get_problem().get_filepath(),\n+                    \"num_states:\", instance_data.mimir_ss.get_num_states(),\n+                    \"num_state_equivalences:\", instance_data.gfa.get_num_states())\n+\n+            logging.info(colored(\"Initializing DomainFeatureData...\", \"blue\", \"on_grey\"))\n+            iteration_data.feature_pool = compute_feature_pool(\n+                preprocessing_data,\n+                iteration_data,\n+                gfa_state_id_to_tuple_graph,\n+                state_finder,\n+                disable_feature_generation,\n+                enable_incomplete_feature_pruning,\n+                concept_complexity_limit,\n+                role_complexity_limit,\n+                boolean_complexity_limit,\n+                count_numerical_complexity_limit,\n+                distance_numerical_complexity_limit,\n+                feature_limit,\n+                additional_booleans,\n+                additional_numericals\n+            )\n+            # Add features to features that were generated\n+            total_features.update(feature.dlplan_feature for feature in iteration_data.feature_pool)\n+\n+            logging.info(colored(\"Constructing PerStateFeatureValuations...\", \"blue\", \"on_grey\"))\n+            iteration_data.gfa_state_global_idx_to_feature_evaluations = compute_per_state_feature_valuations(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Constructing StatePairEquivalenceDatas...\", \"blue\", \"on_grey\"))\n+            iteration_data.state_pair_equivalences, iteration_data.gfa_state_global_idx_to_state_pair_equivalence = compute_state_pair_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Constructing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n+            iteration_data.gfa_state_global_idx_to_tuple_graph_equivalence = compute_tuple_graph_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Minimizing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n+            minimize_tuple_graph_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+            preprocessing_timer.stop()\n+            asp_timer.resume()\n+            for gfa_state_id, tuple_graph in gfa_state_id_to_tuple_graph.items():\n+                states_in_graph = tuple_graph.get_state_space()\n+                #print(f\"GFA State ID: {gfa_state_id} -> States in Tuple Graph: {states_in_graph}\")\n+            # We count the number of subgoal tuples for which no rule could be found.\n+            # This usually happens if the pool of features is not sufficiently rich.\n+            count_unsat_tuples = 0\n+            tuple_graph_states = set()\n+           \n+\n+            for gfa_state in instance_data.gfa.get_states():\n+                gfa_state_global_idx = gfa_state.get_global_index()\n+                gfa_state_idx = gfa_state.get_index()\n+\n+                if instance_data.gfa.is_deadend_state(gfa_state_idx):\n+                    continue\n+\n+                #print(f\"Keys in gfa_state_global_idx_to_tuple_graph: {list(preprocessing_data.gfa_state_global_idx_to_tuple_graph.keys())}\")\n+                #print(f\"gfa_state_global_idx: {gfa_state_global_idx}\")\n+\n+                tuple_graph = preprocessing_data.gfa_state_global_idx_to_tuple_graph[gfa_state_global_idx]\n+\n+                for distance, group in enumerate(tuple_graph.get_vertices_grouped_by_distance()):\n+                    if distance == 0:\n+                        # We skip subgoal tuples at distance zero because they do not encode progress towards a goal.\n+                        continue\n+                    for vertex in group:\n+                        # Here we find all simplest single sketch rules for a pair (state, subgoal tuple).\n+                        t_idx = vertex.get_index()\n+                        tuple_graph_states.add((gfa_state_global_idx, t_idx))\n+    \n+                        start_time = time.time()\n+                        asp_factory = ASPFactory(encoding_type, enable_goal_separating_features, max_num_rules)\n+                        facts = asp_factory.make_facts(preprocessing_data, iteration_data, gfa_state, t_idx)\n+\n+                        print(f\"Factory time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        start_time = time.time()\n+                        asp_factory.ground(facts)\n+                        print(f\"Grounding time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        start_time = time.time()\n+                        symbolss, returncode = asp_factory.solve_all_opt()\n+                        print(f\"Solving time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        if returncode in [ClingoExitCode.UNSATISFIABLE, ClingoExitCode.EXHAUSTED]:\n+                            print(colored(\"ASP is unsatisfiable or exhausted!\", \"red\", \"on_grey\"))\n+                            print(colored(f\"No sketch of width {width} exists that solves all instances!\", \"red\", \"on_grey\"))\n+                            # There might be tuples where we get unsat.\n+                            # If such cases occur, which is very likely in complex domains, then we must look closer into this.\n+                            count_unsat_tuples += 1\n+                            continue\n+                        elif returncode == ClingoExitCode.UNKNOWN:\n+                            print(colored(\"ASP solving throws unknown error!\", \"red\", \"on_grey\"))\n+                            exit(ExitCode.UNKNOWN)\n+                        elif returncode == ClingoExitCode.INTERRUPTED:\n+                            print(colored(\"ASP solving interrupted!\", \"red\", \"on_grey\"))\n+                            exit(ExitCode.INTERRUPTED)\n+                        else:\n+                            logging.info(f\"Solution found with return code: {returncode}\")\n+\n+                        for symbols in symbolss:\n+                            dlplan_policy = ExplicitDlplanPolicyFactory().make_dlplan_policy_from_answer_set(symbols, preprocessing_data, iteration_data)\n+\n+                            sketch = Sketch(dlplan_policy, width)\n+                            sketches.add(sketch)\n+                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_booleans())\n+                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_numericals())\n+                            print(dlplan_policy)\n+                            \n+\n+    else:\n+        raise Exception(\"No implementation for the given encoding type.\")\n+\n+    preprocessing_timer.stop()\n+    asp_timer.stop()\n+    verification_timer.stop()\n+    total_timer.stop()\n+\n+    print(\"Comparison between Tuple Graph States and State Pair Equivalences:\")\n+    for (s_i, s_j) in tuple_graph_states:\n+        match_found = False\n+        for gfa_state_global_idx, state_pair_equivalence in iteration_data.gfa_state_global_idx_to_state_pair_equivalence.items():\n+            for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n+                for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n+                    if gfa_state_global_idx == s_i and subgoal_gfa_state_global_idx == s_j:\n+                        #print(f\" ({s_i} -> {s_j}) matches with state pair equivalence.\")\n+                        match_found = True\n+                        break\n+                if match_found:\n+                    break\n+\n+        #if not match_found:\n+            #print(f\" ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n+            \n+        inverse_pairs = set()\n+\n+        for gfa_state_global_idx, state_pair_equivalence in iteration_data.gfa_state_global_idx_to_state_pair_equivalence.items():\n+            for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n+                for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n+                    inverse_pairs.add((gfa_state_global_idx, subgoal_gfa_state_global_idx))\n+                    inverse_pairs.add((subgoal_gfa_state_global_idx, gfa_state_global_idx))\n+\n+        tuple_graph_states_without = set()\n+        tuple_graph_states_with_inverse = []\n+       \n+                \n+        for (s_i, s_j) in tuple_graph_states:\n+            if (s_i, s_j) not in inverse_pairs:\n+                tuple_graph_states_without.add((s_i, s_j))\n+            else:\n+                tuple_graph_states_with_inverse.append((s_i, s_j))\n+                \n+        print(\"Tuple Graph States without inverse pairs:\")\n+        if tuple_graph_states_without:\n+            for state in tuple_graph_states_without:\n+                print(state)\n+        else:\n+            print(\"No tuple graph states without inverse pairs.\")\n+\n+        print(\"\\nTuple Graph States with Inverse Pairs:\")\n+        if tuple_graph_states_with_inverse:\n+            for state in tuple_graph_states_with_inverse:\n+                print(state)\n+        else:\n+            print(\"No tuple graph states with inverse pairs.\")\n+            \n+    # Compute feature histograms by complexity\n+    total_features_by_complexity = defaultdict(int)\n+    for feature in total_features:\n+        total_features_by_complexity[feature.compute_complexity()] += 1\n+    total_features_by_complexity = {key: total_features_by_complexity[key] for key in sorted(total_features_by_complexity)}\n+\n+    sketch_features_by_complexity = defaultdict(int)\n+    for feature in sketch_features:\n+        sketch_features_by_complexity[feature.compute_complexity()] += 1\n+    sketch_features_by_complexity = {key: sketch_features_by_complexity[key] for key in sorted(sketch_features_by_complexity)}\n+\n+    # Output the result\n+    with change_dir(\"output\"):\n+        print_separation_line()\n+        logging.info(colored(\"Summary:\", \"green\", \"on_grey\"))\n+\n+        print(f\"Preprocessing time: {int(preprocessing_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"ASP time: {int(asp_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Verification time: {int(verification_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Total time: {int(total_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Total memory: {int(memory_usage() / 1024)} GiB.\")\n+        print(f\"Total number of states: {num_ss_states}\")\n+        print(f\"Total number of abstract states: {num_gfa_states}\")\n+        print(f\"Number of unsat tuples: {count_unsat_tuples}\")\n+        print(f\"Number of sketch rules: {len(sketches)}\")\n+        print(f\"Number of total features: {len(total_features)}\")\n+        print(f\"Number of sketch features: {len(sketch_features)}\")\n+        print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n+        print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n"
                },
                {
                    "date": 1727882039081,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -234,39 +234,45 @@\n \n         #if not match_found:\n             #print(f\" ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n             \n-        inverse_pairs = set()\n+       # Create sets for tuple graph states and inverse pairs\n+    inverse_pairs = set()\n+    tuple_graph_states = set()  # Populate this with your existing tuple graph states\n \n-        for gfa_state_global_idx, state_pair_equivalence in iteration_data.gfa_state_global_idx_to_state_pair_equivalence.items():\n-            for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n-                for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n-                    inverse_pairs.add((gfa_state_global_idx, subgoal_gfa_state_global_idx))\n-                    inverse_pairs.add((subgoal_gfa_state_global_idx, gfa_state_global_idx))\n+    # Populate inverse pairs from state pair equivalences\n+    for gfa_state_global_idx, state_pair_equivalence in iteration_data.gfa_state_global_idx_to_state_pair_equivalence.items():\n+        for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n+            for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n+                inverse_pairs.add((gfa_state_global_idx, subgoal_gfa_state_global_idx))\n+                inverse_pairs.add((subgoal_gfa_state_global_idx, gfa_state_global_idx))\n \n-        tuple_graph_states_without = set()\n-        tuple_graph_states_with_inverse = []\n-       \n-                \n-        for (s_i, s_j) in tuple_graph_states:\n-            if (s_i, s_j) not in inverse_pairs:\n-                tuple_graph_states_without.add((s_i, s_j))\n-            else:\n-                tuple_graph_states_with_inverse.append((s_i, s_j))\n-                \n-        print(\"Tuple Graph States without inverse pairs:\")\n-        if tuple_graph_states_without:\n-            for state in tuple_graph_states_without:\n-                print(state)\n+    # Identify filtered states and states with inverse pairs\n+    filtered_tuple_graph_states = set()\n+    tuple_graph_states_with_inverse = set()\n+\n+    for (s_i, s_j) in tuple_graph_states:\n+        # Check if the inverse exists\n+        if (s_j, s_i) in tuple_graph_states:\n+            tuple_graph_states_with_inverse.add((s_i, s_j))\n         else:\n-            print(\"No tuple graph states without inverse pairs.\")\n+            filtered_tuple_graph_states.add((s_i, s_j))\n \n-        print(\"\\nTuple Graph States with Inverse Pairs:\")\n-        if tuple_graph_states_with_inverse:\n-            for state in tuple_graph_states_with_inverse:\n-                print(state)\n-        else:\n-            print(\"No tuple graph states with inverse pairs.\")\n+    # Print results\n+    print(\"Tuple Graph States without inverse pairs:\")\n+    if filtered_tuple_graph_states:\n+        for state in filtered_tuple_graph_states:\n+            print(state)\n+    else:\n+        print(\"No valid tuple graph states found without inverse pairs.\")\n+\n+    print(\"\\nTuple Graph States with Inverse Pairs:\")\n+    if tuple_graph_states_with_inverse:\n+        for state in tuple_graph_states_with_inverse:\n+            print(state)\n+    else:\n+        print(\"No tuple graph states found with inverse pairs.\")\n+\n             \n     # Compute feature histograms by complexity\n     total_features_by_complexity = defaultdict(int)\n     for feature in total_features:\n"
                },
                {
                    "date": 1727882066340,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -245,333 +245,35 @@\n             for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n                 inverse_pairs.add((gfa_state_global_idx, subgoal_gfa_state_global_idx))\n                 inverse_pairs.add((subgoal_gfa_state_global_idx, gfa_state_global_idx))\n \n-    # Identify filtered states and states with inverse pairs\n-    filtered_tuple_graph_states = set()\n-    tuple_graph_states_with_inverse = set()\n+        # Identify filtered states and states with inverse pairs\n+        filtered_tuple_graph_states = set()\n+        tuple_graph_states_with_inverse = set()\n \n-    for (s_i, s_j) in tuple_graph_states:\n-        # Check if the inverse exists\n-        if (s_j, s_i) in tuple_graph_states:\n-            tuple_graph_states_with_inverse.add((s_i, s_j))\n-        else:\n-            filtered_tuple_graph_states.add((s_i, s_j))\n-\n-    # Print results\n-    print(\"Tuple Graph States without inverse pairs:\")\n-    if filtered_tuple_graph_states:\n-        for state in filtered_tuple_graph_states:\n-            print(state)\n-    else:\n-        print(\"No valid tuple graph states found without inverse pairs.\")\n-\n-    print(\"\\nTuple Graph States with Inverse Pairs:\")\n-    if tuple_graph_states_with_inverse:\n-        for state in tuple_graph_states_with_inverse:\n-            print(state)\n-    else:\n-        print(\"No tuple graph states found with inverse pairs.\")\n-\n-            \n-    # Compute feature histograms by complexity\n-    total_features_by_complexity = defaultdict(int)\n-    for feature in total_features:\n-        total_features_by_complexity[feature.compute_complexity()] += 1\n-    total_features_by_complexity = {key: total_features_by_complexity[key] for key in sorted(total_features_by_complexity)}\n-\n-    sketch_features_by_complexity = defaultdict(int)\n-    for feature in sketch_features:\n-        sketch_features_by_complexity[feature.compute_complexity()] += 1\n-    sketch_features_by_complexity = {key: sketch_features_by_complexity[key] for key in sorted(sketch_features_by_complexity)}\n-\n-    # Output the result\n-    with change_dir(\"output\"):\n-        print_separation_line()\n-        logging.info(colored(\"Summary:\", \"green\", \"on_grey\"))\n-\n-        print(f\"Preprocessing time: {int(preprocessing_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"ASP time: {int(asp_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Verification time: {int(verification_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Total time: {int(total_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Total memory: {int(memory_usage() / 1024)} GiB.\")\n-        print(f\"Total number of states: {num_ss_states}\")\n-        print(f\"Total number of abstract states: {num_gfa_states}\")\n-        print(f\"Number of unsat tuples: {count_unsat_tuples}\")\n-        print(f\"Number of sketch rules: {len(sketches)}\")\n-        print(f\"Number of total features: {len(total_features)}\")\n-        print(f\"Number of sketch features: {len(sketch_features)}\")\n-        print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n-        print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n-import logging\n-import time\n-from collections import defaultdict\n-from pathlib import Path\n-from termcolor import colored\n-from typing import List, MutableSet, Dict\n-import clingo\n-import pymimir as mm\n-from dlplan.policy import PolicyMinimizer, NamedBoolean, NamedNumerical\n-from collections import defaultdict\n-from .src.exit_codes import ExitCode\n-from .src.iteration import EncodingType, ASPFactory, ClingoExitCode, IterationData, LearningStatistics, Sketch, D2sepDlplanPolicyFactory, ExplicitDlplanPolicyFactory, compute_feature_pool, compute_per_state_feature_valuations, compute_state_pair_equivalences, compute_tuple_graph_equivalences, minimize_tuple_graph_equivalences\n-from .src.util import Timer, create_experiment_workspace, change_working_directory, write_file, change_dir, memory_usage, add_console_handler, print_separation_line\n-from .src.preprocessing import InstanceData, PreprocessingData, StateFinder, compute_instance_datas, compute_tuple_graphs\n-\n-def learn_sketch_for_problem_class(\n-    domain_filepath: Path,\n-    problems_directory: Path,\n-    workspace: Path,\n-    width: int,\n-    disable_closed_Q: bool = True,\n-    max_num_states_per_instance: int = 10000,\n-    max_time_per_instance: int = 10,\n-    encoding_type: EncodingType = EncodingType.EXPLICIT,\n-    max_num_rules: int = 1,\n-    enable_goal_separating_features: bool = True,\n-    disable_feature_generation: bool = True,\n-    enable_incomplete_feature_pruning: bool = False,\n-    concept_complexity_limit: int = 9,\n-    role_complexity_limit: int = 9,\n-    boolean_complexity_limit: int = 9,\n-    count_numerical_complexity_limit: int = 9,\n-    distance_numerical_complexity_limit: int = 9,\n-    feature_limit: int = 1000000,\n-    additional_booleans: List[str] = None,\n-    additional_numericals: List[str] = None,\n-    enable_dump_files: bool = False,\n-):\n-    # Initialize variables and setup\n-    if additional_booleans is None:\n-        additional_booleans = []\n-    if additional_numericals is None:\n-        additional_numericals = []\n-    instance_filepaths = list(problems_directory.iterdir())\n-   \n-\n-    add_console_handler(logging.getLogger(), logging.INFO)\n-    create_experiment_workspace(workspace)\n-    change_working_directory(workspace)\n-\n-    total_timer = Timer()\n-    preprocessing_timer = Timer()\n-    asp_timer = Timer(stopped=True)\n-    verification_timer = Timer(stopped=True)\n-    iteration_data = IterationData()\n-\n-    # Generate data\n-    with change_dir(\"input\"):\n-        logging.info(colored(\"Constructing InstanceDatas...\", \"blue\", \"on_grey\"))\n-        domain_data, instance_datas, num_ss_states, num_gfa_states = compute_instance_datas(domain_filepath, instance_filepaths, disable_closed_Q, max_num_states_per_instance, max_time_per_instance, enable_dump_files)\n-        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-        if instance_datas is None:\n-            raise Exception(\"Failed to create InstanceDatas.\")\n-\n-        state_finder = StateFinder(domain_data, instance_datas)\n-\n-        logging.info(colored(\"Initializing TupleGraphs...\", \"blue\", \"on_grey\"))\n-        gfa_state_id_to_tuple_graph: Dict[int, mm.TupleGraph] = compute_tuple_graphs(domain_data, instance_datas, state_finder, width, enable_dump_files)\n-        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-    preprocessing_data = PreprocessingData(domain_data, instance_datas, state_finder, gfa_state_id_to_tuple_graph)\n-    preprocessing_timer.stop()\n-\n-    if not preprocessing_data.instance_datas:\n-        raise RuntimeError(\"Data is empty\")\n-\n-    sketches = set()\n-    total_features = set()\n-    sketch_features = set()\n-    # Learn sketch\n-    if encoding_type == EncodingType.EXPLICIT:\n-        create_experiment_workspace(workspace)\n-        preprocessing_timer.resume()\n-\n-        for instance_data in preprocessing_data.instance_datas:\n-            print(f\"Instance {instance_data.idx}:\")\n-            for state in instance_data.mimir_ss.get_states():\n-                    state_data = state.get_state()\n-                    fluents = state_data.get_fluent_atoms()\n-                    #print(f\"State {state.get_index()} fluents: {fluents}\")\n-            # TODO: when is the best time to generate features?\n-            # Based on current instance currently looks most reasonable.\n-            # It could be restricted to the states in the tuple graph as well,\n-            # which reduces the number of rules, but not fully sure yet about the consequences in the bigger picture.\n-            iteration_data = IterationData()\n-            iteration_data.instance_datas = [instance_data]\n-            iteration_data.gfa_states = instance_data.gfa.get_states()\n-\n-            # Report some progress\n-            write_file(f\"{instance_data.idx}.dot\", instance_data.dlplan_ss.to_dot(1))\n-            print(\"     \", end=\"\")\n-            print(\"id:\", instance_data.idx,\n-                    \"problem_filepath:\", instance_data.mimir_ss.get_problem().get_filepath(),\n-                    \"num_states:\", instance_data.mimir_ss.get_num_states(),\n-                    \"num_state_equivalences:\", instance_data.gfa.get_num_states())\n-\n-            logging.info(colored(\"Initializing DomainFeatureData...\", \"blue\", \"on_grey\"))\n-            iteration_data.feature_pool = compute_feature_pool(\n-                preprocessing_data,\n-                iteration_data,\n-                gfa_state_id_to_tuple_graph,\n-                state_finder,\n-                disable_feature_generation,\n-                enable_incomplete_feature_pruning,\n-                concept_complexity_limit,\n-                role_complexity_limit,\n-                boolean_complexity_limit,\n-                count_numerical_complexity_limit,\n-                distance_numerical_complexity_limit,\n-                feature_limit,\n-                additional_booleans,\n-                additional_numericals\n-            )\n-            # Add features to features that were generated\n-            total_features.update(feature.dlplan_feature for feature in iteration_data.feature_pool)\n-\n-            logging.info(colored(\"Constructing PerStateFeatureValuations...\", \"blue\", \"on_grey\"))\n-            iteration_data.gfa_state_global_idx_to_feature_evaluations = compute_per_state_feature_valuations(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Constructing StatePairEquivalenceDatas...\", \"blue\", \"on_grey\"))\n-            iteration_data.state_pair_equivalences, iteration_data.gfa_state_global_idx_to_state_pair_equivalence = compute_state_pair_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Constructing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n-            iteration_data.gfa_state_global_idx_to_tuple_graph_equivalence = compute_tuple_graph_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Minimizing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n-            minimize_tuple_graph_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-            preprocessing_timer.stop()\n-            asp_timer.resume()\n-            for gfa_state_id, tuple_graph in gfa_state_id_to_tuple_graph.items():\n-                states_in_graph = tuple_graph.get_state_space()\n-                #print(f\"GFA State ID: {gfa_state_id} -> States in Tuple Graph: {states_in_graph}\")\n-            # We count the number of subgoal tuples for which no rule could be found.\n-            # This usually happens if the pool of features is not sufficiently rich.\n-            count_unsat_tuples = 0\n-            tuple_graph_states = set()\n-           \n-\n-            for gfa_state in instance_data.gfa.get_states():\n-                gfa_state_global_idx = gfa_state.get_global_index()\n-                gfa_state_idx = gfa_state.get_index()\n-\n-                if instance_data.gfa.is_deadend_state(gfa_state_idx):\n-                    continue\n-\n-                #print(f\"Keys in gfa_state_global_idx_to_tuple_graph: {list(preprocessing_data.gfa_state_global_idx_to_tuple_graph.keys())}\")\n-                #print(f\"gfa_state_global_idx: {gfa_state_global_idx}\")\n-\n-                tuple_graph = preprocessing_data.gfa_state_global_idx_to_tuple_graph[gfa_state_global_idx]\n-\n-                for distance, group in enumerate(tuple_graph.get_vertices_grouped_by_distance()):\n-                    if distance == 0:\n-                        # We skip subgoal tuples at distance zero because they do not encode progress towards a goal.\n-                        continue\n-                    for vertex in group:\n-                        # Here we find all simplest single sketch rules for a pair (state, subgoal tuple).\n-                        t_idx = vertex.get_index()\n-                        tuple_graph_states.add((gfa_state_global_idx, t_idx))\n-    \n-                        start_time = time.time()\n-                        asp_factory = ASPFactory(encoding_type, enable_goal_separating_features, max_num_rules)\n-                        facts = asp_factory.make_facts(preprocessing_data, iteration_data, gfa_state, t_idx)\n-\n-                        print(f\"Factory time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        start_time = time.time()\n-                        asp_factory.ground(facts)\n-                        print(f\"Grounding time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        start_time = time.time()\n-                        symbolss, returncode = asp_factory.solve_all_opt()\n-                        print(f\"Solving time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        if returncode in [ClingoExitCode.UNSATISFIABLE, ClingoExitCode.EXHAUSTED]:\n-                            print(colored(\"ASP is unsatisfiable or exhausted!\", \"red\", \"on_grey\"))\n-                            print(colored(f\"No sketch of width {width} exists that solves all instances!\", \"red\", \"on_grey\"))\n-                            # There might be tuples where we get unsat.\n-                            # If such cases occur, which is very likely in complex domains, then we must look closer into this.\n-                            count_unsat_tuples += 1\n-                            continue\n-                        elif returncode == ClingoExitCode.UNKNOWN:\n-                            print(colored(\"ASP solving throws unknown error!\", \"red\", \"on_grey\"))\n-                            exit(ExitCode.UNKNOWN)\n-                        elif returncode == ClingoExitCode.INTERRUPTED:\n-                            print(colored(\"ASP solving interrupted!\", \"red\", \"on_grey\"))\n-                            exit(ExitCode.INTERRUPTED)\n-                        else:\n-                            logging.info(f\"Solution found with return code: {returncode}\")\n-\n-                        for symbols in symbolss:\n-                            dlplan_policy = ExplicitDlplanPolicyFactory().make_dlplan_policy_from_answer_set(symbols, preprocessing_data, iteration_data)\n-\n-                            sketch = Sketch(dlplan_policy, width)\n-                            sketches.add(sketch)\n-                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_booleans())\n-                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_numericals())\n-                            print(dlplan_policy)\n-                            \n-\n-    else:\n-        raise Exception(\"No implementation for the given encoding type.\")\n-\n-    preprocessing_timer.stop()\n-    asp_timer.stop()\n-    verification_timer.stop()\n-    total_timer.stop()\n-\n-    print(\"Comparison between Tuple Graph States and State Pair Equivalences:\")\n-    for (s_i, s_j) in tuple_graph_states:\n-        match_found = False\n-        for gfa_state_global_idx, state_pair_equivalence in iteration_data.gfa_state_global_idx_to_state_pair_equivalence.items():\n-            for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n-                for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n-                    if gfa_state_global_idx == s_i and subgoal_gfa_state_global_idx == s_j:\n-                        #print(f\" ({s_i} -> {s_j}) matches with state pair equivalence.\")\n-                        match_found = True\n-                        break\n-                if match_found:\n-                    break\n-\n-        #if not match_found:\n-            #print(f\" ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n-            \n-        inverse_pairs = set()\n-\n-        for gfa_state_global_idx, state_pair_equivalence in iteration_data.gfa_state_global_idx_to_state_pair_equivalence.items():\n-            for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n-                for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n-                    #inverse_pairs.add((gfa_state_global_idx, subgoal_gfa_state_global_idx))\n-                    inverse_pairs.add((subgoal_gfa_state_global_idx, gfa_state_global_idx))\n-\n-        tuple_graph_states_without = set()\n-        tuple_graph_states_with_inverse = []\n-       \n-                \n         for (s_i, s_j) in tuple_graph_states:\n-            if (s_i, s_j) not in inverse_pairs:\n-                tuple_graph_states_without.add((s_i, s_j))\n+            # Check if the inverse exists\n+            if (s_j, s_i) in tuple_graph_states:\n+                tuple_graph_states_with_inverse.add((s_i, s_j))\n             else:\n-                tuple_graph_states_with_inverse.append((s_i, s_j))\n-                \n+                filtered_tuple_graph_states.add((s_i, s_j))\n+\n+        # Print results\n         print(\"Tuple Graph States without inverse pairs:\")\n-        if tuple_graph_states_without:\n-            for state in tuple_graph_states_without:\n+        if filtered_tuple_graph_states:\n+            for state in filtered_tuple_graph_states:\n                 print(state)\n         else:\n-            print(\"No tuple graph states without inverse pairs.\")\n+            print(\"No valid tuple graph states found without inverse pairs.\")\n \n         print(\"\\nTuple Graph States with Inverse Pairs:\")\n         if tuple_graph_states_with_inverse:\n             for state in tuple_graph_states_with_inverse:\n                 print(state)\n         else:\n-            print(\"No tuple graph states with inverse pairs.\")\n-            \n+            print(\"No tuple graph states found with inverse pairs.\")\n+\n+                \n     # Compute feature histograms by complexity\n     total_features_by_complexity = defaultdict(int)\n     for feature in total_features:\n         total_features_by_complexity[feature.compute_complexity()] += 1\n"
                },
                {
                    "date": 1727882099641,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -234,46 +234,40 @@\n \n         #if not match_found:\n             #print(f\" ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n             \n-       # Create sets for tuple graph states and inverse pairs\n-    inverse_pairs = set()\n-    tuple_graph_states = set()  # Populate this with your existing tuple graph states\n+        inverse_pairs = set()\n \n-    # Populate inverse pairs from state pair equivalences\n-    for gfa_state_global_idx, state_pair_equivalence in iteration_data.gfa_state_global_idx_to_state_pair_equivalence.items():\n-        for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n-            for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n-                inverse_pairs.add((gfa_state_global_idx, subgoal_gfa_state_global_idx))\n-                inverse_pairs.add((subgoal_gfa_state_global_idx, gfa_state_global_idx))\n+        for gfa_state_global_idx, state_pair_equivalence in iteration_data.gfa_state_global_idx_to_state_pair_equivalence.items():\n+            for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n+                for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n+                    inverse_pairs.add((gfa_state_global_idx, subgoal_gfa_state_global_idx))\n+                    inverse_pairs.add((subgoal_gfa_state_global_idx, gfa_state_global_idx))\n \n-        # Identify filtered states and states with inverse pairs\n-        filtered_tuple_graph_states = set()\n-        tuple_graph_states_with_inverse = set()\n-\n+        tuple_graph_states_without = set()\n+        tuple_graph_states_with_inverse = []\n+       \n+                \n         for (s_i, s_j) in tuple_graph_states:\n-            # Check if the inverse exists\n-            if (s_j, s_i) in tuple_graph_states:\n-                tuple_graph_states_with_inverse.add((s_i, s_j))\n+            if (s_i, s_j) not in inverse_pairs:\n+                tuple_graph_states_without.add((s_i, s_j))\n             else:\n-                filtered_tuple_graph_states.add((s_i, s_j))\n-\n-        # Print results\n+                tuple_graph_states_with_inverse.append((s_i, s_j))\n+                \n         print(\"Tuple Graph States without inverse pairs:\")\n-        if filtered_tuple_graph_states:\n-            for state in filtered_tuple_graph_states:\n+        if tuple_graph_states_without:\n+            for state in tuple_graph_states_without:\n                 print(state)\n         else:\n-            print(\"No valid tuple graph states found without inverse pairs.\")\n+            print(\"No tuple graph states without inverse pairs.\")\n \n         print(\"\\nTuple Graph States with Inverse Pairs:\")\n         if tuple_graph_states_with_inverse:\n             for state in tuple_graph_states_with_inverse:\n                 print(state)\n         else:\n-            print(\"No tuple graph states found with inverse pairs.\")\n-\n-                \n+            print(\"No tuple graph states with inverse pairs.\")\n+            \n     # Compute feature histograms by complexity\n     total_features_by_complexity = defaultdict(int)\n     for feature in total_features:\n         total_features_by_complexity[feature.compute_complexity()] += 1\n"
                },
                {
                    "date": 1727882334652,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -219,54 +219,43 @@\n     verification_timer.stop()\n     total_timer.stop()\n \n     print(\"Comparison between Tuple Graph States and State Pair Equivalences:\")\n-    for (s_i, s_j) in tuple_graph_states:\n-        match_found = False\n-        for gfa_state_global_idx, state_pair_equivalence in iteration_data.gfa_state_global_idx_to_state_pair_equivalence.items():\n-            for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n-                for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n-                    if gfa_state_global_idx == s_i and subgoal_gfa_state_global_idx == s_j:\n-                        #print(f\" ({s_i} -> {s_j}) matches with state pair equivalence.\")\n-                        match_found = True\n-                        break\n-                if match_found:\n-                    break\n \n-        #if not match_found:\n-            #print(f\" ({s_i} -> {s_j}) does NOT match any state pair equivalence.\")\n-            \n-        inverse_pairs = set()\n+    # Create a set for inverse pairs\n+    inverse_pairs = set()\n+    for gfa_state_global_idx, state_pair_equivalence in iteration_data.gfa_state_global_idx_to_state_pair_equivalence.items():\n+        for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n+            for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n+                inverse_pairs.add((gfa_state_global_idx, subgoal_gfa_state_global_idx))\n+                inverse_pairs.add((subgoal_gfa_state_global_idx, gfa_state_global_idx))\n \n-        for gfa_state_global_idx, state_pair_equivalence in iteration_data.gfa_state_global_idx_to_state_pair_equivalence.items():\n-            for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n-                for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n-                    inverse_pairs.add((gfa_state_global_idx, subgoal_gfa_state_global_idx))\n-                    inverse_pairs.add((subgoal_gfa_state_global_idx, gfa_state_global_idx))\n+    # Initialize sets for storing results\n+    tuple_graph_states_without = set()\n+    tuple_graph_states_with_inverse = []\n \n-        tuple_graph_states_without = set()\n-        tuple_graph_states_with_inverse = []\n-       \n-                \n-        for (s_i, s_j) in tuple_graph_states:\n-            if (s_i, s_j) not in inverse_pairs:\n-                tuple_graph_states_without.add((s_i, s_j))\n-            else:\n-                tuple_graph_states_with_inverse.append((s_i, s_j))\n-                \n-        print(\"Tuple Graph States without inverse pairs:\")\n-        if tuple_graph_states_without:\n-            for state in tuple_graph_states_without:\n-                print(state)\n+    # Classify tuple graph states\n+    for (s_i, s_j) in tuple_graph_states:\n+        if (s_i, s_j) in inverse_pairs and (s_j, s_i) in inverse_pairs:\n+            tuple_graph_states_with_inverse.append((s_i, s_j))\n         else:\n-            print(\"No tuple graph states without inverse pairs.\")\n+            tuple_graph_states_without.add((s_i, s_j))\n \n-        print(\"\\nTuple Graph States with Inverse Pairs:\")\n-        if tuple_graph_states_with_inverse:\n-            for state in tuple_graph_states_with_inverse:\n-                print(state)\n-        else:\n-            print(\"No tuple graph states with inverse pairs.\")\n+    # Print results\n+    print(\"Tuple Graph States without inverse pairs:\")\n+    if tuple_graph_states_without:\n+        for state in tuple_graph_states_without:\n+            print(state)\n+    else:\n+        print(\"No tuple graph states without inverse pairs.\")\n+\n+    print(\"\\nTuple Graph States with Inverse Pairs:\")\n+    if tuple_graph_states_with_inverse:\n+        for state in tuple_graph_states_with_inverse:\n+            print(state)\n+    else:\n+        print(\"No tuple graph states with inverse pairs.\")\n+\n             \n     # Compute feature histograms by complexity\n     total_features_by_complexity = defaultdict(int)\n     for feature in total_features:\n"
                },
                {
                    "date": 1727882435457,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -228,17 +228,17 @@\n             for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n                 inverse_pairs.add((gfa_state_global_idx, subgoal_gfa_state_global_idx))\n                 inverse_pairs.add((subgoal_gfa_state_global_idx, gfa_state_global_idx))\n \n-    # Initialize sets for storing results\n+    # Initialize sets for results\n     tuple_graph_states_without = set()\n-    tuple_graph_states_with_inverse = []\n+    tuple_graph_states_with_inverse = set()\n \n     # Classify tuple graph states\n     for (s_i, s_j) in tuple_graph_states:\n         if (s_i, s_j) in inverse_pairs and (s_j, s_i) in inverse_pairs:\n-            tuple_graph_states_with_inverse.append((s_i, s_j))\n-        else:\n+            tuple_graph_states_with_inverse.add((s_i, s_j))\n+        elif (s_i, s_j) not in inverse_pairs:\n             tuple_graph_states_without.add((s_i, s_j))\n \n     # Print results\n     print(\"Tuple Graph States without inverse pairs:\")\n@@ -254,8 +254,9 @@\n             print(state)\n     else:\n         print(\"No tuple graph states with inverse pairs.\")\n \n+\n             \n     # Compute feature histograms by complexity\n     total_features_by_complexity = defaultdict(int)\n     for feature in total_features:\n"
                },
                {
                    "date": 1727882602620,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,288 @@\n+import logging\n+import time\n+from collections import defaultdict\n+from pathlib import Path\n+from termcolor import colored\n+from typing import List, MutableSet, Dict\n+import clingo\n+import pymimir as mm\n+from dlplan.policy import PolicyMinimizer, NamedBoolean, NamedNumerical\n+from collections import defaultdict\n+from .src.exit_codes import ExitCode\n+from .src.iteration import EncodingType, ASPFactory, ClingoExitCode, IterationData, LearningStatistics, Sketch, D2sepDlplanPolicyFactory, ExplicitDlplanPolicyFactory, compute_feature_pool, compute_per_state_feature_valuations, compute_state_pair_equivalences, compute_tuple_graph_equivalences, minimize_tuple_graph_equivalences\n+from .src.util import Timer, create_experiment_workspace, change_working_directory, write_file, change_dir, memory_usage, add_console_handler, print_separation_line\n+from .src.preprocessing import InstanceData, PreprocessingData, StateFinder, compute_instance_datas, compute_tuple_graphs\n+\n+def learn_sketch_for_problem_class(\n+    domain_filepath: Path,\n+    problems_directory: Path,\n+    workspace: Path,\n+    width: int,\n+    disable_closed_Q: bool = True,\n+    max_num_states_per_instance: int = 10000,\n+    max_time_per_instance: int = 10,\n+    encoding_type: EncodingType = EncodingType.EXPLICIT,\n+    max_num_rules: int = 1,\n+    enable_goal_separating_features: bool = True,\n+    disable_feature_generation: bool = True,\n+    enable_incomplete_feature_pruning: bool = False,\n+    concept_complexity_limit: int = 9,\n+    role_complexity_limit: int = 9,\n+    boolean_complexity_limit: int = 9,\n+    count_numerical_complexity_limit: int = 9,\n+    distance_numerical_complexity_limit: int = 9,\n+    feature_limit: int = 1000000,\n+    additional_booleans: List[str] = None,\n+    additional_numericals: List[str] = None,\n+    enable_dump_files: bool = False,\n+):\n+    # Initialize variables and setup\n+    if additional_booleans is None:\n+        additional_booleans = []\n+    if additional_numericals is None:\n+        additional_numericals = []\n+    instance_filepaths = list(problems_directory.iterdir())\n+   \n+\n+    add_console_handler(logging.getLogger(), logging.INFO)\n+    create_experiment_workspace(workspace)\n+    change_working_directory(workspace)\n+\n+    total_timer = Timer()\n+    preprocessing_timer = Timer()\n+    asp_timer = Timer(stopped=True)\n+    verification_timer = Timer(stopped=True)\n+    iteration_data = IterationData()\n+\n+    # Generate data\n+    with change_dir(\"input\"):\n+        logging.info(colored(\"Constructing InstanceDatas...\", \"blue\", \"on_grey\"))\n+        domain_data, instance_datas, num_ss_states, num_gfa_states = compute_instance_datas(domain_filepath, instance_filepaths, disable_closed_Q, max_num_states_per_instance, max_time_per_instance, enable_dump_files)\n+        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+        if instance_datas is None:\n+            raise Exception(\"Failed to create InstanceDatas.\")\n+\n+        state_finder = StateFinder(domain_data, instance_datas)\n+\n+        logging.info(colored(\"Initializing TupleGraphs...\", \"blue\", \"on_grey\"))\n+        gfa_state_id_to_tuple_graph: Dict[int, mm.TupleGraph] = compute_tuple_graphs(domain_data, instance_datas, state_finder, width, enable_dump_files)\n+        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+    preprocessing_data = PreprocessingData(domain_data, instance_datas, state_finder, gfa_state_id_to_tuple_graph)\n+    preprocessing_timer.stop()\n+\n+    if not preprocessing_data.instance_datas:\n+        raise RuntimeError(\"Data is empty\")\n+\n+    sketches = set()\n+    total_features = set()\n+    sketch_features = set()\n+    # Learn sketch\n+    if encoding_type == EncodingType.EXPLICIT:\n+        create_experiment_workspace(workspace)\n+        preprocessing_timer.resume()\n+\n+        for instance_data in preprocessing_data.instance_datas:\n+            print(f\"Instance {instance_data.idx}:\")\n+            for state in instance_data.mimir_ss.get_states():\n+                    state_data = state.get_state()\n+                    fluents = state_data.get_fluent_atoms()\n+                    #print(f\"State {state.get_index()} fluents: {fluents}\")\n+            # TODO: when is the best time to generate features?\n+            # Based on current instance currently looks most reasonable.\n+            # It could be restricted to the states in the tuple graph as well,\n+            # which reduces the number of rules, but not fully sure yet about the consequences in the bigger picture.\n+            iteration_data = IterationData()\n+            iteration_data.instance_datas = [instance_data]\n+            iteration_data.gfa_states = instance_data.gfa.get_states()\n+\n+            # Report some progress\n+            write_file(f\"{instance_data.idx}.dot\", instance_data.dlplan_ss.to_dot(1))\n+            print(\"     \", end=\"\")\n+            print(\"id:\", instance_data.idx,\n+                    \"problem_filepath:\", instance_data.mimir_ss.get_problem().get_filepath(),\n+                    \"num_states:\", instance_data.mimir_ss.get_num_states(),\n+                    \"num_state_equivalences:\", instance_data.gfa.get_num_states())\n+\n+            logging.info(colored(\"Initializing DomainFeatureData...\", \"blue\", \"on_grey\"))\n+            iteration_data.feature_pool = compute_feature_pool(\n+                preprocessing_data,\n+                iteration_data,\n+                gfa_state_id_to_tuple_graph,\n+                state_finder,\n+                disable_feature_generation,\n+                enable_incomplete_feature_pruning,\n+                concept_complexity_limit,\n+                role_complexity_limit,\n+                boolean_complexity_limit,\n+                count_numerical_complexity_limit,\n+                distance_numerical_complexity_limit,\n+                feature_limit,\n+                additional_booleans,\n+                additional_numericals\n+            )\n+            # Add features to features that were generated\n+            total_features.update(feature.dlplan_feature for feature in iteration_data.feature_pool)\n+\n+            logging.info(colored(\"Constructing PerStateFeatureValuations...\", \"blue\", \"on_grey\"))\n+            iteration_data.gfa_state_global_idx_to_feature_evaluations = compute_per_state_feature_valuations(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Constructing StatePairEquivalenceDatas...\", \"blue\", \"on_grey\"))\n+            iteration_data.state_pair_equivalences, iteration_data.gfa_state_global_idx_to_state_pair_equivalence = compute_state_pair_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Constructing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n+            iteration_data.gfa_state_global_idx_to_tuple_graph_equivalence = compute_tuple_graph_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+\n+            logging.info(colored(\"Minimizing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n+            minimize_tuple_graph_equivalences(preprocessing_data, iteration_data)\n+            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n+            preprocessing_timer.stop()\n+            asp_timer.resume()\n+            for gfa_state_id, tuple_graph in gfa_state_id_to_tuple_graph.items():\n+                states_in_graph = tuple_graph.get_state_space()\n+                #print(f\"GFA State ID: {gfa_state_id} -> States in Tuple Graph: {states_in_graph}\")\n+            # We count the number of subgoal tuples for which no rule could be found.\n+            # This usually happens if the pool of features is not sufficiently rich.\n+            count_unsat_tuples = 0\n+            tuple_graph_states = set()\n+           \n+\n+            for gfa_state in instance_data.gfa.get_states():\n+                gfa_state_global_idx = gfa_state.get_global_index()\n+                gfa_state_idx = gfa_state.get_index()\n+\n+                if instance_data.gfa.is_deadend_state(gfa_state_idx):\n+                    continue\n+\n+                #print(f\"Keys in gfa_state_global_idx_to_tuple_graph: {list(preprocessing_data.gfa_state_global_idx_to_tuple_graph.keys())}\")\n+                #print(f\"gfa_state_global_idx: {gfa_state_global_idx}\")\n+\n+                tuple_graph = preprocessing_data.gfa_state_global_idx_to_tuple_graph[gfa_state_global_idx]\n+\n+                for distance, group in enumerate(tuple_graph.get_vertices_grouped_by_distance()):\n+                    if distance == 0:\n+                        # We skip subgoal tuples at distance zero because they do not encode progress towards a goal.\n+                        continue\n+                    for vertex in group:\n+                        # Here we find all simplest single sketch rules for a pair (state, subgoal tuple).\n+                        t_idx = vertex.get_index()\n+                        tuple_graph_states.add((gfa_state_global_idx, t_idx))\n+    \n+                        start_time = time.time()\n+                        asp_factory = ASPFactory(encoding_type, enable_goal_separating_features, max_num_rules)\n+                        facts = asp_factory.make_facts(preprocessing_data, iteration_data, gfa_state, t_idx)\n+\n+                        print(f\"Factory time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        start_time = time.time()\n+                        asp_factory.ground(facts)\n+                        print(f\"Grounding time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        start_time = time.time()\n+                        symbolss, returncode = asp_factory.solve_all_opt()\n+                        print(f\"Solving time: {(time.time() - start_time):.2f} seconds\")\n+\n+                        if returncode in [ClingoExitCode.UNSATISFIABLE, ClingoExitCode.EXHAUSTED]:\n+                            print(colored(\"ASP is unsatisfiable or exhausted!\", \"red\", \"on_grey\"))\n+                            print(colored(f\"No sketch of width {width} exists that solves all instances!\", \"red\", \"on_grey\"))\n+                            # There might be tuples where we get unsat.\n+                            # If such cases occur, which is very likely in complex domains, then we must look closer into this.\n+                            count_unsat_tuples += 1\n+                            continue\n+                        elif returncode == ClingoExitCode.UNKNOWN:\n+                            print(colored(\"ASP solving throws unknown error!\", \"red\", \"on_grey\"))\n+                            exit(ExitCode.UNKNOWN)\n+                        elif returncode == ClingoExitCode.INTERRUPTED:\n+                            print(colored(\"ASP solving interrupted!\", \"red\", \"on_grey\"))\n+                            exit(ExitCode.INTERRUPTED)\n+                        else:\n+                            logging.info(f\"Solution found with return code: {returncode}\")\n+\n+                        for symbols in symbolss:\n+                            dlplan_policy = ExplicitDlplanPolicyFactory().make_dlplan_policy_from_answer_set(symbols, preprocessing_data, iteration_data)\n+\n+                            sketch = Sketch(dlplan_policy, width)\n+                            sketches.add(sketch)\n+                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_booleans())\n+                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_numericals())\n+                            print(dlplan_policy)\n+                            \n+\n+    else:\n+        raise Exception(\"No implementation for the given encoding type.\")\n+\n+    preprocessing_timer.stop()\n+    asp_timer.stop()\n+    verification_timer.stop()\n+    total_timer.stop()\n+\n+    print(\"Comparison between Tuple Graph States and State Pair Equivalences:\")\n+\n+    # Create a set for inverse pairs\n+    inverse_pairs = set()\n+    for gfa_state_global_idx, state_pair_equivalence in iteration_data.gfa_state_global_idx_to_state_pair_equivalence.items():\n+        for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n+            for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n+                inverse_pairs.add((gfa_state_global_idx, subgoal_gfa_state_global_idx))\n+                inverse_pairs.add((subgoal_gfa_state_global_idx, gfa_state_global_idx))\n+\n+    # Initialize sets for storing results\n+    tuple_graph_states_without = set()\n+    tuple_graph_states_with_inverse = []\n+\n+    # Classify tuple graph states\n+    for (s_i, s_j) in tuple_graph_states:\n+        if (s_j, s_i) in inverse_pairs:\n+            tuple_graph_states_with_inverse.append((s_i, s_j))\n+        else:\n+            tuple_graph_states_without.add((s_i, s_j))\n+\n+    # Print results\n+    print(\"Tuple Graph States without inverse pairs:\")\n+    if tuple_graph_states_without:\n+        for state in tuple_graph_states_without:\n+            print(state)\n+    else:\n+        print(\"No tuple graph states without inverse pairs.\")\n+\n+    print(\"\\nTuple Graph States with Inverse Pairs:\")\n+    if tuple_graph_states_with_inverse:\n+        for state in tuple_graph_states_with_inverse:\n+            print(state)\n+    else:\n+        print(\"No tuple graph states with inverse pairs.\")\n+\n+\n+            \n+    # Compute feature histograms by complexity\n+    total_features_by_complexity = defaultdict(int)\n+    for feature in total_features:\n+        total_features_by_complexity[feature.compute_complexity()] += 1\n+    total_features_by_complexity = {key: total_features_by_complexity[key] for key in sorted(total_features_by_complexity)}\n+\n+    sketch_features_by_complexity = defaultdict(int)\n+    for feature in sketch_features:\n+        sketch_features_by_complexity[feature.compute_complexity()] += 1\n+    sketch_features_by_complexity = {key: sketch_features_by_complexity[key] for key in sorted(sketch_features_by_complexity)}\n+\n+    # Output the result\n+    with change_dir(\"output\"):\n+        print_separation_line()\n+        logging.info(colored(\"Summary:\", \"green\", \"on_grey\"))\n+\n+        print(f\"Preprocessing time: {int(preprocessing_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"ASP time: {int(asp_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Verification time: {int(verification_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Total time: {int(total_timer.get_elapsed_sec()) + 1} seconds.\")\n+        print(f\"Total memory: {int(memory_usage() / 1024)} GiB.\")\n+        print(f\"Total number of states: {num_ss_states}\")\n+        print(f\"Total number of abstract states: {num_gfa_states}\")\n+        print(f\"Number of unsat tuples: {count_unsat_tuples}\")\n+        print(f\"Number of sketch rules: {len(sketches)}\")\n+        print(f\"Number of total features: {len(total_features)}\")\n+        print(f\"Number of sketch features: {len(sketch_features)}\")\n+        print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n+        print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n"
                },
                {
                    "date": 1727882617032,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -285,292 +285,4 @@\n         print(f\"Number of total features: {len(total_features)}\")\n         print(f\"Number of sketch features: {len(sketch_features)}\")\n         print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n         print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n-import logging\n-import time\n-from collections import defaultdict\n-from pathlib import Path\n-from termcolor import colored\n-from typing import List, MutableSet, Dict\n-import clingo\n-import pymimir as mm\n-from dlplan.policy import PolicyMinimizer, NamedBoolean, NamedNumerical\n-from collections import defaultdict\n-from .src.exit_codes import ExitCode\n-from .src.iteration import EncodingType, ASPFactory, ClingoExitCode, IterationData, LearningStatistics, Sketch, D2sepDlplanPolicyFactory, ExplicitDlplanPolicyFactory, compute_feature_pool, compute_per_state_feature_valuations, compute_state_pair_equivalences, compute_tuple_graph_equivalences, minimize_tuple_graph_equivalences\n-from .src.util import Timer, create_experiment_workspace, change_working_directory, write_file, change_dir, memory_usage, add_console_handler, print_separation_line\n-from .src.preprocessing import InstanceData, PreprocessingData, StateFinder, compute_instance_datas, compute_tuple_graphs\n-\n-def learn_sketch_for_problem_class(\n-    domain_filepath: Path,\n-    problems_directory: Path,\n-    workspace: Path,\n-    width: int,\n-    disable_closed_Q: bool = True,\n-    max_num_states_per_instance: int = 10000,\n-    max_time_per_instance: int = 10,\n-    encoding_type: EncodingType = EncodingType.EXPLICIT,\n-    max_num_rules: int = 1,\n-    enable_goal_separating_features: bool = True,\n-    disable_feature_generation: bool = True,\n-    enable_incomplete_feature_pruning: bool = False,\n-    concept_complexity_limit: int = 9,\n-    role_complexity_limit: int = 9,\n-    boolean_complexity_limit: int = 9,\n-    count_numerical_complexity_limit: int = 9,\n-    distance_numerical_complexity_limit: int = 9,\n-    feature_limit: int = 1000000,\n-    additional_booleans: List[str] = None,\n-    additional_numericals: List[str] = None,\n-    enable_dump_files: bool = False,\n-):\n-    # Initialize variables and setup\n-    if additional_booleans is None:\n-        additional_booleans = []\n-    if additional_numericals is None:\n-        additional_numericals = []\n-    instance_filepaths = list(problems_directory.iterdir())\n-   \n-\n-    add_console_handler(logging.getLogger(), logging.INFO)\n-    create_experiment_workspace(workspace)\n-    change_working_directory(workspace)\n-\n-    total_timer = Timer()\n-    preprocessing_timer = Timer()\n-    asp_timer = Timer(stopped=True)\n-    verification_timer = Timer(stopped=True)\n-    iteration_data = IterationData()\n-\n-    # Generate data\n-    with change_dir(\"input\"):\n-        logging.info(colored(\"Constructing InstanceDatas...\", \"blue\", \"on_grey\"))\n-        domain_data, instance_datas, num_ss_states, num_gfa_states = compute_instance_datas(domain_filepath, instance_filepaths, disable_closed_Q, max_num_states_per_instance, max_time_per_instance, enable_dump_files)\n-        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-        if instance_datas is None:\n-            raise Exception(\"Failed to create InstanceDatas.\")\n-\n-        state_finder = StateFinder(domain_data, instance_datas)\n-\n-        logging.info(colored(\"Initializing TupleGraphs...\", \"blue\", \"on_grey\"))\n-        gfa_state_id_to_tuple_graph: Dict[int, mm.TupleGraph] = compute_tuple_graphs(domain_data, instance_datas, state_finder, width, enable_dump_files)\n-        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-    preprocessing_data = PreprocessingData(domain_data, instance_datas, state_finder, gfa_state_id_to_tuple_graph)\n-    preprocessing_timer.stop()\n-\n-    if not preprocessing_data.instance_datas:\n-        raise RuntimeError(\"Data is empty\")\n-\n-    sketches = set()\n-    total_features = set()\n-    sketch_features = set()\n-    # Learn sketch\n-    if encoding_type == EncodingType.EXPLICIT:\n-        create_experiment_workspace(workspace)\n-        preprocessing_timer.resume()\n-\n-        for instance_data in preprocessing_data.instance_datas:\n-            print(f\"Instance {instance_data.idx}:\")\n-            for state in instance_data.mimir_ss.get_states():\n-                    state_data = state.get_state()\n-                    fluents = state_data.get_fluent_atoms()\n-                    #print(f\"State {state.get_index()} fluents: {fluents}\")\n-            # TODO: when is the best time to generate features?\n-            # Based on current instance currently looks most reasonable.\n-            # It could be restricted to the states in the tuple graph as well,\n-            # which reduces the number of rules, but not fully sure yet about the consequences in the bigger picture.\n-            iteration_data = IterationData()\n-            iteration_data.instance_datas = [instance_data]\n-            iteration_data.gfa_states = instance_data.gfa.get_states()\n-\n-            # Report some progress\n-            write_file(f\"{instance_data.idx}.dot\", instance_data.dlplan_ss.to_dot(1))\n-            print(\"     \", end=\"\")\n-            print(\"id:\", instance_data.idx,\n-                    \"problem_filepath:\", instance_data.mimir_ss.get_problem().get_filepath(),\n-                    \"num_states:\", instance_data.mimir_ss.get_num_states(),\n-                    \"num_state_equivalences:\", instance_data.gfa.get_num_states())\n-\n-            logging.info(colored(\"Initializing DomainFeatureData...\", \"blue\", \"on_grey\"))\n-            iteration_data.feature_pool = compute_feature_pool(\n-                preprocessing_data,\n-                iteration_data,\n-                gfa_state_id_to_tuple_graph,\n-                state_finder,\n-                disable_feature_generation,\n-                enable_incomplete_feature_pruning,\n-                concept_complexity_limit,\n-                role_complexity_limit,\n-                boolean_complexity_limit,\n-                count_numerical_complexity_limit,\n-                distance_numerical_complexity_limit,\n-                feature_limit,\n-                additional_booleans,\n-                additional_numericals\n-            )\n-            # Add features to features that were generated\n-            total_features.update(feature.dlplan_feature for feature in iteration_data.feature_pool)\n-\n-            logging.info(colored(\"Constructing PerStateFeatureValuations...\", \"blue\", \"on_grey\"))\n-            iteration_data.gfa_state_global_idx_to_feature_evaluations = compute_per_state_feature_valuations(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Constructing StatePairEquivalenceDatas...\", \"blue\", \"on_grey\"))\n-            iteration_data.state_pair_equivalences, iteration_data.gfa_state_global_idx_to_state_pair_equivalence = compute_state_pair_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Constructing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n-            iteration_data.gfa_state_global_idx_to_tuple_graph_equivalence = compute_tuple_graph_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-\n-            logging.info(colored(\"Minimizing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n-            minimize_tuple_graph_equivalences(preprocessing_data, iteration_data)\n-            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n-            preprocessing_timer.stop()\n-            asp_timer.resume()\n-            for gfa_state_id, tuple_graph in gfa_state_id_to_tuple_graph.items():\n-                states_in_graph = tuple_graph.get_state_space()\n-                #print(f\"GFA State ID: {gfa_state_id} -> States in Tuple Graph: {states_in_graph}\")\n-            # We count the number of subgoal tuples for which no rule could be found.\n-            # This usually happens if the pool of features is not sufficiently rich.\n-            count_unsat_tuples = 0\n-            tuple_graph_states = set()\n-           \n-\n-            for gfa_state in instance_data.gfa.get_states():\n-                gfa_state_global_idx = gfa_state.get_global_index()\n-                gfa_state_idx = gfa_state.get_index()\n-\n-                if instance_data.gfa.is_deadend_state(gfa_state_idx):\n-                    continue\n-\n-                #print(f\"Keys in gfa_state_global_idx_to_tuple_graph: {list(preprocessing_data.gfa_state_global_idx_to_tuple_graph.keys())}\")\n-                #print(f\"gfa_state_global_idx: {gfa_state_global_idx}\")\n-\n-                tuple_graph = preprocessing_data.gfa_state_global_idx_to_tuple_graph[gfa_state_global_idx]\n-\n-                for distance, group in enumerate(tuple_graph.get_vertices_grouped_by_distance()):\n-                    if distance == 0:\n-                        # We skip subgoal tuples at distance zero because they do not encode progress towards a goal.\n-                        continue\n-                    for vertex in group:\n-                        # Here we find all simplest single sketch rules for a pair (state, subgoal tuple).\n-                        t_idx = vertex.get_index()\n-                        tuple_graph_states.add((gfa_state_global_idx, t_idx))\n-    \n-                        start_time = time.time()\n-                        asp_factory = ASPFactory(encoding_type, enable_goal_separating_features, max_num_rules)\n-                        facts = asp_factory.make_facts(preprocessing_data, iteration_data, gfa_state, t_idx)\n-\n-                        print(f\"Factory time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        start_time = time.time()\n-                        asp_factory.ground(facts)\n-                        print(f\"Grounding time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        start_time = time.time()\n-                        symbolss, returncode = asp_factory.solve_all_opt()\n-                        print(f\"Solving time: {(time.time() - start_time):.2f} seconds\")\n-\n-                        if returncode in [ClingoExitCode.UNSATISFIABLE, ClingoExitCode.EXHAUSTED]:\n-                            print(colored(\"ASP is unsatisfiable or exhausted!\", \"red\", \"on_grey\"))\n-                            print(colored(f\"No sketch of width {width} exists that solves all instances!\", \"red\", \"on_grey\"))\n-                            # There might be tuples where we get unsat.\n-                            # If such cases occur, which is very likely in complex domains, then we must look closer into this.\n-                            count_unsat_tuples += 1\n-                            continue\n-                        elif returncode == ClingoExitCode.UNKNOWN:\n-                            print(colored(\"ASP solving throws unknown error!\", \"red\", \"on_grey\"))\n-                            exit(ExitCode.UNKNOWN)\n-                        elif returncode == ClingoExitCode.INTERRUPTED:\n-                            print(colored(\"ASP solving interrupted!\", \"red\", \"on_grey\"))\n-                            exit(ExitCode.INTERRUPTED)\n-                        else:\n-                            logging.info(f\"Solution found with return code: {returncode}\")\n-\n-                        for symbols in symbolss:\n-                            dlplan_policy = ExplicitDlplanPolicyFactory().make_dlplan_policy_from_answer_set(symbols, preprocessing_data, iteration_data)\n-\n-                            sketch = Sketch(dlplan_policy, width)\n-                            sketches.add(sketch)\n-                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_booleans())\n-                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_numericals())\n-                            print(dlplan_policy)\n-                            \n-\n-    else:\n-        raise Exception(\"No implementation for the given encoding type.\")\n-\n-    preprocessing_timer.stop()\n-    asp_timer.stop()\n-    verification_timer.stop()\n-    total_timer.stop()\n-\n-    print(\"Comparison between Tuple Graph States and State Pair Equivalences:\")\n-\n-    # Create a set for inverse pairs\n-    inverse_pairs = set()\n-    for gfa_state_global_idx, state_pair_equivalence in iteration_data.gfa_state_global_idx_to_state_pair_equivalence.items():\n-        for r_idx, subgoal_gfa_state_global_idxs in state_pair_equivalence.r_idx_to_subgoal_gfa_state_global_idxs.items():\n-            for subgoal_gfa_state_global_idx in subgoal_gfa_state_global_idxs:\n-                inverse_pairs.add((gfa_state_global_idx, subgoal_gfa_state_global_idx))\n-                inverse_pairs.add((subgoal_gfa_state_global_idx, gfa_state_global_idx))\n-\n-    # Initialize sets for results\n-    tuple_graph_states_without = set()\n-    tuple_graph_states_with_inverse = set()\n-\n-    # Classify tuple graph states\n-    for (s_i, s_j) in tuple_graph_states:\n-        if (s_i, s_j) in inverse_pairs and (s_j, s_i) in inverse_pairs:\n-            tuple_graph_states_with_inverse.add((s_i, s_j))\n-        elif (s_i, s_j) not in inverse_pairs:\n-            tuple_graph_states_without.add((s_i, s_j))\n-\n-    # Print results\n-    print(\"Tuple Graph States without inverse pairs:\")\n-    if tuple_graph_states_without:\n-        for state in tuple_graph_states_without:\n-            print(state)\n-    else:\n-        print(\"No tuple graph states without inverse pairs.\")\n-\n-    print(\"\\nTuple Graph States with Inverse Pairs:\")\n-    if tuple_graph_states_with_inverse:\n-        for state in tuple_graph_states_with_inverse:\n-            print(state)\n-    else:\n-        print(\"No tuple graph states with inverse pairs.\")\n-\n-\n-            \n-    # Compute feature histograms by complexity\n-    total_features_by_complexity = defaultdict(int)\n-    for feature in total_features:\n-        total_features_by_complexity[feature.compute_complexity()] += 1\n-    total_features_by_complexity = {key: total_features_by_complexity[key] for key in sorted(total_features_by_complexity)}\n-\n-    sketch_features_by_complexity = defaultdict(int)\n-    for feature in sketch_features:\n-        sketch_features_by_complexity[feature.compute_complexity()] += 1\n-    sketch_features_by_complexity = {key: sketch_features_by_complexity[key] for key in sorted(sketch_features_by_complexity)}\n-\n-    # Output the result\n-    with change_dir(\"output\"):\n-        print_separation_line()\n-        logging.info(colored(\"Summary:\", \"green\", \"on_grey\"))\n-\n-        print(f\"Preprocessing time: {int(preprocessing_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"ASP time: {int(asp_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Verification time: {int(verification_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Total time: {int(total_timer.get_elapsed_sec()) + 1} seconds.\")\n-        print(f\"Total memory: {int(memory_usage() / 1024)} GiB.\")\n-        print(f\"Total number of states: {num_ss_states}\")\n-        print(f\"Total number of abstract states: {num_gfa_states}\")\n-        print(f\"Number of unsat tuples: {count_unsat_tuples}\")\n-        print(f\"Number of sketch rules: {len(sketches)}\")\n-        print(f\"Number of total features: {len(total_features)}\")\n-        print(f\"Number of sketch features: {len(sketch_features)}\")\n-        print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n-        print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n"
                }
            ],
            "date": 1727819756744,
            "name": "Commit-0",
            "content": "import logging\nimport time\nfrom collections import defaultdict\nfrom pathlib import Path\nfrom termcolor import colored\nfrom typing import List, MutableSet, Dict\nimport clingo\nimport pymimir as mm\nfrom dlplan.policy import PolicyMinimizer, NamedBoolean, NamedNumerical\nfrom collections import defaultdict\nfrom .src.exit_codes import ExitCode\nfrom .src.iteration import EncodingType, ASPFactory, ClingoExitCode, IterationData, LearningStatistics, Sketch, D2sepDlplanPolicyFactory, ExplicitDlplanPolicyFactory, compute_feature_pool, compute_per_state_feature_valuations, compute_state_pair_equivalences, compute_tuple_graph_equivalences, minimize_tuple_graph_equivalences\nfrom .src.util import Timer, create_experiment_workspace, change_working_directory, write_file, change_dir, memory_usage, add_console_handler, print_separation_line\nfrom .src.preprocessing import InstanceData, PreprocessingData, StateFinder, compute_instance_datas, compute_tuple_graphs\n\ndef learn_sketch_for_problem_class(\n    domain_filepath: Path,\n    problems_directory: Path,\n    workspace: Path,\n    width: int,\n    disable_closed_Q: bool = True,\n    max_num_states_per_instance: int = 10000,\n    max_time_per_instance: int = 10,\n    encoding_type: EncodingType = EncodingType.EXPLICIT,\n    max_num_rules: int = 1,\n    enable_goal_separating_features: bool = True,\n    disable_feature_generation: bool = True,\n    enable_incomplete_feature_pruning: bool = False,\n    concept_complexity_limit: int = 9,\n    role_complexity_limit: int = 9,\n    boolean_complexity_limit: int = 9,\n    count_numerical_complexity_limit: int = 9,\n    distance_numerical_complexity_limit: int = 9,\n    feature_limit: int = 1000000,\n    additional_booleans: List[str] = None,\n    additional_numericals: List[str] = None,\n    enable_dump_files: bool = False,\n):\n    # Initialize variables and setup\n    if additional_booleans is None:\n        additional_booleans = []\n    if additional_numericals is None:\n        additional_numericals = []\n    instance_filepaths = list(problems_directory.iterdir())\n   \n\n    add_console_handler(logging.getLogger(), logging.INFO)\n    create_experiment_workspace(workspace)\n    change_working_directory(workspace)\n\n    total_timer = Timer()\n    preprocessing_timer = Timer()\n    asp_timer = Timer(stopped=True)\n    verification_timer = Timer(stopped=True)\n    iteration_data = IterationData()\n\n    # Generate data\n    with change_dir(\"input\"):\n        logging.info(colored(\"Constructing InstanceDatas...\", \"blue\", \"on_grey\"))\n        domain_data, instance_datas, num_ss_states, num_gfa_states = compute_instance_datas(domain_filepath, instance_filepaths, disable_closed_Q, max_num_states_per_instance, max_time_per_instance, enable_dump_files)\n        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n        if instance_datas is None:\n            raise Exception(\"Failed to create InstanceDatas.\")\n\n        state_finder = StateFinder(domain_data, instance_datas)\n\n        logging.info(colored(\"Initializing TupleGraphs...\", \"blue\", \"on_grey\"))\n        gfa_state_id_to_tuple_graph: Dict[int, mm.TupleGraph] = compute_tuple_graphs(domain_data, instance_datas, state_finder, width, enable_dump_files)\n        logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n\n    preprocessing_data = PreprocessingData(domain_data, instance_datas, state_finder, gfa_state_id_to_tuple_graph)\n    preprocessing_timer.stop()\n\n    if not preprocessing_data.instance_datas:\n        raise RuntimeError(\"Data is empty\")\n\n    sketches = set()\n    total_features = set()\n    sketch_features = set()\n    # Learn sketch\n    if encoding_type == EncodingType.EXPLICIT:\n        create_experiment_workspace(workspace)\n        preprocessing_timer.resume()\n\n        for instance_data in preprocessing_data.instance_datas:\n            print(f\"Instance {instance_data.idx}:\")\n            for state in instance_data.mimir_ss.get_states():\n                    state_data = state.get_state()\n                    fluents = state_data.get_fluent_atoms()\n                    print(f\"State {state.get_index()} fluents: {fluents}\")\n            # TODO: when is the best time to generate features?\n            # Based on current instance currently looks most reasonable.\n            # It could be restricted to the states in the tuple graph as well,\n            # which reduces the number of rules, but not fully sure yet about the consequences in the bigger picture.\n            iteration_data = IterationData()\n            iteration_data.instance_datas = [instance_data]\n            iteration_data.gfa_states = instance_data.gfa.get_states()\n\n            # Report some progress\n            write_file(f\"{instance_data.idx}.dot\", instance_data.dlplan_ss.to_dot(1))\n            print(\"     \", end=\"\")\n            print(\"id:\", instance_data.idx,\n                    \"problem_filepath:\", instance_data.mimir_ss.get_problem().get_filepath(),\n                    \"num_states:\", instance_data.mimir_ss.get_num_states(),\n                    \"num_state_equivalences:\", instance_data.gfa.get_num_states())\n\n            logging.info(colored(\"Initializing DomainFeatureData...\", \"blue\", \"on_grey\"))\n            iteration_data.feature_pool = compute_feature_pool(\n                preprocessing_data,\n                iteration_data,\n                gfa_state_id_to_tuple_graph,\n                state_finder,\n                disable_feature_generation,\n                enable_incomplete_feature_pruning,\n                concept_complexity_limit,\n                role_complexity_limit,\n                boolean_complexity_limit,\n                count_numerical_complexity_limit,\n                distance_numerical_complexity_limit,\n                feature_limit,\n                additional_booleans,\n                additional_numericals\n            )\n            # Add features to features that were generated\n            total_features.update(feature.dlplan_feature for feature in iteration_data.feature_pool)\n\n            logging.info(colored(\"Constructing PerStateFeatureValuations...\", \"blue\", \"on_grey\"))\n            iteration_data.gfa_state_global_idx_to_feature_evaluations = compute_per_state_feature_valuations(preprocessing_data, iteration_data)\n            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n\n            logging.info(colored(\"Constructing StatePairEquivalenceDatas...\", \"blue\", \"on_grey\"))\n            iteration_data.state_pair_equivalences, iteration_data.gfa_state_global_idx_to_state_pair_equivalence = compute_state_pair_equivalences(preprocessing_data, iteration_data)\n            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n\n            logging.info(colored(\"Constructing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n            iteration_data.gfa_state_global_idx_to_tuple_graph_equivalence = compute_tuple_graph_equivalences(preprocessing_data, iteration_data)\n            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n\n            logging.info(colored(\"Minimizing TupleGraphEquivalences...\", \"blue\", \"on_grey\"))\n            minimize_tuple_graph_equivalences(preprocessing_data, iteration_data)\n            logging.info(colored(\"..done\", \"blue\", \"on_grey\"))\n            preprocessing_timer.stop()\n            asp_timer.resume()\n\n            # We count the number of subgoal tuples for which no rule could be found.\n            # This usually happens if the pool of features is not sufficiently rich.\n            count_unsat_tuples = 0\n\n            for gfa_state in instance_data.gfa.get_states():\n                gfa_state_global_idx = gfa_state.get_global_index()\n                gfa_state_idx = gfa_state.get_index()\n\n                if instance_data.gfa.is_deadend_state(gfa_state_idx):\n                    continue\n\n                #print(f\"Keys in gfa_state_global_idx_to_tuple_graph: {list(preprocessing_data.gfa_state_global_idx_to_tuple_graph.keys())}\")\n                #print(f\"gfa_state_global_idx: {gfa_state_global_idx}\")\n\n                tuple_graph = preprocessing_data.gfa_state_global_idx_to_tuple_graph[gfa_state_global_idx]\n\n                for distance, group in enumerate(tuple_graph.get_vertices_grouped_by_distance()):\n                    if distance == 0:\n                        # We skip subgoal tuples at distance zero because they do not encode progress towards a goal.\n                        continue\n                    for vertex in group:\n                        # Here we find all simplest single sketch rules for a pair (state, subgoal tuple).\n                        t_idx = vertex.get_index()\n\n                        start_time = time.time()\n                        asp_factory = ASPFactory(encoding_type, enable_goal_separating_features, max_num_rules)\n                        facts = asp_factory.make_facts(preprocessing_data, iteration_data, gfa_state, t_idx)\n\n                        print(f\"Factory time: {(time.time() - start_time):.2f} seconds\")\n\n                        start_time = time.time()\n                        asp_factory.ground(facts)\n                        print(f\"Grounding time: {(time.time() - start_time):.2f} seconds\")\n\n                        start_time = time.time()\n                        symbolss, returncode = asp_factory.solve_all_opt()\n                        print(f\"Solving time: {(time.time() - start_time):.2f} seconds\")\n\n                        if returncode in [ClingoExitCode.UNSATISFIABLE, ClingoExitCode.EXHAUSTED]:\n                            print(colored(\"ASP is unsatisfiable or exhausted!\", \"red\", \"on_grey\"))\n                            print(colored(f\"No sketch of width {width} exists that solves all instances!\", \"red\", \"on_grey\"))\n                            # There might be tuples where we get unsat.\n                            # If such cases occur, which is very likely in complex domains, then we must look closer into this.\n                            count_unsat_tuples += 1\n                            continue\n                        elif returncode == ClingoExitCode.UNKNOWN:\n                            print(colored(\"ASP solving throws unknown error!\", \"red\", \"on_grey\"))\n                            exit(ExitCode.UNKNOWN)\n                        elif returncode == ClingoExitCode.INTERRUPTED:\n                            print(colored(\"ASP solving interrupted!\", \"red\", \"on_grey\"))\n                            exit(ExitCode.INTERRUPTED)\n                        else:\n                            logging.info(f\"Solution found with return code: {returncode}\")\n\n                        for symbols in symbolss:\n                            dlplan_policy = ExplicitDlplanPolicyFactory().make_dlplan_policy_from_answer_set(symbols, preprocessing_data, iteration_data)\n\n                            sketch = Sketch(dlplan_policy, width)\n                            sketches.add(sketch)\n                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_booleans())\n                            sketch_features.update(feature.get_element() for feature in sketch.dlplan_policy.get_numericals())\n                            print(dlplan_policy)\n\n    else:\n        raise Exception(\"No implementation for the given encoding type.\")\n\n    preprocessing_timer.stop()\n    asp_timer.stop()\n    verification_timer.stop()\n    total_timer.stop()\n\n    # Compute feature histograms by complexity\n    total_features_by_complexity = defaultdict(int)\n    for feature in total_features:\n        total_features_by_complexity[feature.compute_complexity()] += 1\n    total_features_by_complexity = {key: total_features_by_complexity[key] for key in sorted(total_features_by_complexity)}\n\n    sketch_features_by_complexity = defaultdict(int)\n    for feature in sketch_features:\n        sketch_features_by_complexity[feature.compute_complexity()] += 1\n    sketch_features_by_complexity = {key: sketch_features_by_complexity[key] for key in sorted(sketch_features_by_complexity)}\n\n    # Output the result\n    with change_dir(\"output\"):\n        print_separation_line()\n        logging.info(colored(\"Summary:\", \"green\", \"on_grey\"))\n\n        print(f\"Preprocessing time: {int(preprocessing_timer.get_elapsed_sec()) + 1} seconds.\")\n        print(f\"ASP time: {int(asp_timer.get_elapsed_sec()) + 1} seconds.\")\n        print(f\"Verification time: {int(verification_timer.get_elapsed_sec()) + 1} seconds.\")\n        print(f\"Total time: {int(total_timer.get_elapsed_sec()) + 1} seconds.\")\n        print(f\"Total memory: {int(memory_usage() / 1024)} GiB.\")\n        print(f\"Total number of states: {num_ss_states}\")\n        print(f\"Total number of abstract states: {num_gfa_states}\")\n        print(f\"Number of unsat tuples: {count_unsat_tuples}\")\n        print(f\"Number of sketch rules: {len(sketches)}\")\n        print(f\"Number of total features: {len(total_features)}\")\n        print(f\"Number of sketch features: {len(sketch_features)}\")\n        print(f\"Number of total features by complexity: {total_features_by_complexity}\")\n        print(f\"Number of sketch features by complexity: {sketch_features_by_complexity}\")\n"
        }
    ]
}